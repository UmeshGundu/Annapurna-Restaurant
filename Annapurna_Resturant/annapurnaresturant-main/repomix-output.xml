This file is a merged representation of the entire codebase, combined into a single document by Repomix.

<file_summary>
This section contains a summary of this file.

<purpose>
This file contains a packed representation of the entire repository's contents.
It is designed to be easily consumable by AI systems for analysis, code review,
or other automated processes.
</purpose>

<file_format>
The content is organized as follows:
1. This summary section
2. Repository information
3. Directory structure
4. Repository files (if enabled)
5. Multiple file entries, each consisting of:
  - File path as an attribute
  - Full contents of the file
</file_format>

<usage_guidelines>
- This file should be treated as read-only. Any changes should be made to the
  original repository files, not this packed version.
- When processing this file, use the file path to distinguish
  between different files in the repository.
- Be aware that this file may contain sensitive information. Handle it with
  the same level of security as you would the original repository.
</usage_guidelines>

<notes>
- Some files may have been excluded based on .gitignore rules and Repomix's configuration
- Binary files are not included in this packed representation. Please refer to the Repository Structure section for a complete list of file paths, including binary files
- Files matching patterns in .gitignore are excluded
- Files matching default ignore patterns are excluded
- Files are sorted by Git change count (files with more changes are at the bottom)
</notes>

</file_summary>

<directory_structure>
.expo/devices.json
.expo/README.md
.expo/web/cache/production/images/favicon/favicon-a4e030697a7571b3e95d31860e4da55d2f98e5e861e2b55e414f45a8556828ba-contain-transparent/favicon-48.png
.gitattributes
.gitignore
api.js
App.js
app.json
assets/android-icon-background.png
assets/android-icon-foreground.png
assets/android-icon-monochrome.png
assets/bananabackground.jpg
assets/bell.png
assets/biryani.png
assets/biryani/chickenbiryani.png
assets/biryani/chillichickenbiryani.png
assets/biryani/eggbiryani.png
assets/biryani/fishbiryani.png
assets/biryani/kajubiryani.png
assets/biryani/muttonbiryani.png
assets/biryani/pannerbiryani.png
assets/biryani/prawnsbiryani.png
assets/biryani/ulavacharubiryani.png
assets/biryani/vegbiryani.png
assets/biryanicm.png
assets/chicken.jpg
assets/chickenb.jpg
assets/desserts.png
assets/desserts/brownie.png
assets/desserts/chocolatecake.png
assets/desserts/cupcake.png
assets/desserts/gulabjamun.png
assets/desserts/icecream.png
assets/desserts/rasmalai.png
assets/drinks.png
assets/drinks/buttermilk.png
assets/drinks/coke.png
assets/drinks/lassi.png
assets/drinks/pepsi.png
assets/drinks/sprite.png
assets/drinks/thumsup.png
assets/favicon.png
assets/fonts/CormorantGaramond-Bold.ttf
assets/fonts/CormorantGaramond-Regular.ttf
assets/fonts/Poppins-Bold.ttf
assets/fonts/Poppins-Medium.ttf
assets/fonts/Poppins-Regular.ttf
assets/fonts/Poppins-SemiBold.ttf
assets/food.png
assets/foood.png
assets/herobg.jpg
assets/home.png
assets/icon.png
assets/logo.png
assets/logotp.png
assets/mainbg.jpg
assets/meals/chickenmeal.png
assets/meals/fishmeal.png
assets/meals/minimeal.png
assets/meals/muttonmeal.png
assets/meals/nonvegmeal.png
assets/meals/paneermeal.png
assets/meals/specialmeal.png
assets/meals/vegmeals.png
assets/mealscm.png
assets/mutton.jpg
assets/muttong.jpeg
assets/muttong.jpg
assets/RestaurantLogo.png
assets/splash-icon.png
assets/starter.png
assets/starters/chicken65.png
assets/starters/chickenchilli.png
assets/starters/chickengarlicbread.png
assets/starters/chickenlollipop.png
assets/starters/chickenmanchurian.png
assets/starters/gobimanchurian.png
assets/starters/muttonchilli.png
assets/starters/paneerchilli.png
assets/starters/vegmanchurian.png
assets/tandoori.png
assets/tandoori/chickenseekhkebab.png
assets/tandoori/chickentikka.png
assets/tandoori/kalmikebab.png
assets/tandoori/mushroomtikka.png
assets/tandoori/paneertikka.png
assets/tandoori/tandoorifull.png
assets/tandoori/tandoorihalf.png
assets/tandoori/tangadikebab.png
Biryani.js
CartContext.js
Cartscreen.js
ContactUsScreen.js
Data.js
Desserts.js
Drinks.js
Editaddress.js
EditProfileScreen.js
HelpFAQScreen.js
Home.js
index.js
Login.js
Meals.js
Menu.js
Onboarding.js
OrderHistoryScreen.js
OrderSuccess.js
Otpscreen.js
package.json
PrivacyPolicyScreen.js
Profile.js
ProfileScreen.js
RateAppScreen.js
README.md
SavedAddressesScreen.js
Signup.js
Starters.js
Success.js
Successup.js
Tandoori.js
Transaction.js
WishlistContext.js
WishlistScreen.js
</directory_structure>

<files>
This section contains the contents of the repository's files.

<file path=".expo/devices.json">
{
  "devices": []
}
</file>

<file path=".expo/README.md">
> Why do I have a folder named ".expo" in my project?

The ".expo" folder is created when an Expo project is started using "expo start" command.

> What do the files contain?

- "devices.json": contains information about devices that have recently opened this project. This is used to populate the "Development sessions" list in your development builds.
- "settings.json": contains the server configuration that is used to serve the application manifest.

> Should I commit the ".expo" folder?

No, you should not share the ".expo" folder. It does not contain any information that is relevant for other developers working on the project, it is specific to your machine.
Upon project creation, the ".expo" folder is already added to your ".gitignore" file.
</file>

<file path=".gitattributes">
# Auto detect text files and perform LF normalization
* text=auto
</file>

<file path=".gitignore">
# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*
.pnpm-debug.log*

# Diagnostic reports (https://nodejs.org/api/report.html)
report.[0-9]*.[0-9]*.[0-9]*.[0-9]*.json

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Directory for instrumented libs generated by jscoverage/JSCover
lib-cov

# Coverage directory used by tools like istanbul
coverage
*.lcov

# nyc test coverage
.nyc_output

# Grunt intermediate storage (https://gruntjs.com/creating-plugins#storing-task-files)
.grunt

# Bower dependency directory (https://bower.io/)
bower_components

# node-waf configuration
.lock-wscript

# Compiled binary addons (https://nodejs.org/api/addons.html)
build/Release

# Dependency directories
node_modules/
jspm_packages/

# Snowpack dependency directory (https://snowpack.dev/)
web_modules/

# TypeScript cache
*.tsbuildinfo

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# Optional stylelint cache
.stylelintcache

# Microbundle cache
.rpt2_cache/
.rts2_cache_cjs/
.rts2_cache_es/
.rts2_cache_umd/

# Optional REPL history
.node_repl_history

# Output of 'npm pack'
*.tgz

# Yarn Integrity file
.yarn-integrity

# dotenv environment variable files
.env
.env.development.local
.env.test.local
.env.production.local
.env.local

# parcel-bundler cache (https://parceljs.org/)
.cache
.parcel-cache

# Next.js build output
.next
out

# Nuxt.js build / generate output
.nuxt
dist

# Gatsby files
.cache/
# Comment in the public line in if your project uses Gatsby and not Next.js
# https://nextjs.org/blog/next-9-1#public-directory-support
# public

# vuepress build output
.vuepress/dist

# vuepress v2.x temp and cache directory
.temp
.cache

# vitepress build output
**/.vitepress/dist

# vitepress cache directory
**/.vitepress/cache

# Docusaurus cache and generated files
.docusaurus

# Serverless directories
.serverless/

# FuseBox cache
.fusebox/

# DynamoDB Local files
.dynamodb/

# TernJS port file
.tern-port

# Stores VSCode versions used for testing VSCode extensions
.vscode-test

# yarn v2
.yarn/cache
.yarn/unplugged
.yarn/build-state.yml
.yarn/install-state.gz
.pnp.*
</file>

<file path="api.js">
import AsyncStorage from '@react-native-async-storage/async-storage';
import { Platform } from "react-native";

const getBaseUrl = () => {
    // 🌐 React Native Web (browser only)
    if (Platform.OS === "web") {
        return `http://${window.location.hostname}:8080/api`;
    }
    // 🤖 Android Emulator
    if (Platform.OS === "android") {
        return "http://10.0.2.2:8080/api";
    }
    // 🍎 iOS Simulator
    if (Platform.OS === "ios") {
        return "http://localhost:8080/api";
    }
    // 📱 Real Device (Expo Go)
    return "http://192.168.0.22:8080/api"; // your laptop IP
};
export const BASE_URL = getBaseUrl();
const getToken = async () => {
    return await AsyncStorage.getItem('token');
};
const authHeaders = async () => {
    const token = await getToken();
    return {
        'Content-Type': 'application/json',
        ...(token ? { Authorization: `Bearer ${token}` } : {}),
    };
};
const handleResponse = async (res) => {
    const json = await res.json();
    if (!json.success) throw new Error(json.message);
    return json.data;
};
// ─── AUTH ───────────────────────────────────────────────────────────────────────
export const login = async (mobile, password) => {
    const res = await fetch(`${BASE_URL}/auth/login`, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ mobile, password }),
    });
    const json = await res.json();
    if (json.success) {
        await AsyncStorage.setItem('token', json.data.token);
        await AsyncStorage.setItem('userId', String(json.data.userId));
        await AsyncStorage.setItem('userName', json.data.name);
    }
    return json;
};
export const register = async (name, mobile, password) => {
    const res = await fetch(`${BASE_URL}/auth/register`, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ name, mobile, password }),
    });
    const json = await res.json();
    if (json.success) {
        await AsyncStorage.setItem('token', json.data.token);
        await AsyncStorage.setItem('userId', String(json.data.userId));
    }
    return json;
};
export const sendOtp = async (mobile) => {
    const res = await fetch(`${BASE_URL}/auth/otp/send`, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ mobile }),
    });
    return res.json();
};
export const verifyOtp = async (mobile, otp) => {
    const res = await fetch(`${BASE_URL}/auth/otp/verify`, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ mobile, otp }),
    });
    return res.json();
};
export const logout = async () => {
    await AsyncStorage.multiRemove(['token', 'userId', 'userName']);
};
// ─── MENU ────────────────────────────────────────────────────────────────────────
export const getAllMenu = async () => {
    const res = await fetch(`${BASE_URL}/menu`, {
        headers: await authHeaders(),
    });
    return handleResponse(res);
};
export const getMenuByCategory = async (category) => {
    const res = await fetch(`${BASE_URL}/menu/category/${category}`, {
        headers: await authHeaders(),
    });
    return handleResponse(res);
};
export const searchMenu = async (query) => {
    const res = await fetch(`${BASE_URL}/menu/search?q=${encodeURIComponent(query)}`, {
        headers: await authHeaders(),
    });
    return handleResponse(res);
};
// ─── CART ────────────────────────────────────────────────────────────────────────
export const getCart = async () => {
    const res = await fetch(`${BASE_URL}/cart`, {
        headers: await authHeaders(),
    });
    return handleResponse(res);
};
export const addToCart = async (menuItemId, quantity = 1) => {
    const res = await fetch(`${BASE_URL}/cart/add`, {
        method: 'POST',
        headers: await authHeaders(),
        body: JSON.stringify({ menuItemId, quantity }),
    });
    return handleResponse(res);
};
export const removeFromCart = async (menuItemId) => {
    const res = await fetch(`${BASE_URL}/cart/remove/${menuItemId}`, {
        method: 'POST',
        headers: await authHeaders(),
    });
    return handleResponse(res);
};
export const clearCart = async () => {
    const res = await fetch(`${BASE_URL}/cart/clear`, {
        method: 'DELETE',
        headers: await authHeaders(),
    });
    return handleResponse(res);
};
// ─── WISHLIST ────────────────────────────────────────────────────────────────────
export const getWishlist = async () => {
    const res = await fetch(`${BASE_URL}/wishlist`, {
        headers: await authHeaders(),
    });
    return handleResponse(res);
};
export const toggleWishlist = async (menuItemId) => {
    const res = await fetch(`${BASE_URL}/wishlist/toggle/${menuItemId}`, {
        method: 'POST',
        headers: await authHeaders(),
    });
    return handleResponse(res);
};
// ─── ORDERS ──────────────────────────────────────────────────────────────────────
export const placeOrder = async (paymentMethod, deliveryAddress, total) => {
    const res = await fetch(`${BASE_URL}/orders/place`, {
        method: 'POST',
        headers: await authHeaders(),
        body: JSON.stringify({ paymentMethod, deliveryAddress, total }),
    });
    return handleResponse(res);
};
export const getOrderHistory = async () => {
    const res = await fetch(`${BASE_URL}/orders/history`, {
        headers: await authHeaders(),
    });
    return handleResponse(res);
};
export const getOrderById = async (orderId) => {
    const res = await fetch(`${BASE_URL}/orders/${orderId}`, {
        headers: await authHeaders(),
    });
    return handleResponse(res);
};
// ─── PROFILE ─────────────────────────────────────────────────────────────────────
export const getProfile = async () => {
    const res = await fetch(`${BASE_URL}/user/profile`, {
        headers: await authHeaders(),
    });
    return handleResponse(res);
};
export const updateProfile = async (data) => {
    const res = await fetch(`${BASE_URL}/user/profile`, {
        method: "PUT",
        headers: await authHeaders(),
        body: JSON.stringify(data),
    });
    return handleResponse(res);
};
// ─── ADDRESSES ───────────────────────────────────────────────────────────────────
export const getAddresses = async () => {
    const res = await fetch(`${BASE_URL}/addresses`, {
        headers: await authHeaders(),
    });
    return handleResponse(res);
};
export const addAddress = async (addressData) => {
    const res = await fetch(`${BASE_URL}/addresses`, {
        method: 'POST',
        headers: await authHeaders(),
        body: JSON.stringify(addressData),
    });
    return handleResponse(res);
};
export const updateAddress = async (addressId, addressData) => {
    const res = await fetch(`${BASE_URL}/addresses/${addressId}`, {
        method: 'PUT',
        headers: await authHeaders(),
        body: JSON.stringify(addressData),
    });
    return handleResponse(res);
};
export const deleteAddress = async (id) => {
    const token = localStorage.getItem("token"); // or AsyncStorage
    console.log("TOKEN:", token);
    const response = await fetch(`http://localhost:8080/api/addresses/${id}`, {
        method: "DELETE",
        headers: {
            "Authorization": `Bearer ${token}`,   // 🔥 IMPORTANT
            "Content-Type": "application/json"
        }
    });
    if (!response.ok) {
        throw new Error("Delete failed");
    }
    return true;
};
export const setDefaultAddress = async (addressId) => {
    const res = await fetch(`${BASE_URL}/addresses/${addressId}/set-default`, {
        method: 'POST',
        headers: await authHeaders(),
    });
    return handleResponse(res);
};
// ─── PAYMENT ─────────────────────────────────────────────────────────────────────
export const processPayment = async (paymentData) => {
    const res = await fetch(`${BASE_URL}/payment/process`, {
        method: 'POST',
        headers: await authHeaders(),
        body: JSON.stringify(paymentData),
    });
    return handleResponse(res);
};
// ─── INFO ────────────────────────────────────────────────────────────────────────
export const getFaqs = async () => {
    const res = await fetch(`${BASE_URL}/faq`);
    return handleResponse(res);
};
export const getContactInfo = async () => {
    const res = await fetch(`${BASE_URL}/contact`);
    return handleResponse(res);
};
export const getPrivacyPolicy = async () => {
    const res = await fetch(`${BASE_URL}/privacy`);
    return handleResponse(res);
};
export const submitRating = async (rating, review) => {
    const res = await fetch(`${BASE_URL}/rate`, {
        method: 'POST',
        headers: await authHeaders(),
        body: JSON.stringify({ rating, review }),
    });
    return handleResponse(res);
};
</file>

<file path="App.js">
import React from "react";
import { NavigationContainer } from "@react-navigation/native";
import { createNativeStackNavigator } from "@react-navigation/native-stack";
import { useFonts } from "expo-font";
import { Text } from "react-native";

import Onboarding from "./Onboarding";
import Login from "./Login";
import Success from "./Success";
import Signup from "./Signup";
import Otpscreen from "./Otpscreen";
import Successup from "./Successup";
import Home from "./Home";
import CartScreen from "./Cartscreen";
import BiryaniScreen from "./Biryani";
import MealsScreen from "./Meals";
import StartersScreen from "./Starters";
import DessertsScreen from "./Desserts";
import DrinksScreen from "./Drinks";
import TandooriScreen from "./Tandoori";
import WishlistScreen from "./WishlistScreen";
import { CartProvider } from "./CartContext";
import { WishlistProvider } from "./WishlistContext";
import ProfileScreen from "./Profile";
import Transaction from "./Transaction";
import OrderSuccess from "./OrderSuccess";
import EditProfileScreen from "./EditProfileScreen";
import SavedAddressesScreen from "./SavedAddressesScreen";

import EditAddress from "./Editaddress"; 
import OrderHistoryScreen from "./OrderHistoryScreen";
import ContactUsScreen from "./ContactUsScreen";
import HelpFAQScreen from "./HelpFAQScreen";
import RateAppScreen from "./RateAppScreen";
import PrivacyPolicyScreen from "./PrivacyPolicyScreen";


const Stack = createNativeStackNavigator();

export default function App() {
  const [loaded] = useFonts({
    PoppinsRegular: require("./assets/fonts/Poppins-Regular.ttf"),
    PoppinsMedium: require("./assets/fonts/Poppins-Medium.ttf"),
    PoppinsSemiBold: require("./assets/fonts/Poppins-SemiBold.ttf"),
    PoppinsBold: require("./assets/fonts/Poppins-Bold.ttf"),
  });

  if (!loaded) return null;

  Text.defaultProps = Text.defaultProps || {};
  Text.defaultProps.style = {
    fontFamily: "PoppinsRegular",
  };

  return (
    <CartProvider>
      <WishlistProvider>
        <NavigationContainer>
          <Stack.Navigator screenOptions={{ headerShown: false }}>
            <Stack.Screen name="Onboarding" component={Onboarding} />
            <Stack.Screen name="Login" component={Login} />
            <Stack.Screen name="Success" component={Success} />
            <Stack.Screen name="Signup" component={Signup} />
            <Stack.Screen name="Otpscreen" component={Otpscreen} />
            <Stack.Screen name="Successup" component={Successup} />
            <Stack.Screen name="Home" component={Home} />
            <Stack.Screen name="Profile" component={ProfileScreen} />
            <Stack.Screen name="Cart" component={CartScreen} />
            <Stack.Screen name="Biryani" component={BiryaniScreen} />
            <Stack.Screen name="Meals" component={MealsScreen} />
            <Stack.Screen name="Starters" component={StartersScreen} />
            <Stack.Screen name="Tandoori" component={TandooriScreen} />
            <Stack.Screen name="Desserts" component={DessertsScreen} />
            <Stack.Screen name="Drinks" component={DrinksScreen} />
            <Stack.Screen name="Wishlist" component={WishlistScreen} />
            <Stack.Screen name="Transaction" component={Transaction} />
            <Stack.Screen name="OrderSuccess" component={OrderSuccess} />
            <Stack.Screen name="EditProfile" component={EditProfileScreen} />
            <Stack.Screen name="SavedAddresses" component={SavedAddressesScreen} />
            <Stack.Screen name="EditAddress" component={EditAddress} />
            <Stack.Screen name="OrderHistory" component={OrderHistoryScreen} />
            <Stack.Screen name="HelpFAQ" component={HelpFAQScreen} />
            <Stack.Screen name="ContactUs" component={ContactUsScreen} />
            <Stack.Screen name="RateApp" component={RateAppScreen} />
            <Stack.Screen name="PrivacyPolicy" component={PrivacyPolicyScreen} />
          </Stack.Navigator>
        </NavigationContainer>
      </WishlistProvider>
    </CartProvider>
  );
}
</file>

<file path="app.json">
{
  "expo": {
    "name": "annapurnaresturant",
    "slug": "annapurnaresturant",
    "version": "1.0.0",
    "orientation": "portrait",
    "icon": "./assets/icon.png",
    "userInterfaceStyle": "light",
    "splash": {
      "image": "./assets/splash-icon.png",
      "resizeMode": "contain",
      "backgroundColor": "#ffffff"
    },
    "ios": {
      "supportsTablet": true
    },
    "android": {
      "adaptiveIcon": {
        "backgroundColor": "#E6F4FE",
        "foregroundImage": "./assets/android-icon-foreground.png",
        "backgroundImage": "./assets/android-icon-background.png",
        "monochromeImage": "./assets/android-icon-monochrome.png"
      }
    },
    "web": {
      "favicon": "./assets/favicon.png"
    },
    "plugins": [
      "@react-native-community/datetimepicker"
    ]
  }
}
</file>

<file path="Biryani.js">
import React, { useState } from "react";
import {
  View,
  Text,
  StyleSheet,
  TextInput,
  TouchableOpacity,
  FlatList,
  Image,
  Share,
} from "react-native";
import { Ionicons } from "@expo/vector-icons";
import { allFoodItems } from "./Data";
import { useContext } from "react";
import { CartContext } from "./CartContext";
import { WishlistContext } from "./WishlistContext";

export default function BiryaniScreen({ navigation }) {
  const { cart, addToCart, removeFromCart } = useContext(CartContext);
  const { toggleWishlist, isWishlisted } = useContext(WishlistContext);
  const [searchText, setSearchText] = useState("");

  const totalItems = cart.reduce((sum, i) => sum + i.quantity, 0);

  const baseItems = allFoodItems.filter((item) => item.category === "Biryani");
  const filteredItems = baseItems.filter((item) =>
    item.name.toLowerCase().includes(searchText.toLowerCase())
  );

  const shareItem = async (item) => {
    try {
      await Share.share({
        message: `${item.name} - Rs.${item.price}\nTry this delicious food!`,
      });
    } catch (error) {
      console.log(error);
    }
  };

  const renderItem = ({ item }) => {
    const cartItem = cart.find((i) => i.id === item.id);
    const wishlisted = isWishlisted(item.id);

    return (
      <View style={styles.foodCard}>
        {/* LEFT */}
        <View style={{ flex: 1, paddingRight: 10 }}>
          <Text style={styles.foodTitle}>{item.name}</Text>
          <Text style={styles.foodPrice}>Rs.{item.price}</Text>
          <Text style={styles.foodDesc}>{item.description}</Text>

          <View style={styles.iconRow}>
            {/* WISHLIST */}
            <TouchableOpacity style={styles.iconBtn} onPress={() => toggleWishlist(item.id)}>
              <Ionicons
                name={wishlisted ? "heart" : "heart-outline"}
                size={17}
                color="#ff5722"
              />
            </TouchableOpacity>

            {/* SHARE */}
            <TouchableOpacity style={styles.iconBtn} onPress={() => shareItem(item)}>
              <Ionicons name="share-social-outline" size={17} color="#444" />
            </TouchableOpacity>

            {/* ADD / QTY */}
            <View style={styles.actionBox}>
              {!cartItem ? (
                <TouchableOpacity style={styles.addInlineBtn} onPress={() => addToCart(item)}>
                  <Text style={styles.addInlineText}>ADD +</Text>
                </TouchableOpacity>
              ) : (
                <View style={styles.qtyInlineBox}>
                  <TouchableOpacity onPress={() => removeFromCart(item.id)}>
                    <Text style={styles.qtyBtnText}>-</Text>
                  </TouchableOpacity>
                  <Text style={styles.qtyText}>{cartItem.quantity}</Text>
                  <TouchableOpacity onPress={() => addToCart(item)}>
                    <Text style={styles.qtyBtnText}>+</Text>
                  </TouchableOpacity>
                </View>
              )}
            </View>
          </View>
        </View>

        {/* RIGHT */}
        <View style={styles.imageBox}>
          <Image source={item.image} style={styles.foodImage} />
        </View>
      </View>
    );
  };

  return (
    <View style={styles.container}>

      {/* HEADER */}
      <View style={styles.header}>
        <TouchableOpacity style={styles.iconCircle} onPress={() => navigation.goBack()}>
          <Ionicons name="arrow-back" size={22} color="#1a1a2e" />
        </TouchableOpacity>
        <Text style={styles.title}>Biryani Menu</Text>
        <TouchableOpacity style={styles.iconCircle} onPress={() => navigation.navigate("Cart")}>
          <Ionicons name="cart-outline" size={20} color="#1a1a2e" />
          {cart.length > 0 && (
            <View style={styles.cartBadge}>
              <Text style={styles.cartText}>{totalItems}</Text>
            </View>
          )}
        </TouchableOpacity>
      </View>

      {/* SEARCH */}
      <View style={styles.searchContainer}>
        <Ionicons name="search" size={18} color="#aaa" />
        <TextInput
          placeholder="Search biryani..."
          placeholderTextColor="#bbb"
          value={searchText}
          onChangeText={setSearchText}
          style={styles.input}
        />
        {searchText.length > 0 && (
          <TouchableOpacity onPress={() => setSearchText("")}>
            <Ionicons name="close-circle" size={18} color="#bbb" />
          </TouchableOpacity>
        )}
      </View>

      {/* COUNT */}
      <View style={styles.countRow}>
        <Text style={styles.countText}>{filteredItems.length} items</Text>
      </View>

      {/* LIST */}
      <FlatList
        data={filteredItems}
        keyExtractor={(item) => item.id.toString()}
        renderItem={renderItem}
        showsVerticalScrollIndicator={false}
        contentContainerStyle={{ paddingBottom: totalItems > 0 ? 130 : 30, paddingTop: 4 }}
        ListEmptyComponent={
          <View style={styles.emptyBox}>
            <Text style={styles.emptyEmoji}>🍽</Text>
            <Text style={styles.emptyTitle}>Nothing found</Text>
            <Text style={styles.emptySubtitle}>Try a different search</Text>
          </View>
        }
      />

      {/* CART BAR */}
      {totalItems > 0 && (
        <View style={styles.cartBar}>
          <View style={styles.cartBarLeft}>
            <View style={styles.cartCountBadge}>
              <Text style={styles.cartCountText}>{totalItems}</Text>
            </View>
            <Text style={styles.cartBarText}>
              {totalItems} {totalItems === 1 ? "item" : "items"} added
            </Text>
          </View>
          <TouchableOpacity style={styles.checkoutBtn} onPress={() => navigation.navigate("Cart")}>
            <Text style={styles.checkoutText}>Checkout</Text>
            <Ionicons name="arrow-forward" size={14} color="#ff5722" />
          </TouchableOpacity>
        </View>
      )}
    </View>
  );
}

const styles = StyleSheet.create({
  container: { flex: 1, backgroundColor: "#faf9f7" },

  // HEADER
  header: {
    flexDirection: "row",
    justifyContent: "space-between",
    alignItems: "center",
    marginTop: 50,
    paddingHorizontal: 16,
    paddingVertical: 14,
    backgroundColor: "#fff",
    elevation: 3,
    shadowColor: "#000",
    shadowOpacity: 0.07,
    shadowOffset: { width: 0, height: 2 },
    shadowRadius: 6,
  },
  title: {
    fontSize: 17,
    fontFamily: "PoppinsSemiBold",
    color: "#1a1a2e",
    letterSpacing: 0.2,
  },
  iconCircle: {
    padding: 8,
    borderRadius: 22,
    backgroundColor: "#f0ece8",
    shadowColor: "#000",
    shadowOpacity: 0.05,
    shadowOffset: { width: 0, height: 1 },
    shadowRadius: 3,
    elevation: 1,
  },
  cartBadge: {
    position: "absolute",
    top: -4,
    right: -4,
    backgroundColor: "#ff5722",
    borderRadius: 10,
    minWidth: 18,
    height: 18,
    justifyContent: "center",
    alignItems: "center",
    paddingHorizontal: 4,
  },
  cartText: { color: "#fff", fontSize: 9, fontFamily: "PoppinsBold" },

  // SEARCH
  searchContainer: {
    flexDirection: "row",
    alignItems: "center",
    backgroundColor: "#f0ece8",
    marginHorizontal: 16,
    marginTop: 14,
    marginBottom: 4,
    paddingHorizontal: 16,
    paddingVertical: 2,
    borderRadius: 30,
  },
  input: {
    marginLeft: 8,
    flex: 1,
    fontFamily: "PoppinsRegular",
    fontSize: 13.5,
    color: "#1a1a2e",
    paddingVertical: 10,
  },

  // COUNT
  countRow: {
    paddingHorizontal: 20,
    paddingTop: 10,
    paddingBottom: 2,
  },
  countText: {
    fontFamily: "PoppinsRegular",
    fontSize: 12,
    color: "#999",
  },

  // FOOD CARD
  foodCard: {
    flexDirection: "row",
    padding: 14,
    backgroundColor: "#fff",
    marginHorizontal: 14,
    marginVertical: 7,
    borderRadius: 18,
    alignItems: "stretch",
    elevation: 3,
    shadowColor: "#000",
    shadowOpacity: 0.07,
    shadowOffset: { width: 0, height: 3 },
    shadowRadius: 8,
  },
  foodTitle: {
    fontSize: 15,
    fontFamily: "PoppinsSemiBold",
    color: "#1a1a2e",
    letterSpacing: 0.1,
  },
  foodPrice: {
    fontSize: 14.5,
    fontFamily: "PoppinsBold",
    color: "#ff5722",
    marginTop: 3,
  },
  foodDesc: {
    fontSize: 12,
    fontFamily: "PoppinsRegular",
    color: "#888",
    marginTop: 5,
    lineHeight: 17,
    marginRight: 4,
    textAlign: "justify",
    flexShrink: 1,
  },
  iconRow: {
    flexDirection: "row",
    marginTop: 10,
    alignItems: "center",
    flexWrap: "nowrap",
  },
  iconBtn: {
    width: 34,
    height: 34,
    borderRadius: 17,
    borderWidth: 1,
    borderColor: "#ede8e4",
    justifyContent: "center",
    alignItems: "center",
    marginRight: 8,
    backgroundColor: "#faf9f7",
  },
  imageBox: {
    width: 120,
    justifyContent: "center",
    alignItems: "center",
  },
  foodImage: {
    width: 110,
    height: 115,
    borderRadius: 18,
  },
  actionBox: {
    marginLeft: 6,
    width: 90,
    height: 36,
    justifyContent: "center",
    alignItems: "center",
    flexShrink: 1,
  },
  addInlineBtn: {
    width: 90,
    height: 36,
    backgroundColor: "#fff",
    borderWidth: 1.5,
    borderColor: "#ff5722",
    borderRadius: 12,
    justifyContent: "center",
    alignItems: "center",
  },
  addInlineText: {
    color: "#ff5722",
    fontFamily: "PoppinsSemiBold",
    fontSize: 12.5,
    letterSpacing: 0.5,
  },
  qtyInlineBox: {
    width: 90,
    height: 36,
    flexDirection: "row",
    backgroundColor: "#ff5722",
    borderRadius: 12,
    justifyContent: "space-around",
    alignItems: "center",
  },
  qtyBtnText: {
    color: "#fff",
    fontSize: 17,
    fontFamily: "PoppinsMedium",
    marginHorizontal: 6,
  },
  qtyText: {
    color: "#fff",
    fontFamily: "PoppinsBold",
    fontSize: 14,
  },

  // EMPTY STATE
  emptyBox: { alignItems: "center", marginTop: 60 },
  emptyEmoji: { fontSize: 48 },
  emptyTitle: { fontFamily: "PoppinsSemiBold", fontSize: 16, color: "#1a1a2e", marginTop: 12 },
  emptySubtitle: { fontFamily: "PoppinsRegular", fontSize: 13, color: "#999", marginTop: 5 },

  // CART BAR
  cartBar: {
    position: "absolute",
    bottom: 16,
    left: 14,
    right: 14,
    height: 56,
    backgroundColor: "#1a1a2e",
    flexDirection: "row",
    justifyContent: "space-between",
    alignItems: "center",
    paddingHorizontal: 16,
    borderRadius: 32,
    elevation: 10,
    shadowColor: "#000",
    shadowOpacity: 0.22,
    shadowOffset: { width: 0, height: 5 },
    shadowRadius: 10,
  },
  cartBarLeft: { flexDirection: "row", alignItems: "center", gap: 10 },
  cartCountBadge: {
    width: 26,
    height: 26,
    borderRadius: 13,
    backgroundColor: "#ff5722",
    justifyContent: "center",
    alignItems: "center",
  },
  cartCountText: { color: "#fff", fontFamily: "PoppinsBold", fontSize: 11 },
  cartBarText: { color: "rgba(255,255,255,0.85)", fontFamily: "PoppinsMedium", fontSize: 13 },
  checkoutBtn: {
    flexDirection: "row",
    alignItems: "center",
    backgroundColor: "#fff",
    paddingHorizontal: 16,
    paddingVertical: 8,
    borderRadius: 20,
    gap: 5,
  },
  checkoutText: { color: "#ff5722", fontFamily: "PoppinsSemiBold", fontSize: 13 },
});
</file>

<file path="CartContext.js">
import React, { createContext, useState, useEffect } from "react";
import AsyncStorage from "@react-native-async-storage/async-storage";
import * as api from "./api";
export const CartContext = createContext();
export const CartProvider = ({ children }) => {
  const [cart, setCart] = useState([]);
  const [isLoggedIn, setIsLoggedIn] = useState(false);
  // Check if user is logged in on mount
  useEffect(() => {
    AsyncStorage.getItem("token").then((token) => {
      if (token) {
        setIsLoggedIn(true);
        fetchCartFromServer();
      }
    });
  }, []);
  const fetchCartFromServer = async () => {
    try {
      const data = await api.getCart();
      // Convert backend format to local format for UI compatibility
      const localCart = data.items.map((item) => ({
        id: item.menuItemId,
        name: item.name,
        price: item.price,
        category: item.category,
        quantity: item.quantity,
        image: null, // images are local assets, not from server
      }));
      setCart(localCart);
    } catch (e) {
      // If not logged in or error, cart stays local
    }
  };
  const addToCart = async (item) => {
    // Always update local state immediately for smooth UX
    setCart((prev) => {
      const existing = prev.find((i) => i.id === item.id);
      if (existing) {
        return prev.map((i) => i.id === item.id ? { ...i, quantity: i.quantity + 1 } : i);
      } else {
        return [...prev, { ...item, quantity: 1 }];
      }
    });
    // Sync with backend if logged in
    const token = await AsyncStorage.getItem("token");
    if (token) {
      try {
        await api.addToCart(item.id, 1);
      } catch (e) {
        console.log("Cart sync error:", e);
      }
    }
  };
  const removeFromCart = async (id) => {
    setCart((prev) => {
      const existing = prev.find((i) => i.id === id);
      if (!existing) return prev;
      if (existing.quantity === 1) return prev.filter((i) => i.id !== id);
      return prev.map((i) => i.id === id ? { ...i, quantity: i.quantity - 1 } : i);
    });
    // Sync with backend if logged in
    const token = await AsyncStorage.getItem("token");
    if (token) {
      try {
        await api.removeFromCart(id);
      } catch (e) {
        console.log("Cart sync error:", e);
      }
    }
  };
  const clearCart = async () => {
    setCart([]);
    const token = await AsyncStorage.getItem("token");
    if (token) {
      try {
        await api.clearCart();
      } catch (e) { }
    }
  };
  return (
    <CartContext.Provider value={{ cart, addToCart, removeFromCart, clearCart, fetchCartFromServer }}>
      {children}
    </CartContext.Provider>
  );
};
</file>

<file path="Cartscreen.js">
import React, { useContext } from "react";
import {
  View,
  Text,
  StyleSheet,
  TouchableOpacity,
  ScrollView,
  Image,
} from "react-native";
import { Ionicons } from "@expo/vector-icons";
import { CartContext } from "./CartContext";

export default function CartScreen({ navigation }) {
  const { cart, addToCart, removeFromCart } = useContext(CartContext);

  const subtotal = cart.reduce((sum, item) => sum + item.price * item.quantity, 0);
  const tax = subtotal * 0.1;
  const total = subtotal + tax;

  return (
    <View style={styles.container}>

      {/* HEADER */}
      <View style={styles.header}>
        <TouchableOpacity onPress={() => navigation.goBack()} style={styles.iconCircle}>
          <Ionicons name="arrow-back" size={20} color="#1a1a2e" />
        </TouchableOpacity>
        <Text style={styles.title}>Your Order</Text>
        <View style={{ width: 38 }} />
      </View>

      <ScrollView showsVerticalScrollIndicator={false}>

        {/* EMPTY STATE */}
        {cart.length === 0 ? (
          <View style={styles.emptyContainer}>
            <View style={styles.emptyIconCircle}>
              <Ionicons name="cart-outline" size={40} color="#ff5722" />
            </View>
            <Text style={styles.emptyText}>Your cart is empty</Text>
            <Text style={styles.emptySubText}>Add items from the menu to get started</Text>
          </View>
        ) : (
          <>
            {/* ITEMS */}
            {cart.map((item) => (
              <View key={item.id} style={styles.itemCard}>
                <Image source={item.image} style={styles.itemImage} />

                <View style={{ flex: 1 }}>
                  <Text style={styles.itemName}>{item.name}</Text>
                  <Text style={styles.itemPrice}>₹{item.price}</Text>
                </View>

                <View style={{ alignItems: "flex-end", gap: 8 }}>
                  <View style={styles.qtyBox}>
                    <TouchableOpacity onPress={() => removeFromCart(item.id)}>
                      <Text style={styles.qtyBtn}>−</Text>
                    </TouchableOpacity>
                    <Text style={styles.qtyText}>{item.quantity}</Text>
                    <TouchableOpacity onPress={() => addToCart(item)}>
                      <Text style={styles.qtyBtn}>+</Text>
                    </TouchableOpacity>
                  </View>
                  <Text style={styles.itemTotal}>₹{item.price * item.quantity}</Text>
                </View>
              </View>
            ))}

            {/* SUMMARY */}
            <View style={styles.summaryBox}>
              <Text style={styles.summaryTitle}>Order Summary</Text>

              <View style={styles.row}>
                <Text style={styles.rowLabel}>Subtotal</Text>
                <Text style={styles.rowValue}>₹{subtotal}</Text>
              </View>
              <View style={styles.row}>
                <Text style={styles.rowLabel}>Tax (10%)</Text>
                <Text style={styles.rowValue}>₹{tax.toFixed(0)}</Text>
              </View>

              <View style={styles.divider} />

              <View style={styles.row}>
                <Text style={styles.totalLabel}>Total</Text>
                <Text style={styles.totalValue}>₹{total.toFixed(0)}</Text>
              </View>
            </View>
          </>
        )}

        <View style={{ height: 110 }} />
      </ScrollView>

      {/* FOOTER BUTTON */}
      {cart.length > 0 && (
        <View style={styles.footer}>
          <TouchableOpacity style={styles.button}onPress={() => navigation.navigate("Transaction", { total: total.toFixed(0) })}>
            <Text style={styles.buttonText}>Process Transaction</Text>
            <Ionicons name="arrow-forward" size={16} color="#fff" style={{ marginLeft: 8 }} />
          </TouchableOpacity>
        </View>
      )}
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#faf9f7",
    paddingHorizontal: 16,
  },

  // HEADER
  header: {
    flexDirection: "row",
    justifyContent: "space-between",
    alignItems: "center",
    marginTop: 54,
    marginBottom: 14,
  },
  iconCircle: {
    padding: 8,
    borderRadius: 22,
    backgroundColor: "#f0ece8",
    elevation: 2,
  },
  title: {
    fontSize: 17,
    fontFamily: "PoppinsSemiBold",
    color: "#1a1a2e",
  },

  // EMPTY
  emptyContainer: {
    alignItems: "center",
    marginTop: 100,
  },
  emptyIconCircle: {
    width: 90,
    height: 90,
    borderRadius: 45,
    backgroundColor: "#fff0eb",
    justifyContent: "center",
    alignItems: "center",
    marginBottom: 18,
  },
  emptyText: {
    fontSize: 17,
    fontFamily: "PoppinsSemiBold",
    color: "#1a1a2e",
    marginTop: 4,
  },
  emptySubText: {
    fontSize: 13,
    fontFamily: "PoppinsRegular",
    color: "#999",
    marginTop: 6,
  },

  // ITEM CARD
  itemCard: {
    flexDirection: "row",
    alignItems: "center",
    backgroundColor: "#fff",
    padding: 12,
    borderRadius: 18,
    marginBottom: 12,
    elevation: 3,
  },
  itemImage: {
    width: 64,
    height: 64,
    borderRadius: 14,
    marginRight: 12,
  },
  itemName: {
    fontFamily: "PoppinsSemiBold",
    fontSize: 14,
    color: "#1a1a2e",
  },
  itemPrice: {
    fontFamily: "PoppinsRegular",
    color: "#999",
    fontSize: 13,
    marginTop: 3,
  },
  qtyBox: {
    flexDirection: "row",
    alignItems: "center",
    backgroundColor: "#ff5722",
    borderRadius: 10,
    paddingHorizontal: 8,
    paddingVertical: 5,
  },
  qtyBtn: {
    fontSize: 16,
    fontFamily: "PoppinsMedium",
    color: "#fff",
    paddingHorizontal: 4,
  },
  qtyText: {
    marginHorizontal: 8,
    fontFamily: "PoppinsBold",
    color: "#fff",
    fontSize: 14,
  },
  itemTotal: {
    fontFamily: "PoppinsBold",
    color: "#ff5722",
    fontSize: 14,
  },

  // SUMMARY
  summaryBox: {
    backgroundColor: "#fff",
    padding: 18,
    borderRadius: 18,
    marginTop: 4,
    elevation: 3,
  },
  summaryTitle: {
    fontFamily: "PoppinsSemiBold",
    fontSize: 15,
    color: "#1a1a2e",
    marginBottom: 12,
  },
  row: {
    flexDirection: "row",
    justifyContent: "space-between",
    marginBottom: 8,
  },
  rowLabel: {
    fontFamily: "PoppinsRegular",
    fontSize: 13.5,
    color: "#888",
  },
  rowValue: {
    fontFamily: "PoppinsMedium",
    fontSize: 13.5,
    color: "#1a1a2e",
  },
  divider: {
    height: 1,
    backgroundColor: "#f0ece8",
    marginVertical: 10,
  },
  totalLabel: {
    fontFamily: "PoppinsSemiBold",
    fontSize: 15,
    color: "#1a1a2e",
  },
  totalValue: {
    fontFamily: "PoppinsBold",
    fontSize: 15,
    color: "#ff5722",
  },

  // FOOTER
  footer: {
    position: "absolute",
    bottom: 20,
    left: 16,
    right: 16,
  },
  button: {
    backgroundColor: "#ff5722",
    paddingVertical: 16,
    borderRadius: 18,
    alignItems: "center",
    flexDirection: "row",
    justifyContent: "center",
    elevation: 4,
  },
  buttonText: {
    color: "#fff",
    fontFamily: "PoppinsSemiBold",
    fontSize: 15,
    letterSpacing: 0.4,
  },
});
</file>

<file path="ContactUsScreen.js">
import React from "react";
import { View, Text, StyleSheet, ScrollView, TouchableOpacity } from "react-native";
import { Ionicons } from "@expo/vector-icons";

export default function ContactUsScreen({ navigation }) {
  const contacts = [
    {
      icon: "call-outline",
      label: "Phone",
      value: "+91 98765 43210",
    },
    {
      icon: "mail-outline",
      label: "Email",
      value: "support@annapurna.com",
    },
    {
      icon: "location-outline",
      label: "Location",
      value: "Hyderabad, India",
    },
  ];

  return (
    <View style={styles.container}>
      <ScrollView showsVerticalScrollIndicator={false}>
        
        {/* HEADER */}
        <View style={styles.header}>
          <TouchableOpacity
            style={styles.backBtn}
            onPress={() => navigation.goBack()}
          >
            <Ionicons name="arrow-back" size={20} color="#1a1a2e" />
          </TouchableOpacity>
          <Text style={styles.headerTitle}>Contact Us</Text>
          <View style={{ width: 36 }} />
        </View>

        {/* CONTACT CARD */}
        <View style={styles.section}>
          <View style={styles.sectionCard}>
            {contacts.map((item, index) => (
              <View
                key={index}
                style={[
                  styles.row,
                  index < contacts.length - 1 && styles.borderBottom,
                ]}
              >
                <View style={styles.left}>
                  <View style={styles.iconBox}>
                    <Ionicons name={item.icon} size={18} color="#ff5722" />
                  </View>

                  <View>
                    <Text style={styles.label}>{item.label}</Text>
                    <Text style={styles.value}>{item.value}</Text>
                  </View>
                </View>
              </View>
            ))}
          </View>
        </View>

        {/* SUPPORT NOTE */}
        <View style={styles.infoCard}>
          <Ionicons name="time-outline" size={18} color="#ff5722" />
          <Text style={styles.infoText}>
            Our support team is available from 9 AM to 9 PM.
          </Text>
        </View>
      </ScrollView>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#faf9f7",
  },

  /* HEADER */
  header: {
    flexDirection: "row",
    alignItems: "center",
    justifyContent: "space-between",
    marginTop: 54,
    paddingHorizontal: 18,
    marginBottom: 20,
  },

  backBtn: {
    width: 36,
    height: 36,
    borderRadius: 18,
    backgroundColor: "#f0ece8",
    justifyContent: "center",
    alignItems: "center",
  },

  headerTitle: {
    fontSize: 22,
    fontFamily: "PoppinsSemiBold",
    color: "#1a1a2e",
  },

  /* SECTION */
  section: {
    marginHorizontal: 16,
    marginBottom: 18,
  },

  sectionCard: {
    backgroundColor: "#fff",
    borderRadius: 18,
    elevation: 3,
    overflow: "hidden",
  },

  row: {
    paddingVertical: 14,
    paddingHorizontal: 16,
  },

  borderBottom: {
    borderBottomWidth: 1,
    borderBottomColor: "#f0ece8",
  },

  left: {
    flexDirection: "row",
    alignItems: "center",
    gap: 14,
  },

  iconBox: {
    width: 36,
    height: 36,
    borderRadius: 10,
    backgroundColor: "#fff0eb",
    justifyContent: "center",
    alignItems: "center",
  },

  label: {
    fontFamily: "PoppinsMedium",
    fontSize: 13,
    color: "#888",
  },

  value: {
    fontFamily: "PoppinsSemiBold",
    fontSize: 14,
    color: "#1a1a2e",
  },

  /* INFO CARD */
  infoCard: {
    flexDirection: "row",
    alignItems: "center",
    backgroundColor: "#fff0eb",
    marginHorizontal: 16,
    padding: 14,
    borderRadius: 16,
    gap: 10,
  },

  infoText: {
    fontFamily: "PoppinsMedium",
    fontSize: 13,
    color: "#ff5722",
    flex: 1,
  },
});
</file>

<file path="Data.js">
export const allFoodItems = [

  // 🍛 BIRYANI
  { id: 1, name: "Chicken Dum Biryani", price: 150, category: "Biryani", image: require("./assets/biryani/chickenbiryani.png"), description: "Aromatic basmati rice layered with juicy chicken. Slow-cooked in dum style with rich spices."},
  { id: 2, name: "Mutton Biryani", price: 200, category: "Biryani", image: require("./assets/biryani/muttonbiryani.png"), description: "Tender mutton cooked with flavorful long-grain rice. Packed with traditional spices and deep aroma."},
  { id: 3, name: "Egg Biryani", price: 120, category: "Biryani", image: require("./assets/biryani/eggbiryani.png"), description: "Boiled eggs blended with spicy masala rice. Simple, tasty and perfectly satisfying meal."},
  { id: 4, name: "Veg Biryani", price: 110, category: "Biryani", image: require("./assets/biryani/vegbiryani.png"), description: "Fresh vegetables cooked with fragrant basmati rice. Light, healthy and full of flavor."},
  { id: 5, name: "Paneer Biryani", price: 140, category: "Biryani", image: require("./assets/biryani/pannerbiryani.png"), description: "Soft paneer cubes mixed with rich masala rice. Creamy texture with delicious spices."},
  { id: 6, name: "Chilli Chicken Biryani", price: 170, category: "Biryani", image: require("./assets/biryani/chillichickenbiryani.png"), description: "Spicy chilli chicken tossed with flavorful rice. A bold fusion of Indo-Chinese taste."},
  { id: 7, name: "Kaju Biryani", price: 150, category: "Biryani", image: require("./assets/biryani/kajubiryani.png"), description: "Crunchy cashews blended with aromatic rice. Rich, nutty flavor with mild spices."},
  { id: 8, name: "Ulavacharu Biryani", price: 180, category: "Biryani", image: require("./assets/biryani/ulavacharubiryani.png"), description: "Traditional ulavacharu gravy mixed with rice. Authentic Andhra taste with tangy richness."},
  { id: 9, name: "Fish Biryani", price: 190, category: "Biryani", image: require("./assets/biryani/fishbiryani.png"), description: "Fresh fish cooked with spiced basmati rice. Light, flavorful and coastal-style delight."},
  { id: 10, name: "Prawn Biryani", price: 220, category: "Biryani", image: require("./assets/biryani/prawnsbiryani.png"), description: "Juicy prawns cooked in rich masala rice. Seafood lovers' favorite with bold flavor."},

  // 🍱 MEALS
  { id: 11, name: "Veg Meals", price: 110, category: "Meals", image: require("./assets/meals/vegmeals.png"), description: "Complete vegetarian meal with rice, dal, curries, roti, and pickle. Healthy and balanced." },
  { id: 12, name: "Special Meals", price: 140, category: "Meals", image: require("./assets/meals/specialmeal.png"), description: "Deluxe combo with extra curries, sweets, and special dishes. Rich and filling experience." },
  { id: 13, name: "Non Veg Meals", price: 180, category: "Meals", image: require("./assets/meals/nonvegmeal.png"), description: "Rice served with chicken or mutton curry, dal, and sides. A complete feast." },
  { id: 14, name: "Mini Meals", price: 90, category: "Meals", image: require("./assets/meals/minimeal.png"), description: "Light meal with rice, one curry, dal, and pickle. Perfect for quick hunger." },
  { id: 15, name: "Paneer Meals", price: 130, category: "Meals", image: require("./assets/meals/paneermeal.png"), description: "Paneer curry served with rice, roti, and sides. Creamy and delicious." },
  { id: 16, name: "Chicken Meals", price: 170, category: "Meals", image: require("./assets/meals/chickenmeal.png"), description: "Chicken curry paired with rice, dal, and sides. Protein-rich and tasty." },
  { id: 17, name: "Fish Meals", price: 190, category: "Meals", image: require("./assets/meals/fishmeal.png"), description: "Fish curry served with rice and traditional sides. Coastal flavor delight." },
  { id: 18, name: "Mutton Meals", price: 210, category: "Meals", image: require("./assets/meals/muttonmeal.png"), description: "Tender mutton curry with rice and sides. Rich and satisfying meal." },

  // 🍗 STARTERS
  { id: 19, name: "Gobi Manchurian", price: 140, category: "Starters", image: require("./assets/starters/gobimanchurian.png"), description: "Crispy cauliflower tossed in spicy Manchurian sauce. Perfect Indo-Chinese starter." },
  { id: 20, name: "Paneer Chilli", price: 160, category: "Starters", image: require("./assets/starters/paneerchilli.png"), description: "Fried paneer cubes cooked with capsicum and spicy sauces. Soft and flavorful." },
  { id: 21, name: "Veg Manchurian", price: 140, category: "Starters", image: require("./assets/starters/vegmanchurian.png"), description: "Vegetable balls tossed in tangy Manchurian gravy. A classic veg starter." },
  { id: 22, name: "Chicken Chilli", price: 150, category: "Starters", image: require("./assets/starters/chickenchilli.png"), description: "Juicy chicken pieces stir-fried with spices and sauces. Spicy and delicious." },
  { id: 23, name: "Chicken 65", price: 150, category: "Starters", image: require("./assets/starters/chicken65.png"), description: "Deep-fried spicy chicken with curry leaves and masala. South Indian favorite." },
  { id: 24, name: "Chicken Manchurian", price: 150, category: "Starters", image: require("./assets/starters/chickenmanchurian.png"), description: "Chicken balls tossed in flavorful Manchurian sauce. Rich and tangy taste." },
  { id: 25, name: "Chicken Garlic Bread", price: 150, category: "Starters", image: require("./assets/starters/chickengarlicbread.png"), description: "Crispy bread topped with garlic butter and chicken. Crunchy and savory." },
  { id: 26, name: "Chicken Lollipop", price: 160, category: "Starters", image: require("./assets/starters/chickenlollipop.png"), description: "Fried chicken wings shaped like lollipops. Crispy outside, juicy inside." },
  { id: 27, name: "Mutton Chilli", price: 180, category: "Starters", image: require("./assets/starters/muttonchilli.png"), description: "Spicy mutton pieces cooked with sauces and peppers. Bold and rich flavor." },

  // 🔥 TANDOORI
  { id: 28, name: "Tandoori Chicken (Half)", price: 190, category: "Tandoori", image: require("./assets/tandoori/tandoorihalf.png"), description: "Half chicken marinated in spices and grilled in tandoor. Smoky and juicy." },
  { id: 29, name: "Tandoori Chicken (Full)", price: 320, category: "Tandoori", image: require("./assets/tandoori/tandoorifull.png"), description: "Full chicken roasted in traditional tandoor. Rich smoky flavor." },
  { id: 30, name: "Chicken Tikka", price: 200, category: "Tandoori", image: require("./assets/tandoori/chickentikka.png"), description: "Boneless chicken marinated and grilled. Soft, smoky and flavorful." },
  { id: 31, name: "Kalmi Kebab", price: 200, category: "Tandoori", image: require("./assets/tandoori/kalmikebab.png"), description: "Juicy chicken leg pieces marinated and grilled. Rich and tender taste." },
  { id: 32, name: "Tangdi Kebab", price: 210, category: "Tandoori", image: require("./assets/tandoori/tangadikebab.png"), description: "Spiced chicken drumsticks grilled to perfection. Smoky and juicy." },
  { id: 33, name: "Chicken Seekh Kebab", price: 190, category: "Tandoori", image: require("./assets/tandoori/chickenseekhkebab.png"), description: "Minced chicken skewers grilled in tandoor. Soft and flavorful." },
  { id: 34, name: "Paneer Tikka", price: 180, category: "Tandoori", image: require("./assets/tandoori/paneertikka.png"), description: "Paneer cubes grilled with spices and veggies. Smoky vegetarian delight." },
  { id: 35, name: "Mushroom Tikka", price: 170, category: "Tandoori", image: require("./assets/tandoori/mushroomtikka.png"), description: "Juicy mushrooms marinated and grilled. Light and flavorful." },

  // 🍰 DESSERTS
  { id: 36, name: "Gulab Jamun", price: 60, category: "Desserts", image: require("./assets/desserts/gulabjamun.png"), description: "Soft fried dumplings soaked in sugar syrup. Sweet and classic dessert." },
  { id: 37, name: "Rasmalai", price: 80, category: "Desserts", image: require("./assets/desserts/rasmalai.png"), description: "Soft paneer discs soaked in creamy milk. Rich and delicious sweet." },
  { id: 38, name: "Ice Cream", price: 50, category: "Desserts", image: require("./assets/desserts/icecream.png"), description: "Chilled creamy ice cream. Perfect refreshing dessert." },
  { id: 39, name: "Chocolate Cake", price: 120, category: "Desserts", image: require("./assets/desserts/chocolatecake.png"), description: "Moist chocolate cake with rich flavor. Perfect sweet treat." },
  { id: 40, name: "Brownie", price: 100, category: "Desserts", image: require("./assets/desserts/brownie.png"), description: "Soft chocolate brownie with fudgy texture. Rich and indulgent." },
  { id: 41, name: "Cupcake", price: 90, category: "Desserts", image: require("./assets/desserts/cupcake.png"), description: "Mini cake topped with cream frosting. Sweet and delightful." },

  // // 🥤 DRINKS
  { id: 42, name: "Coke", price: 20, category: "Drinks", image: require("./assets/drinks/coke.png"), description: "Chilled carbonated soft drink. Refreshing and fizzy." },
  { id: 43, name: "Pepsi", price: 40, category: "Drinks", image: require("./assets/drinks/pepsi.png"), description: "Classic cola drink with refreshing taste." },
  { id: 44, name: "Sprite", price: 20, category: "Drinks", image: require("./assets/drinks/sprite.png"), description: "Lemon-lime flavored soft drink. Crisp and refreshing." },
  { id: 45, name: "Thumbs Up", price: 20, category: "Drinks", image: require("./assets/drinks/thumsup.png"), description: "Strong cola with bold taste. Popular Indian favorite." },
  { id: 46, name: "Lassi", price: 30, category: "Drinks", image: require("./assets/drinks/lassi.png"), description: "Traditional yogurt-based drink. Cool and refreshing." },
  { id: 47, name: "Buttermilk", price: 30, category: "Drinks", image: require("./assets/drinks/buttermilk.png"), description: "Light spiced buttermilk. Perfect for digestion." },
];
</file>

<file path="Desserts.js">
import React, { useState } from "react";
import {
  View,
  Text,
  StyleSheet,
  TextInput,
  TouchableOpacity,
  FlatList,
  Image,
  Share,
} from "react-native";
import { Ionicons } from "@expo/vector-icons";
import { allFoodItems } from "./Data";
import { useContext } from "react";
import { CartContext } from "./CartContext";
import { WishlistContext } from "./WishlistContext";

export default function DessertsScreen({ navigation }) {
  const { cart, addToCart, removeFromCart } = useContext(CartContext);
  const { toggleWishlist, isWishlisted } = useContext(WishlistContext);
  const [searchText, setSearchText] = useState("");

  const totalItems = cart.reduce((sum, i) => sum + i.quantity, 0);

  const baseItems = allFoodItems.filter((item) => item.category === "Desserts");
  const filteredItems = baseItems.filter((item) =>
    item.name.toLowerCase().includes(searchText.toLowerCase())
  );

  const shareItem = async (item) => {
    try {
      await Share.share({
        message: `${item.name} - Rs.${item.price}\nTry this delicious food!`,
      });
    } catch (error) {
      console.log(error);
    }
  };

  const renderItem = ({ item }) => {
    const cartItem = cart.find((i) => i.id === item.id);
    const wishlisted = isWishlisted(item.id);

    return (
      <View style={styles.foodCard}>
        {/* LEFT */}
        <View style={{ flex: 1, paddingRight: 10 }}>
          <Text style={styles.foodTitle}>{item.name}</Text>
          <Text style={styles.foodPrice}>Rs.{item.price}</Text>
          <Text style={styles.foodDesc}>{item.description}</Text>

          <View style={styles.iconRow}>
            {/* WISHLIST */}
            <TouchableOpacity style={styles.iconBtn} onPress={() => toggleWishlist(item)}>
              <Ionicons
                name={wishlisted ? "heart" : "heart-outline"}
                size={17}
                color="#ff5722"
              />
            </TouchableOpacity>

            {/* SHARE */}
            <TouchableOpacity style={styles.iconBtn} onPress={() => shareItem(item)}>
              <Ionicons name="share-social-outline" size={17} color="#444" />
            </TouchableOpacity>

            {/* ADD / QTY */}
            <View style={styles.actionBox}>
              {!cartItem ? (
                <TouchableOpacity style={styles.addInlineBtn} onPress={() => addToCart(item)}>
                  <Text style={styles.addInlineText}>ADD +</Text>
                </TouchableOpacity>
              ) : (
                <View style={styles.qtyInlineBox}>
                  <TouchableOpacity onPress={() => removeFromCart(item.id)}>
                    <Text style={styles.qtyBtnText}>-</Text>
                  </TouchableOpacity>
                  <Text style={styles.qtyText}>{cartItem.quantity}</Text>
                  <TouchableOpacity onPress={() => addToCart(item)}>
                    <Text style={styles.qtyBtnText}>+</Text>
                  </TouchableOpacity>
                </View>
              )}
            </View>
          </View>
        </View>

        {/* RIGHT */}
        <View style={styles.imageBox}>
          <Image source={item.image} style={styles.foodImage} />
        </View>
      </View>
    );
  };

  return (
    <View style={styles.container}>

      {/* HEADER */}
      <View style={styles.header}>
        <TouchableOpacity style={styles.iconCircle} onPress={() => navigation.goBack()}>
          <Ionicons name="arrow-back" size={22} color="#1a1a2e" />
        </TouchableOpacity>
        <Text style={styles.title}>Desserts Menu</Text>
        <TouchableOpacity style={styles.iconCircle} onPress={() => navigation.navigate("Cart")}>
          <Ionicons name="cart-outline" size={20} color="#1a1a2e" />
          {cart.length > 0 && (
            <View style={styles.cartBadge}>
              <Text style={styles.cartText}>{totalItems}</Text>
            </View>
          )}
        </TouchableOpacity>
      </View>

      {/* SEARCH */}
      <View style={styles.searchContainer}>
        <Ionicons name="search" size={18} color="#aaa" />
        <TextInput
          placeholder="Search desserts..."
          placeholderTextColor="#bbb"
          value={searchText}
          onChangeText={setSearchText}
          style={styles.input}
        />
        {searchText.length > 0 && (
          <TouchableOpacity onPress={() => setSearchText("")}>
            <Ionicons name="close-circle" size={18} color="#bbb" />
          </TouchableOpacity>
        )}
      </View>

      {/* COUNT */}
      <View style={styles.countRow}>
        <Text style={styles.countText}>{filteredItems.length} items</Text>
      </View>

      {/* LIST */}
      <FlatList
        data={filteredItems}
        keyExtractor={(item) => item.id.toString()}
        renderItem={renderItem}
        showsVerticalScrollIndicator={false}
        contentContainerStyle={{ paddingBottom: totalItems > 0 ? 130 : 30, paddingTop: 4 }}
        ListEmptyComponent={
          <View style={styles.emptyBox}>
            <Text style={styles.emptyEmoji}>🍽</Text>
            <Text style={styles.emptyTitle}>Nothing found</Text>
            <Text style={styles.emptySubtitle}>Try a different search</Text>
          </View>
        }
      />

      {/* CART BAR */}
      {totalItems > 0 && (
        <View style={styles.cartBar}>
          <View style={styles.cartBarLeft}>
            <View style={styles.cartCountBadge}>
              <Text style={styles.cartCountText}>{totalItems}</Text>
            </View>
            <Text style={styles.cartBarText}>
              {totalItems} {totalItems === 1 ? "item" : "items"} added
            </Text>
          </View>
          <TouchableOpacity style={styles.checkoutBtn} onPress={() => navigation.navigate("Cart")}>
            <Text style={styles.checkoutText}>Checkout</Text>
            <Ionicons name="arrow-forward" size={14} color="#ff5722" />
          </TouchableOpacity>
        </View>
      )}
    </View>
  );
}

const styles = StyleSheet.create({
  container: { flex: 1, backgroundColor: "#faf9f7" },

  // HEADER
  header: {
    flexDirection: "row",
    justifyContent: "space-between",
    alignItems: "center",
    marginTop: 50,
    paddingHorizontal: 16,
    paddingVertical: 14,
    backgroundColor: "#fff",
    elevation: 3,
    shadowColor: "#000",
    shadowOpacity: 0.07,
    shadowOffset: { width: 0, height: 2 },
    shadowRadius: 6,
  },
  title: {
    fontSize: 17,
    fontFamily: "PoppinsSemiBold",
    color: "#1a1a2e",
    letterSpacing: 0.2,
  },
  iconCircle: {
    padding: 8,
    borderRadius: 22,
    backgroundColor: "#f0ece8",
    shadowColor: "#000",
    shadowOpacity: 0.05,
    shadowOffset: { width: 0, height: 1 },
    shadowRadius: 3,
    elevation: 1,
  },
  cartBadge: {
    position: "absolute",
    top: -4,
    right: -4,
    backgroundColor: "#ff5722",
    borderRadius: 10,
    minWidth: 18,
    height: 18,
    justifyContent: "center",
    alignItems: "center",
    paddingHorizontal: 4,
  },
  cartText: { color: "#fff", fontSize: 9, fontFamily: "PoppinsBold" },

  // SEARCH
  searchContainer: {
    flexDirection: "row",
    alignItems: "center",
    backgroundColor: "#f0ece8",
    marginHorizontal: 16,
    marginTop: 14,
    marginBottom: 4,
    paddingHorizontal: 16,
    paddingVertical: 2,
    borderRadius: 30,
  },
  input: {
    marginLeft: 8,
    flex: 1,
    fontFamily: "PoppinsRegular",
    fontSize: 13.5,
    color: "#1a1a2e",
    paddingVertical: 10,
  },

  // COUNT
  countRow: {
    paddingHorizontal: 20,
    paddingTop: 10,
    paddingBottom: 2,
  },
  countText: {
    fontFamily: "PoppinsRegular",
    fontSize: 12,
    color: "#999",
  },

  // FOOD CARD
  foodCard: {
    flexDirection: "row",
    padding: 14,
    backgroundColor: "#fff",
    marginHorizontal: 14,
    marginVertical: 7,
    borderRadius: 18,
    alignItems: "stretch",
    elevation: 3,
    shadowColor: "#000",
    shadowOpacity: 0.07,
    shadowOffset: { width: 0, height: 3 },
    shadowRadius: 8,
  },
  foodTitle: {
    fontSize: 15,
    fontFamily: "PoppinsSemiBold",
    color: "#1a1a2e",
    letterSpacing: 0.1,
  },
  foodPrice: {
    fontSize: 14.5,
    fontFamily: "PoppinsBold",
    color: "#ff5722",
    marginTop: 3,
  },
  foodDesc: {
    fontSize: 12,
    fontFamily: "PoppinsRegular",
    color: "#888",
    marginTop: 5,
    lineHeight: 17,
    marginRight: 4,
    textAlign: "justify",
    flexShrink: 1,
  },
  iconRow: {
    flexDirection: "row",
    marginTop: 10,
    alignItems: "center",
    flexWrap: "nowrap",
  },
  iconBtn: {
    width: 34,
    height: 34,
    borderRadius: 17,
    borderWidth: 1,
    borderColor: "#ede8e4",
    justifyContent: "center",
    alignItems: "center",
    marginRight: 8,
    backgroundColor: "#faf9f7",
  },
  imageBox: {
    width: 120,
    justifyContent: "center",
    alignItems: "center",
  },
  foodImage: {
    width: 110,
    height: 115,
    borderRadius: 18,
  },
  actionBox: {
    marginLeft: 6,
    width: 90,
    height: 36,
    justifyContent: "center",
    alignItems: "center",
    flexShrink: 1,
  },
  addInlineBtn: {
    width: 90,
    height: 36,
    backgroundColor: "#fff",
    borderWidth: 1.5,
    borderColor: "#ff5722",
    borderRadius: 12,
    justifyContent: "center",
    alignItems: "center",
  },
  addInlineText: {
    color: "#ff5722",
    fontFamily: "PoppinsSemiBold",
    fontSize: 12.5,
    letterSpacing: 0.5,
  },
  qtyInlineBox: {
    width: 90,
    height: 36,
    flexDirection: "row",
    backgroundColor: "#ff5722",
    borderRadius: 12,
    justifyContent: "space-around",
    alignItems: "center",
  },
  qtyBtnText: {
    color: "#fff",
    fontSize: 17,
    fontFamily: "PoppinsMedium",
    marginHorizontal: 6,
  },
  qtyText: {
    color: "#fff",
    fontFamily: "PoppinsBold",
    fontSize: 14,
  },

  // EMPTY STATE
  emptyBox: { alignItems: "center", marginTop: 60 },
  emptyEmoji: { fontSize: 48 },
  emptyTitle: { fontFamily: "PoppinsSemiBold", fontSize: 16, color: "#1a1a2e", marginTop: 12 },
  emptySubtitle: { fontFamily: "PoppinsRegular", fontSize: 13, color: "#999", marginTop: 5 },

  // CART BAR
  cartBar: {
    position: "absolute",
    bottom: 16,
    left: 14,
    right: 14,
    height: 56,
    backgroundColor: "#1a1a2e",
    flexDirection: "row",
    justifyContent: "space-between",
    alignItems: "center",
    paddingHorizontal: 16,
    borderRadius: 32,
    elevation: 10,
    shadowColor: "#000",
    shadowOpacity: 0.22,
    shadowOffset: { width: 0, height: 5 },
    shadowRadius: 10,
  },
  cartBarLeft: { flexDirection: "row", alignItems: "center", gap: 10 },
  cartCountBadge: {
    width: 26,
    height: 26,
    borderRadius: 13,
    backgroundColor: "#ff5722",
    justifyContent: "center",
    alignItems: "center",
  },
  cartCountText: { color: "#fff", fontFamily: "PoppinsBold", fontSize: 11 },
  cartBarText: { color: "rgba(255,255,255,0.85)", fontFamily: "PoppinsMedium", fontSize: 13 },
  checkoutBtn: {
    flexDirection: "row",
    alignItems: "center",
    backgroundColor: "#fff",
    paddingHorizontal: 16,
    paddingVertical: 8,
    borderRadius: 20,
    gap: 5,
  },
  checkoutText: { color: "#ff5722", fontFamily: "PoppinsSemiBold", fontSize: 13 },
});
</file>

<file path="Drinks.js">
import React, { useState } from "react";
import {
  View,
  Text,
  StyleSheet,
  TextInput,
  TouchableOpacity,
  FlatList,
  Image,
  Share,
} from "react-native";
import { Ionicons } from "@expo/vector-icons";
import { allFoodItems } from "./Data";
import { useContext } from "react";
import { CartContext } from "./CartContext";
import { WishlistContext } from "./WishlistContext";

export default function DrinksScreen({ navigation }) {
  const { cart, addToCart, removeFromCart } = useContext(CartContext);
  const { toggleWishlist, isWishlisted } = useContext(WishlistContext);
  const [searchText, setSearchText] = useState("");

  const totalItems = cart.reduce((sum, i) => sum + i.quantity, 0);

  const baseItems = allFoodItems.filter((item) => item.category === "Drinks");
  const filteredItems = baseItems.filter((item) =>
    item.name.toLowerCase().includes(searchText.toLowerCase())
  );

  const shareItem = async (item) => {
    try {
      await Share.share({
        message: `${item.name} - Rs.${item.price}\nTry this delicious food!`,
      });
    } catch (error) {
      console.log(error);
    }
  };

  const renderItem = ({ item }) => {
    const cartItem = cart.find((i) => i.id === item.id);
    const wishlisted = isWishlisted(item.id);

    return (
      <View style={styles.foodCard}>
        {/* LEFT */}
        <View style={{ flex: 1, paddingRight: 10 }}>
          <Text style={styles.foodTitle}>{item.name}</Text>
          <Text style={styles.foodPrice}>Rs.{item.price}</Text>
          <Text style={styles.foodDesc}>{item.description}</Text>

          <View style={styles.iconRow}>
            {/* WISHLIST */}
            <TouchableOpacity style={styles.iconBtn} onPress={() => toggleWishlist(item)}>
              <Ionicons
                name={wishlisted ? "heart" : "heart-outline"}
                size={17}
                color="#ff5722"
              />
            </TouchableOpacity>

            {/* SHARE */}
            <TouchableOpacity style={styles.iconBtn} onPress={() => shareItem(item)}>
              <Ionicons name="share-social-outline" size={17} color="#444" />
            </TouchableOpacity>

            {/* ADD / QTY */}
            <View style={styles.actionBox}>
              {!cartItem ? (
                <TouchableOpacity style={styles.addInlineBtn} onPress={() => addToCart(item)}>
                  <Text style={styles.addInlineText}>ADD +</Text>
                </TouchableOpacity>
              ) : (
                <View style={styles.qtyInlineBox}>
                  <TouchableOpacity onPress={() => removeFromCart(item.id)}>
                    <Text style={styles.qtyBtnText}>-</Text>
                  </TouchableOpacity>
                  <Text style={styles.qtyText}>{cartItem.quantity}</Text>
                  <TouchableOpacity onPress={() => addToCart(item)}>
                    <Text style={styles.qtyBtnText}>+</Text>
                  </TouchableOpacity>
                </View>
              )}
            </View>
          </View>
        </View>

        {/* RIGHT */}
        <View style={styles.imageBox}>
          <Image source={item.image} style={styles.foodImage} />
        </View>
      </View>
    );
  };

  return (
    <View style={styles.container}>

      {/* HEADER */}
      <View style={styles.header}>
        <TouchableOpacity style={styles.iconCircle} onPress={() => navigation.goBack()}>
          <Ionicons name="arrow-back" size={22} color="#1a1a2e" />
        </TouchableOpacity>
        <Text style={styles.title}>Drinks Menu</Text>
        <TouchableOpacity style={styles.iconCircle} onPress={() => navigation.navigate("Cart")}>
          <Ionicons name="cart-outline" size={20} color="#1a1a2e" />
          {cart.length > 0 && (
            <View style={styles.cartBadge}>
              <Text style={styles.cartText}>{totalItems}</Text>
            </View>
          )}
        </TouchableOpacity>
      </View>

      {/* SEARCH */}
      <View style={styles.searchContainer}>
        <Ionicons name="search" size={18} color="#aaa" />
        <TextInput
          placeholder="Search drinks..."
          placeholderTextColor="#bbb"
          value={searchText}
          onChangeText={setSearchText}
          style={styles.input}
        />
        {searchText.length > 0 && (
          <TouchableOpacity onPress={() => setSearchText("")}>
            <Ionicons name="close-circle" size={18} color="#bbb" />
          </TouchableOpacity>
        )}
      </View>

      {/* COUNT */}
      <View style={styles.countRow}>
        <Text style={styles.countText}>{filteredItems.length} items</Text>
      </View>

      {/* LIST */}
      <FlatList
        data={filteredItems}
        keyExtractor={(item) => item.id.toString()}
        renderItem={renderItem}
        showsVerticalScrollIndicator={false}
        contentContainerStyle={{ paddingBottom: totalItems > 0 ? 130 : 30, paddingTop: 4 }}
        ListEmptyComponent={
          <View style={styles.emptyBox}>
            <Text style={styles.emptyEmoji}>🍽</Text>
            <Text style={styles.emptyTitle}>Nothing found</Text>
            <Text style={styles.emptySubtitle}>Try a different search</Text>
          </View>
        }
      />

      {/* CART BAR */}
      {totalItems > 0 && (
        <View style={styles.cartBar}>
          <View style={styles.cartBarLeft}>
            <View style={styles.cartCountBadge}>
              <Text style={styles.cartCountText}>{totalItems}</Text>
            </View>
            <Text style={styles.cartBarText}>
              {totalItems} {totalItems === 1 ? "item" : "items"} added
            </Text>
          </View>
          <TouchableOpacity style={styles.checkoutBtn} onPress={() => navigation.navigate("Cart")}>
            <Text style={styles.checkoutText}>Checkout</Text>
            <Ionicons name="arrow-forward" size={14} color="#ff5722" />
          </TouchableOpacity>
        </View>
      )}
    </View>
  );
}

const styles = StyleSheet.create({
  container: { flex: 1, backgroundColor: "#faf9f7" },

  // HEADER
  header: {
    flexDirection: "row",
    justifyContent: "space-between",
    alignItems: "center",
    marginTop: 50,
    paddingHorizontal: 16,
    paddingVertical: 14,
    backgroundColor: "#fff",
    elevation: 3,
    shadowColor: "#000",
    shadowOpacity: 0.07,
    shadowOffset: { width: 0, height: 2 },
    shadowRadius: 6,
  },
  title: {
    fontSize: 17,
    fontFamily: "PoppinsSemiBold",
    color: "#1a1a2e",
    letterSpacing: 0.2,
  },
  iconCircle: {
    padding: 8,
    borderRadius: 22,
    backgroundColor: "#f0ece8",
    shadowColor: "#000",
    shadowOpacity: 0.05,
    shadowOffset: { width: 0, height: 1 },
    shadowRadius: 3,
    elevation: 1,
  },
  cartBadge: {
    position: "absolute",
    top: -4,
    right: -4,
    backgroundColor: "#ff5722",
    borderRadius: 10,
    minWidth: 18,
    height: 18,
    justifyContent: "center",
    alignItems: "center",
    paddingHorizontal: 4,
  },
  cartText: { color: "#fff", fontSize: 9, fontFamily: "PoppinsBold" },

  // SEARCH
  searchContainer: {
    flexDirection: "row",
    alignItems: "center",
    backgroundColor: "#f0ece8",
    marginHorizontal: 16,
    marginTop: 14,
    marginBottom: 4,
    paddingHorizontal: 16,
    paddingVertical: 2,
    borderRadius: 30,
  },
  input: {
    marginLeft: 8,
    flex: 1,
    fontFamily: "PoppinsRegular",
    fontSize: 13.5,
    color: "#1a1a2e",
    paddingVertical: 10,
  },

  // COUNT
  countRow: {
    paddingHorizontal: 20,
    paddingTop: 10,
    paddingBottom: 2,
  },
  countText: {
    fontFamily: "PoppinsRegular",
    fontSize: 12,
    color: "#999",
  },

  // FOOD CARD
  foodCard: {
    flexDirection: "row",
    padding: 14,
    backgroundColor: "#fff",
    marginHorizontal: 14,
    marginVertical: 7,
    borderRadius: 18,
    alignItems: "stretch",
    elevation: 3,
    shadowColor: "#000",
    shadowOpacity: 0.07,
    shadowOffset: { width: 0, height: 3 },
    shadowRadius: 8,
  },
  foodTitle: {
    fontSize: 15,
    fontFamily: "PoppinsSemiBold",
    color: "#1a1a2e",
    letterSpacing: 0.1,
  },
  foodPrice: {
    fontSize: 14.5,
    fontFamily: "PoppinsBold",
    color: "#ff5722",
    marginTop: 3,
  },
  foodDesc: {
    fontSize: 12,
    fontFamily: "PoppinsRegular",
    color: "#888",
    marginTop: 5,
    lineHeight: 17,
    marginRight: 4,
    textAlign: "justify",
    flexShrink: 1,
  },
  iconRow: {
    flexDirection: "row",
    marginTop: 10,
    alignItems: "center",
    flexWrap: "nowrap",
  },
  iconBtn: {
    width: 34,
    height: 34,
    borderRadius: 17,
    borderWidth: 1,
    borderColor: "#ede8e4",
    justifyContent: "center",
    alignItems: "center",
    marginRight: 8,
    backgroundColor: "#faf9f7",
  },
  imageBox: {
    width: 120,
    justifyContent: "center",
    alignItems: "center",
  },
  foodImage: {
    width: 110,
    height: 115,
    borderRadius: 18,
  },
  actionBox: {
    marginLeft: 6,
    width: 90,
    height: 36,
    justifyContent: "center",
    alignItems: "center",
    flexShrink: 1,
  },
  addInlineBtn: {
    width: 90,
    height: 36,
    backgroundColor: "#fff",
    borderWidth: 1.5,
    borderColor: "#ff5722",
    borderRadius: 12,
    justifyContent: "center",
    alignItems: "center",
  },
  addInlineText: {
    color: "#ff5722",
    fontFamily: "PoppinsSemiBold",
    fontSize: 12.5,
    letterSpacing: 0.5,
  },
  qtyInlineBox: {
    width: 90,
    height: 36,
    flexDirection: "row",
    backgroundColor: "#ff5722",
    borderRadius: 12,
    justifyContent: "space-around",
    alignItems: "center",
  },
  qtyBtnText: {
    color: "#fff",
    fontSize: 17,
    fontFamily: "PoppinsMedium",
    marginHorizontal: 6,
  },
  qtyText: {
    color: "#fff",
    fontFamily: "PoppinsBold",
    fontSize: 14,
  },

  // EMPTY STATE
  emptyBox: { alignItems: "center", marginTop: 60 },
  emptyEmoji: { fontSize: 48 },
  emptyTitle: { fontFamily: "PoppinsSemiBold", fontSize: 16, color: "#1a1a2e", marginTop: 12 },
  emptySubtitle: { fontFamily: "PoppinsRegular", fontSize: 13, color: "#999", marginTop: 5 },

  // CART BAR
  cartBar: {
    position: "absolute",
    bottom: 16,
    left: 14,
    right: 14,
    height: 56,
    backgroundColor: "#1a1a2e",
    flexDirection: "row",
    justifyContent: "space-between",
    alignItems: "center",
    paddingHorizontal: 16,
    borderRadius: 32,
    elevation: 10,
    shadowColor: "#000",
    shadowOpacity: 0.22,
    shadowOffset: { width: 0, height: 5 },
    shadowRadius: 10,
  },
  cartBarLeft: { flexDirection: "row", alignItems: "center", gap: 10 },
  cartCountBadge: {
    width: 26,
    height: 26,
    borderRadius: 13,
    backgroundColor: "#ff5722",
    justifyContent: "center",
    alignItems: "center",
  },
  cartCountText: { color: "#fff", fontFamily: "PoppinsBold", fontSize: 11 },
  cartBarText: { color: "rgba(255,255,255,0.85)", fontFamily: "PoppinsMedium", fontSize: 13 },
  checkoutBtn: {
    flexDirection: "row",
    alignItems: "center",
    backgroundColor: "#fff",
    paddingHorizontal: 16,
    paddingVertical: 8,
    borderRadius: 20,
    gap: 5,
  },
  checkoutText: { color: "#ff5722", fontFamily: "PoppinsSemiBold", fontSize: 13 },
});
</file>

<file path="Editaddress.js">
import React, { useState } from "react";
import {
  View,
  Text,
  TextInput,
  TouchableOpacity,
  StyleSheet,
  ScrollView,
  KeyboardAvoidingView,
  Platform,
} from "react-native";
import { Ionicons } from "@expo/vector-icons";

// ✅ VALIDATION FUNCTIONS
function isValidPhone(phone) {
  return /^[6-9]\d{9}$/.test(phone); // strict 10-digit Indian number
}

function isValidPincode(pin) {
  return /^\d{6}$/.test(pin); // strict 6-digit
}

const TAG_STYLES = {
  Home: { bg: "#e8f5e9", color: "#43a047" },
  Work: { bg: "#e3f2fd", color: "#1e88e5" },
  Parents: { bg: "#fff3e0", color: "#fb8c00" },
  Other: { bg: "#f3e5f5", color: "#8e24aa" },
  House: { bg: "#e8f5e9", color: "#43a047" },
  Office: { bg: "#e3f2fd", color: "#1e88e5" },
};

const TAG_ICONS = {
  Home: "🏠",
  Work: "💼",
  Parents: "👨‍👩‍👧",
  House: "🏠",
  Office: "💼",
  Other: "📍",
};

const TAG_OPTIONS = ["Home", "Work", "Parents", "House", "Office", "Other"];

const EMPTY_FORM = {
  id: null,
  name: "",
  phone: "",
  line1: "",
  line2: "",
  city: "",
  state: "",
  pincode: "",
  tag: "Other",
  instructions: "",
  isDefault: false,
};

// FIELD COMPONENT
function Field({ label, value, onChangeText, error, placeholder, keyboardType, maxLength }) {
  return (
    <View style={styles.fieldWrapper}>

      {label ? (
        <Text style={styles.fieldLabel}>{String(label)}</Text>
      ) : null}

      <TextInput
        value={value}
        onChangeText={onChangeText}
        placeholder={placeholder}
        placeholderTextColor="#bbb"
        keyboardType={keyboardType || "default"}
        maxLength={maxLength}
        style={[styles.input, error ? styles.inputError : null]}
      />

      {error ? (
        <Text style={styles.errText}>{String(error)}</Text>
      ) : null}

    </View>
  );
}

// MAIN SCREEN
export default function EditAddress({ navigation, route }) {
  const { address, onSave } = route.params || {};
  const isNew = !address;

  const [form, setForm] = useState(address ? { ...address } : { ...EMPTY_FORM });
  const [errors, setErrors] = useState({});

  const set = (key, val) => {
    setForm((f) => ({ ...f, [key]: val }));
    setErrors((e) => ({ ...e, [key]: "" }));
  };

  // ✅ VALIDATION
  const validate = () => {
    const e = {};
    const cleanedPhone = form.phone.replace(/\D/g, "");

    if (!form.name.trim()) e.name = "Required";

    if (!cleanedPhone) {
      e.phone = "Required";
    } else if (!isValidPhone(cleanedPhone)) {
      e.phone = "Enter valid 10-digit mobile number";
    }

    if (!form.line1.trim()) e.line1 = "Required";

    if (!form.city.trim()) e.city = "Required";

    if (!form.pincode) {
      e.pincode = "Required";
    } else if (!isValidPincode(form.pincode)) {
      e.pincode = "Enter valid 6-digit pincode";
    }

    setErrors(e);
    return Object.keys(e).length === 0;
  };

  // SAVE
  const handleSave = () => {
    if (!validate()) return;

    const cleanedPhone = form.phone.replace(/\D/g, "");

    const saved = {
      id: form.id || null,   // 🔥 important
      name: form.name,
      phone: cleanedPhone,
      line1: form.line1,
      line2: form.line2,
      city: form.city,
      state: form.state,
      pincode: form.pincode,
      tag: form.tag,
      isDefault: form.isDefault ?? false,
    };

    onSave && onSave(saved);
    navigation.goBack();
  };

  const isValid =
    form.name.trim() &&
    form.phone.trim() &&
    form.line1.trim() &&
    form.city.trim() &&
    form.pincode.trim();

  return (
    <View style={styles.container}>
      {/* HEADER */}
      <View style={styles.header}>
        <TouchableOpacity style={styles.backBtn} onPress={() => navigation.goBack()}>
          <Ionicons name="arrow-back" size={20} color="#1a1a2e" />
        </TouchableOpacity>

        <Text style={styles.headerTitle}>
          {isNew ? "Add New Address" : "Edit Address"}
        </Text>

        <View style={{ width: 36 }} ></View>
      </View>

      <KeyboardAvoidingView
        style={{ flex: 1 }}
        behavior={Platform.OS === "ios" ? "padding" : undefined}
      >
        <ScrollView
          contentContainerStyle={styles.formContent}
          showsVerticalScrollIndicator={false}
        >
          {/* Receiver */}
          <Text style={styles.sectionTitle}>Receiver Details</Text>
          <View style={styles.card}>
            <Field
              label="FULL NAME"
              value={form.name}
              onChangeText={(v) => set("name", v)}
              error={errors.name}
              placeholder="Enter name"
            />

            <Field
              label="PHONE NUMBER"
              value={form.phone}
              onChangeText={(v) => set("phone", v.replace(/[^0-9]/g, ""))}
              error={errors.phone}
              placeholder="9876543210"
              keyboardType="number-pad"
              maxLength={10} // ✅ restrict to 10 digits
            />
          </View>

          {/* Address */}
          <Text style={styles.sectionTitle}>Location Details</Text>
          <View style={styles.card}>
            <Field
              label="ADDRESS LINE"
              value={form.line1}
              onChangeText={(v) => set("line1", v)}
              error={errors.line1}
              placeholder="Flat / Street"
            />

            <Field
              label="CITY"
              value={form.city}
              onChangeText={(v) => set("city", v)}
              error={errors.city}
              placeholder="City"
            />

            <Field
              label="STATE"
              value={form.state}
              onChangeText={(v) => set("state", v)}
              placeholder="State"
            />

            <Field
              label="PINCODE"
              value={form.pincode}
              onChangeText={(v) => set("pincode", v.replace(/[^0-9]/g, ""))}
              error={errors.pincode}
              placeholder="500034"
              keyboardType="number-pad"
              maxLength={6} // ✅ restrict to 6 digits
            />
          </View>

          {/* SAVE */}
          <TouchableOpacity
            style={[styles.saveBtn, !isValid && styles.saveBtnDisabled]}
            onPress={handleSave}
          >
            <Text style={styles.saveBtnText}>
              {isNew ? "Save Address" : "Update Address"}
            </Text>
          </TouchableOpacity>
        </ScrollView>
      </KeyboardAvoidingView>
    </View>
  );
}

// STYLES (UNCHANGED)
const styles = StyleSheet.create({
  container: { flex: 1, backgroundColor: "#faf9f7" },

  header: {
    flexDirection: "row",
    justifyContent: "space-between",
    alignItems: "center",
    marginTop: 54,
    paddingHorizontal: 18,
    marginBottom: 10,
  },
  backBtn: {
    width: 36,
    height: 36,
    borderRadius: 18,
    backgroundColor: "#f0ece8",
    justifyContent: "center",
    alignItems: "center",
  },
  headerTitle: {
    fontSize: 20,
    fontWeight: "600",
    color: "#1a1a2e",
  },

  formContent: {
    padding: 16,
    paddingBottom: 40,
  },

  sectionTitle: {
    fontSize: 15,
    fontWeight: "700",
    color: "#1a1a2e",
    marginTop: 14,
    marginBottom: 8,
  },

  card: {
    backgroundColor: "#fff",
    borderRadius: 18,
    padding: 16,
    marginBottom: 10,
  },

  fieldWrapper: { marginBottom: 12 },

  fieldLabel: {
    fontSize: 11,
    fontWeight: "600",
    color: "#888",
    marginBottom: 5,
    textTransform: "uppercase",
  },

  input: {
    borderWidth: 1.5,
    borderColor: "#e0dbd5",
    borderRadius: 12,
    paddingHorizontal: 13,
    paddingVertical: 11,
    fontSize: 14,
  },

  inputError: { borderColor: "#e53935" },

  errText: { fontSize: 11, color: "#e53935", marginTop: 3 },

  saveBtn: {
    backgroundColor: "#ff5722",
    borderRadius: 18,
    paddingVertical: 15,
    alignItems: "center",
    marginTop: 8,
  },

  saveBtnDisabled: { opacity: 0.45 },

  saveBtnText: {
    color: "#fff",
    fontSize: 15,
    fontWeight: "700",
  },
});
</file>

<file path="EditProfileScreen.js">
import React, { useState, useRef } from "react";
import {
  View,
  Text,
  StyleSheet,
  TouchableOpacity,
  ScrollView,
  TextInput,
  Alert
} from "react-native";
import { Ionicons } from "@expo/vector-icons";
import DateTimePicker from "@react-native-community/datetimepicker";
// Add at the top of EditProfileScreen.js
import { updateProfile } from "./api";
import { Platform } from "react-native";




function isValidEmail(email) {
  return /^[^\s@]+@[^\s@]+\.[a-zA-Z]{2,6}$/.test(email.trim());
}

function isValidPhone(phone) {
  return /^[6-9]\d{9}$/.test(phone);
}
function Field({
  label, icon, value, onChangeText, onBlur,
  placeholder, keyboardType = "default", error,
}) {
  const hasError = !!error;
  return (
    <View style={styles.fieldWrapper}>
      <Text style={styles.fieldLabel}>{label}</Text>
      <View style={[styles.inputRow, hasError && styles.inputRowError]}>
        <Ionicons
          name={icon}
          size={17}
          color={hasError ? "#e53935" : "#ff5722"}
          style={{ marginRight: 10 }}
        />
        <TextInput
          style={styles.input}
          value={value}
          onChangeText={onChangeText}
          onBlur={onBlur}
          placeholder={placeholder}
          placeholderTextColor="#bbb"
          keyboardType={keyboardType}
          autoCorrect={false}
          autoCapitalize="none"
        />
        {hasError ? (
          <Ionicons name="alert-circle" size={16} color="#e53935" style={{ marginLeft: 6 }} />
        ) : value.trim().length > 0 ? (
          <Ionicons name="checkmark-circle" size={16} color="#43a047" style={{ marginLeft: 6 }} />
        ) : null}
      </View>
      {hasError && <Text style={styles.errorText}>{error}</Text>}
    </View>
  );
}

// ─────────────────────────────────────────────
const GENDERS = ["Male", "Female", "Other", "Prefer not to say"];

// ─────────────────────────────────────────────
export default function EditProfileScreen({ navigation, route }) {
  const existing = route?.params?.profile || {};

  const [name, setName] = useState(existing.name || "Annapurna User");
  const formatPhone = (phone) => {
    if (!phone) return "";

    // Remove +91 if exists
    return phone.replace(/^(\+91)/, "").replace(/\D/g, "").slice(0, 10);
  };

  const [phone, setPhone] = useState(formatPhone(existing.phone));

  const [email, setEmail] = useState(existing.email || "");
  const [gender, setGender] = useState(existing.gender || "");


  const [dob, setDob] = useState("");
  const [showPicker, setShowPicker] = useState(false);
  const [dobErr, setDobErr] = useState("");

  const [emailTouched, setEmailTouched] = useState(false);
  const [phoneTouched, setPhoneTouched] = useState(false);

  const emailErr = emailTouched && email.trim().length > 0 && !isValidEmail(email)
    ? "Enter a valid email (e.g. you@example.com)"
    : "";

  const cleanedPhone = phone.replace(/\s+/g, "");

  const phoneErr =
    phoneTouched && phone && !isValidPhone(cleanedPhone)
      ? "Enter valid 10-digit mobile number"
      : "";

  const handleDateChange = (event, selectedDate) => {
    setShowPicker(false);

    if (selectedDate) {
      const day = String(selectedDate.getDate()).padStart(2, "0");
      const month = String(selectedDate.getMonth() + 1).padStart(2, "0");
      const year = selectedDate.getFullYear();

      const formatted = `${day}/${month}/${year}`;
      setDob(formatted);

      setDobErr(""); // no error if selected
    }
  };



  // Replace the handleSave function:
  const handleSave = async () => {
    setEmailTouched(true);

    if (!name.trim()) {
      Alert.alert("Validation", "Name cannot be empty.");
      return;
    }
    if (phone && !isValidPhone(cleanedPhone)) {
      Alert.alert("Invalid Phone", "Enter valid mobile number");
      return;
    }
    if (email.trim().length > 0 && !isValidEmail(email)) {
      Alert.alert("Invalid Email", "Please enter a valid email address.");
      return;
    }
    if (!dob) {
      Alert.alert("Invalid Date", "Please select your date of birth");
      return;
    }

    try {
      await updateProfile({
        name: name.trim(),
        email: email.trim(),
        dob: dob,
        gender,
        phone: phone.trim(),
      });
    } catch (e) {
      console.log("Profile update error:", e);
    }

    navigation.navigate("Profile", {
      updatedProfile: {
        name: name.trim(),
        phone: phone.trim(),
        email: email.trim(),
        dob,
        gender,
      },
    });
  };
  return (
    <View style={styles.container}>
      {/* HEADER */}
      <View style={styles.header}>
        <TouchableOpacity style={styles.backBtn} onPress={() => navigation.goBack()}>
          <Ionicons name="arrow-back" size={20} color="#1a1a2e" />
        </TouchableOpacity>
        <Text style={styles.headerTitle}>Edit Profile</Text>
        <View style={{ width: 36 }} />
      </View>

      <ScrollView
        showsVerticalScrollIndicator={false}
        contentContainerStyle={{ paddingBottom: 40 }}
        keyboardShouldPersistTaps="handled"
      >
        {/* AVATAR */}
        <View style={styles.avatarSection}>
          <View style={styles.avatar}>
            <Text style={styles.avatarText}>
              {name.trim().charAt(0).toUpperCase() || "A"}
            </Text>
          </View>
          <TouchableOpacity style={styles.changePhotoBtn}>
            <Ionicons name="camera" size={14} color="#fff" />
          </TouchableOpacity>
          <Text style={styles.changePhotoLabel}>Change Photo</Text>
        </View>

        {/* FORM CARD */}
        <View style={styles.card}>
          <Field
            label="Full Name"
            icon="person-outline"
            value={name}
            onChangeText={setName}
            placeholder="Enter your name"
          />
          <View style={styles.divider} />

          <Field
            label="Phone Number"
            icon="call-outline"
            value={phone}
            onChangeText={(text) => {
              // Remove non-numeric characters
              let cleaned = text.replace(/[^0-9]/g, "");

              // Limit to 10 digits
              if (cleaned.length > 10) {
                cleaned = cleaned.slice(0, 10);
              }

              setPhone(cleaned);
            }}

            onBlur={() => setPhoneTouched(true)}
            placeholder="XXXXX XXXXX"
            keyboardType="phone-pad"
          />
          <View style={styles.divider} />

          <Field
            label="Email Address"
            icon="mail-outline"
            value={email}
            onChangeText={(t) => { setEmail(t); }}
            onBlur={() => setEmailTouched(true)}
            placeholder="you@example.com"
            keyboardType="email-address"
            error={emailErr}
          />
          <View style={styles.divider} />

          <View style={styles.fieldWrapper}>
            <Text style={styles.fieldLabel}>Date of Birth</Text>

            {Platform.OS === "web" ? (
              // ✅ Laptop / Browser
              <input
                type="date"
                value={dob}
                onChange={(e) => setDob(e.target.value)}
                style={{ padding: 10, borderRadius: 8 }}
              />
            ) : (
              // ✅ Mobile
              <>
                <TouchableOpacity onPress={() => setShowPicker(true)}>
                  <Text>{dob ? dob : "Select DOB"}</Text>
                </TouchableOpacity>

                {showPicker && (
                  <DateTimePicker
                    value={dob ? new Date(dob) : new Date()}
                    mode="date"
                    display="default"
                    onChange={(event, selectedDate) => {
                      setShowPicker(false);
                      if (selectedDate) {
                        setDob(selectedDate.toISOString().split("T")[0]);
                      }
                    }}
                  />
                )}
              </>
            )}

            <Text style={styles.hintText}>Tap to select your DOB</Text>

            {showPicker && (
              <DateTimePicker
                value={
                  dob
                    ? (() => {
                      const [d, m, y] = dob.split("/");
                      return new Date(y, m - 1, d);
                    })()
                    : new Date(2000, 0, 1)
                }
                mode="date"
                display={Platform.OS === "ios" ? "spinner" : "default"}
                maximumDate={new Date()}
                onChange={(event, selectedDate) => {
                  if (Platform.OS === "android") {
                    setShowPicker(false); // important for Android
                  }
                  handleDateChange(event, selectedDate);
                }}
              />
            )}
          </View>
        </View>

        {/* GENDER */}
        <Text style={styles.sectionLabel}>GENDER</Text>
        <View style={styles.card}>
          <View style={styles.genderRow}>
            {GENDERS.map((g) => (
              <TouchableOpacity
                key={g}
                style={[styles.genderChip, gender === g && styles.genderChipActive]}
                onPress={() => setGender(g)}
              >
                <Text style={[styles.genderChipText, gender === g && styles.genderChipTextActive]}>
                  {g}
                </Text>
              </TouchableOpacity>
            ))}
          </View>
        </View>

        {/* SAVE */}
        <TouchableOpacity style={styles.saveBtn} onPress={handleSave}>
          <Text style={styles.saveBtnText}>Save Changes</Text>
        </TouchableOpacity>
      </ScrollView>
    </View>
  );
}

const styles = StyleSheet.create({
  container: { flex: 1, backgroundColor: "#faf9f7" },

  header: {
    flexDirection: "row",
    justifyContent: "space-between",
    alignItems: "center",
    marginTop: 54,
    paddingHorizontal: 18,
    marginBottom: 20,
  },
  backBtn: {
    width: 36, height: 36, borderRadius: 18,
    backgroundColor: "#f0ece8",
    justifyContent: "center", alignItems: "center",
  },
  headerTitle: { fontSize: 20, fontFamily: "PoppinsSemiBold", color: "#1a1a2e" },

  avatarSection: { alignItems: "center", marginBottom: 28, position: "relative" },
  avatar: {
    width: 90, height: 90, borderRadius: 45,
    backgroundColor: "#ff5722",
    justifyContent: "center", alignItems: "center",
  },
  avatarText: { fontFamily: "PoppinsBold", fontSize: 36, color: "#fff" },
  changePhotoBtn: {
    position: "absolute", bottom: 26, right: "36%",
    width: 28, height: 28, borderRadius: 14,
    backgroundColor: "#1a1a2e",
    justifyContent: "center", alignItems: "center",
    borderWidth: 2, borderColor: "#faf9f7",
  },
  changePhotoLabel: { marginTop: 8, fontFamily: "PoppinsMedium", fontSize: 12, color: "#ff5722" },

  sectionLabel: {
    fontFamily: "PoppinsSemiBold", fontSize: 11, color: "#aaa",
    letterSpacing: 1, textTransform: "uppercase",
    marginBottom: 8, marginLeft: 20, marginTop: 10,
  },
  card: {
    backgroundColor: "#fff", borderRadius: 18,
    marginHorizontal: 16, marginBottom: 14,
    elevation: 2, paddingHorizontal: 16, paddingVertical: 6,
  },
  divider: { height: 1, backgroundColor: "#f0ece8" },

  fieldWrapper: { paddingVertical: 14 },
  fieldLabel: {
    fontFamily: "PoppinsMedium", fontSize: 11, color: "#aaa",
    marginBottom: 6, textTransform: "uppercase", letterSpacing: 0.5,
  },
  inputRow: {
    flexDirection: "row", alignItems: "center",
    borderBottomWidth: 1.5, borderBottomColor: "transparent", paddingBottom: 4,
  },
  inputRowError: { borderBottomColor: "#e53935" },
  input: { flex: 1, fontFamily: "PoppinsRegular", fontSize: 14, color: "#1a1a2e", paddingVertical: 0 },
  errorText: { fontFamily: "PoppinsRegular", fontSize: 11, color: "#e53935", marginTop: 5 },
  hintText: { fontFamily: "PoppinsRegular", fontSize: 11, color: "#bbb", marginTop: 5 },

  genderRow: { flexDirection: "row", flexWrap: "wrap", gap: 10, paddingVertical: 14 },
  genderChip: {
    paddingHorizontal: 14, paddingVertical: 7, borderRadius: 20,
    borderWidth: 1.5, borderColor: "#e0dbd6", backgroundColor: "#faf9f7",
  },
  genderChipActive: { borderColor: "#ff5722", backgroundColor: "#fff0eb" },
  genderChipText: { fontFamily: "PoppinsMedium", fontSize: 13, color: "#888" },
  genderChipTextActive: { color: "#ff5722" },

  saveBtn: {
    marginHorizontal: 16, marginTop: 8,
    backgroundColor: "#ff5722", borderRadius: 18,
    paddingVertical: 16, alignItems: "center", elevation: 4,
  },
  saveBtnText: { fontFamily: "PoppinsSemiBold", fontSize: 15, color: "#fff" },
});
</file>

<file path="HelpFAQScreen.js">
import React from "react";
import { View, Text, StyleSheet, ScrollView, TouchableOpacity } from "react-native";
import { Ionicons } from "@expo/vector-icons";

export default function HelpFAQScreen({ navigation }) {
  const faqs = [
    {
      question: "How to place an order?",
      answer: "Browse items → Add to cart → Checkout → Confirm order.",
    },
    {
      question: "How to track my order?",
      answer: "Go to Profile → Order History → Select order.",
    },
    {
      question: "Payment issues?",
      answer: "Try again or contact support.",
    },
  ];

  return (
    <View style={styles.container}>
      <ScrollView showsVerticalScrollIndicator={false}>
        
        {/* HEADER */}
        <View style={styles.header}>
          <TouchableOpacity
            style={styles.backBtn}
            onPress={() => navigation.goBack()}
          >
            <Ionicons name="arrow-back" size={20} color="#1a1a2e" />
          </TouchableOpacity>
          <Text style={styles.headerTitle}>Help & FAQ</Text>
          <View style={{ width: 36 }} />
        </View>

        {/* FAQ LIST */}
        <View style={styles.section}>
          <View style={styles.sectionCard}>
            {faqs.map((item, index) => (
              <View
                key={index}
                style={[
                  styles.faqItem,
                  index < faqs.length - 1 && styles.borderBottom,
                ]}
              >
                <View style={styles.questionRow}>
                  <View style={styles.iconBox}>
                    <Ionicons
                      name="help-circle-outline"
                      size={18}
                      color="#ff5722"
                    />
                  </View>
                  <Text style={styles.question}>{item.question}</Text>
                </View>

                <Text style={styles.answer}>{item.answer}</Text>
              </View>
            ))}
          </View>
        </View>
      </ScrollView>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#faf9f7",
  },

  /* HEADER (same as profile) */
  header: {
    flexDirection: "row",
    alignItems: "center",
    justifyContent: "space-between",
    marginTop: 54,
    paddingHorizontal: 18,
    marginBottom: 20,
  },

  backBtn: {
    width: 36,
    height: 36,
    borderRadius: 18,
    backgroundColor: "#f0ece8",
    justifyContent: "center",
    alignItems: "center",
  },

  headerTitle: {
    fontSize: 22,
    fontFamily: "PoppinsSemiBold",
    color: "#1a1a2e",
  },

  /* SECTION */
  section: {
    marginHorizontal: 16,
    marginBottom: 18,
  },

  sectionCard: {
    backgroundColor: "#fff",
    borderRadius: 18,
    elevation: 3,
    overflow: "hidden",
  },

  faqItem: {
    paddingVertical: 14,
    paddingHorizontal: 16,
  },

  borderBottom: {
    borderBottomWidth: 1,
    borderBottomColor: "#f0ece8",
  },

  questionRow: {
    flexDirection: "row",
    alignItems: "center",
    gap: 12,
    marginBottom: 6,
  },

  iconBox: {
    width: 32,
    height: 32,
    borderRadius: 10,
    backgroundColor: "#fff0eb",
    justifyContent: "center",
    alignItems: "center",
  },

  question: {
    fontFamily: "PoppinsMedium",
    fontSize: 14,
    color: "#1a1a2e",
    flex: 1,
  },

  answer: {
    fontFamily: "PoppinsRegular",
    fontSize: 13,
    color: "#888",
    marginLeft: 44,
  },
});
</file>

<file path="Home.js">
import React, { useState, useEffect, useRef } from "react";
import {
  View,
  Text,
  StyleSheet,
  TextInput,
  TouchableOpacity,
  ScrollView,
  Image,
  Dimensions,
  FlatList,
  Share,
  Modal,
  Animated,
} from "react-native";
import { Ionicons, MaterialIcons } from "@expo/vector-icons";
import { useContext } from "react";
import { CartContext } from "./CartContext";

import Herobg from "./assets/herobg.jpg";
import Chickenb from "./assets/chickenb.jpg";
import Muttong from "./assets/muttong.jpg";

import BiryaniImg from "./assets/biryanicm.png";
import MealsImg from "./assets/mealscm.png";
import StartersImg from "./assets/starter.png";
import DessertsImg from "./assets/desserts.png";
import DrinksImg from "./assets/drinks.png";
import TandooriImg from "./assets/tandoori.png";
import { allFoodItems } from "./Data";

const { width, height } = Dimensions.get("window");
const BANNER_WIDTH = width - 32;

const bannerSlides = [
  { id: "1", image: Herobg,    title: "Welcome to Annapurna", subtitle: "Taste the tradition"  },
  { id: "2", image: Chickenb,  title: "Chicken Dum Biryani",  subtitle: "Our signature dish"   },
  { id: "3", image: Muttong,   title: "Mutton Biryani",       subtitle: "Rich & aromatic"       },
];

const bestSellerIds = [1, 2, 29, 30];
const bestSellers = bestSellerIds.map((id) =>
  allFoodItems.find((item) => item.id === id)
);

const CATEGORIES    = ["All", "Biryani", "Meals", "Starters", "Tandoori", "Desserts", "Drinks"];
const SORT_OPTIONS  = [
  { label: "Relevance",         value: "default"    },
  { label: "Price: Low to High", value: "price_asc"  },
  { label: "Price: High to Low", value: "price_desc" },
  { label: "Name: A to Z",       value: "name_asc"   },
];
const PRICE_RANGES  = [
  { label: "All Prices",   min: 0,   max: Infinity },
  { label: "Under ₹100",   min: 0,   max: 99       },
  { label: "₹100 – ₹200",  min: 100, max: 200      },
  { label: "Above ₹200",   min: 201, max: Infinity  },
];

export default function HomeScreen({ navigation }) {
  const [searchText,       setSearchText]       = useState("");
  const [activeBanner,     setActiveBanner]     = useState(0);
  const [savedItems,       setSavedItems]       = useState([]);
  const [showFilter,       setShowFilter]       = useState(false);
  const [selectedCategory, setSelectedCategory] = useState("All");
  const [selectedSort,     setSelectedSort]     = useState("default");
  const [selectedPrice,    setSelectedPrice]    = useState(0);
  const [activeFilters,    setActiveFilters]    = useState(0);

  const slideAnim = useRef(new Animated.Value(height)).current;
  const bannerRef = useRef(null);
  const { cart, addToCart, removeFromCart } = useContext(CartContext);
  const totalItems = cart.reduce((sum, item) => sum + item.quantity, 0);

  const categories = [
    { name: "Biryani",  img: BiryaniImg,  screen: "Biryani"  },
    { name: "Meals",    img: MealsImg,    screen: "Meals"    },
    { name: "Starters", img: StartersImg, screen: "Starters" },
    { name: "Tandoori", img: TandooriImg, screen: "Tandoori" },
    { name: "Desserts", img: DessertsImg, screen: "Desserts" },
    { name: "Drinks",   img: DrinksImg,   screen: "Drinks"   },
  ];

  useEffect(() => {
    const interval = setInterval(() => {
      setActiveBanner((prev) => {
        const next = (prev + 1) % bannerSlides.length;
        bannerRef.current?.scrollToIndex({ index: next, animated: true });
        return next;
      });
    }, 5000);
    return () => clearInterval(interval);
  }, []);

  const openFilter = () => {
    setShowFilter(true);
    Animated.spring(slideAnim, { toValue: 0, useNativeDriver: true, tension: 60, friction: 10 }).start();
  };
  const closeFilter = () => {
    Animated.timing(slideAnim, { toValue: height, duration: 280, useNativeDriver: true }).start(() =>
      setShowFilter(false)
    );
  };

  const applyFilters = () => {
    let count = 0;
    if (selectedCategory !== "All")    count++;
    if (selectedSort     !== "default") count++;
    if (selectedPrice    !== 0)        count++;
    setActiveFilters(count);
    closeFilter();
  };

  const resetFilters = () => {
    setSelectedCategory("All");
    setSelectedSort("default");
    setSelectedPrice(0);
    setActiveFilters(0);
  };

  const getFilteredItems = () => {
    let items = allFoodItems.filter((item) =>
      item.name.toLowerCase().includes(searchText.trim().toLowerCase())
    );
    if (selectedCategory !== "All") items = items.filter((i) => i.category === selectedCategory);
    const range = PRICE_RANGES[selectedPrice];
    items = items.filter((i) => i.price >= range.min && i.price <= range.max);
    if (selectedSort === "price_asc")  items = [...items].sort((a, b) => a.price - b.price);
    if (selectedSort === "price_desc") items = [...items].sort((a, b) => b.price - a.price);
    if (selectedSort === "name_asc")   items = [...items].sort((a, b) => a.name.localeCompare(b.name));
    return items;
  };

  const filteredItems = getFilteredItems();
  const isVeg = (item) =>
    item.category === "Desserts" || item.category === "Drinks" ||
    item.name.toLowerCase().includes("veg") ||
    item.name.toLowerCase().includes("paneer") ||
    item.name.toLowerCase().includes("gobi") ||
    item.name.toLowerCase().includes("mushroom");

  const toggleSave = (item) => {
    const exists = savedItems.find((i) => i.id === item.id);
    setSavedItems(exists ? savedItems.filter((i) => i.id !== item.id) : [...savedItems, item]);
  };

  const shareItem = async (item) => {
    try {
      await Share.share({ message: `${item.name} - Rs.${item.price}\nTry this delicious food!` });
    } catch (error) { console.log(error); }
  };

  const getCartItem = (item) => cart.find((i) => i.id === item.id);

  const CartControl = ({ item, compact }) => {
    const cartItem = getCartItem(item);
    if (!cartItem) {
      return (
        <TouchableOpacity
          style={compact ? styles.addBtnCompact : styles.addInlineBtn}
          onPress={() => addToCart(item)}
        >
          <Text style={compact ? styles.addBtnCompactText : styles.addInlineText}>ADD +</Text>
        </TouchableOpacity>
      );
    }
    return (
      <View style={compact ? styles.qtyCompactBox : styles.qtyInlineBox}>
        <TouchableOpacity onPress={() => removeFromCart(item.id)}>
          <Text style={styles.qtyBtnText}>-</Text>
        </TouchableOpacity>
        <Text style={styles.qtyText}>{cartItem.quantity}</Text>
        <TouchableOpacity onPress={() => addToCart(item)}>
          <Text style={styles.qtyBtnText}>+</Text>
        </TouchableOpacity>
      </View>
    );
  };

  const showResultsView = searchText.length > 0 || activeFilters > 0;

  return (
    <View style={styles.container}>
      <ScrollView showsVerticalScrollIndicator={false} contentContainerStyle={{ paddingBottom: 140 }}>

        {/* HEADER */}
        <View style={styles.header}>
          <View style={styles.titleBox}>
            <Text style={styles.title}>Annapurna Restaurant</Text>
            <Text style={styles.open}>● Open • 10:00 AM – 11:00 PM</Text>
          </View>
          <TouchableOpacity style={styles.iconCircle} onPress={() => navigation.navigate("Cart")}>
            <Ionicons name="cart-outline" size={20} color="#1a1a2e" />
            {cart.length > 0 && (
              <View style={styles.cartBadge}>
                <Text style={styles.cartText}>{totalItems}</Text>
              </View>
            )}
          </TouchableOpacity>
        </View>

        {/* SEARCH BAR */}
        <View style={styles.searchContainer}>
          <View style={styles.searchBox}>
            <Ionicons name="search" size={18} color="#aaa" />
            <TextInput
              placeholder="Search food..."
              placeholderTextColor="#bbb"
              value={searchText}
              onChangeText={setSearchText}
              style={styles.input}
            />
            {searchText.length > 0 && (
              <TouchableOpacity onPress={() => setSearchText("")}>
                <Ionicons name="close-circle" size={18} color="#bbb" />
              </TouchableOpacity>
            )}
          </View>
          <TouchableOpacity
            style={[styles.filterBtn, activeFilters > 0 && styles.filterBtnActive]}
            onPress={openFilter}
          >
            <MaterialIcons name="tune" size={20} color={activeFilters > 0 ? "#fff" : "#ff5722"} />
            {activeFilters > 0 && (
              <View style={styles.filterBadge}>
                <Text style={styles.filterBadgeText}>{activeFilters}</Text>
              </View>
            )}
          </TouchableOpacity>
        </View>

        {/* ACTIVE FILTER CHIPS */}
        {activeFilters > 0 && (
          <ScrollView horizontal showsHorizontalScrollIndicator={false} style={styles.chipRow}>
            {selectedCategory !== "All" && (
              <View style={styles.chip}>
                <Text style={styles.chipText}>{selectedCategory}</Text>
                <TouchableOpacity onPress={() => { setSelectedCategory("All"); setActiveFilters(c => Math.max(0, c - 1)); }}>
                  <Ionicons name="close" size={13} color="#ff5722" style={{ marginLeft: 4 }} />
                </TouchableOpacity>
              </View>
            )}
            {selectedSort !== "default" && (
              <View style={styles.chip}>
                <Text style={styles.chipText}>{SORT_OPTIONS.find(s => s.value === selectedSort)?.label}</Text>
                <TouchableOpacity onPress={() => { setSelectedSort("default"); setActiveFilters(c => Math.max(0, c - 1)); }}>
                  <Ionicons name="close" size={13} color="#ff5722" style={{ marginLeft: 4 }} />
                </TouchableOpacity>
              </View>
            )}
            {selectedPrice !== 0 && (
              <View style={styles.chip}>
                <Text style={styles.chipText}>{PRICE_RANGES[selectedPrice].label}</Text>
                <TouchableOpacity onPress={() => { setSelectedPrice(0); setActiveFilters(c => Math.max(0, c - 1)); }}>
                  <Ionicons name="close" size={13} color="#ff5722" style={{ marginLeft: 4 }} />
                </TouchableOpacity>
              </View>
            )}
            <TouchableOpacity style={styles.chipClear} onPress={resetFilters}>
              <Text style={styles.chipClearText}>Clear all</Text>
            </TouchableOpacity>
          </ScrollView>
        )}

        {/* RESULTS VIEW or HOME VIEW */}
        {showResultsView ? (
          <View style={{ paddingHorizontal: 14 }}>
            <View style={styles.resultHeader}>
              <Text style={styles.sectionTitle}>
                {searchText.length > 0 ? "Search Results" : selectedCategory === "All" ? "All Items" : selectedCategory}
              </Text>
              <Text style={styles.resultCount}>{filteredItems.length} items</Text>
            </View>

            {filteredItems.length > 0 ? (
              filteredItems.map((item) => (
                <View key={item.id} style={styles.foodCard}>
                  <View style={{ flex: 1, paddingRight: 10 }}>
                    <View style={styles.vegBadgeRow}>
                      <View style={[styles.vegDot, { borderColor: isVeg(item) ? "#4caf50" : "#e53935" }]}>
                        <View style={[styles.vegDotInner, { backgroundColor: isVeg(item) ? "#4caf50" : "#e53935" }]} />
                      </View>
                      <Text style={styles.foodCategory}>{item.category}</Text>
                    </View>
                    <Text style={styles.foodTitle}>{item.name}</Text>
                    <Text style={styles.foodPrice}>Rs.{item.price}</Text>
                    <Text style={styles.foodDesc}>{item.description}</Text>
                    <View style={styles.iconRow}>
                      <TouchableOpacity style={styles.iconBtn} onPress={() => toggleSave(item)}>
                        <Ionicons
                          name={savedItems.find((i) => i.id === item.id) ? "heart" : "heart-outline"}
                          size={17} color="#ff5722"
                        />
                      </TouchableOpacity>
                      <TouchableOpacity style={styles.iconBtn} onPress={() => shareItem(item)}>
                        <Ionicons name="share-social-outline" size={17} color="#444" />
                      </TouchableOpacity>
                      <View style={styles.actionBox}>
                        <CartControl item={item} compact={false} />
                      </View>
                    </View>
                  </View>
                  <View style={styles.imageBox}>
                    <Image source={item.image} style={styles.foodImage} />
                  </View>
                </View>
              ))
            ) : (
              <View style={styles.emptyBox}>
                <Text style={styles.emptyEmoji}>🍽</Text>
                <Text style={styles.emptyTitle}>Nothing found</Text>
                <Text style={styles.emptySubtitle}>Try adjusting your search or filters</Text>
              </View>
            )}
          </View>
        ) : (
          <>
            {/* BANNER */}
            <View style={styles.bannerWrapper}>
              <FlatList
                ref={bannerRef}
                data={bannerSlides}
                horizontal pagingEnabled scrollEnabled
                showsHorizontalScrollIndicator={false}
                keyExtractor={(item) => item.id}
                onMomentumScrollEnd={(e) => {
                  const index = Math.round(e.nativeEvent.contentOffset.x / BANNER_WIDTH);
                  setActiveBanner(index);
                }}
                getItemLayout={(_, index) => ({ length: BANNER_WIDTH, offset: BANNER_WIDTH * index, index })}
                renderItem={({ item }) => (
                  <View style={styles.bannerSlide}>
                    <Image source={item.image} style={styles.bannerImage} />
                    <View style={styles.bannerOverlay}>
                      <Text style={styles.bannerTitle}>{item.title}</Text>
                      <Text style={styles.bannerSubtitle}>{item.subtitle}</Text>
                    </View>
                  </View>
                )}
              />
              <View style={styles.dotsRow}>
                {bannerSlides.map((_, i) => (
                  <View key={i} style={[styles.dot, i === activeBanner && styles.dotActive]} />
                ))}
              </View>
            </View>

            {/* CATEGORIES */}
            <ScrollView horizontal showsHorizontalScrollIndicator={false} style={styles.catRow}
              contentContainerStyle={{ paddingLeft: 16, paddingRight: 16 }}>
              {categories.map((item, index) => (
                <TouchableOpacity key={index} style={[styles.categoryCard, { marginRight: index !== categories.length - 1 ? 12 : 0 }]} onPress={() => navigation.navigate(item.screen)}>
                  <Image source={item.img} style={styles.categoryImg} />
                  <Text style={styles.categoryText}>{item.name}</Text>
                </TouchableOpacity>
              ))}
            </ScrollView>

            {/* BEST SELLERS */}
            <View style={styles.rowTitle}>
              <Text style={styles.sectionTitle}>Best Sellers</Text>
              <Text style={styles.sectionSub}>Our most loved dishes</Text>
            </View>
            <ScrollView horizontal showsHorizontalScrollIndicator={false} contentContainerStyle={{ paddingLeft: 16, paddingRight: 8 }}>
              {bestSellers.map((item) => (
                <View key={item.id} style={styles.card}>
                  <Image source={item.image} style={styles.cardImg} />
                  <Text style={styles.cardTitle}>{item.name}</Text>
                  <View style={styles.cardBottom}>
                    <Text style={styles.cardPrice}>Rs.{item.price}</Text>
                    <CartControl item={item} compact={true} />
                  </View>
                </View>
              ))}
            </ScrollView>
          </>
        )}
      </ScrollView>

      {/* FOOTER */}
      <View style={styles.footer}>
        <TouchableOpacity style={styles.footerBtn} onPress={() => navigation.navigate("Home")}>
          <Ionicons name="home" size={22} color="#fff" />
          <Text style={styles.footerLabel}>Home</Text>
        </TouchableOpacity>
        <TouchableOpacity style={styles.footerBtn} onPress={() => navigation.navigate("Wishlist")}>
          <Ionicons name="heart-outline" size={22} color="#fff" />
          <Text style={styles.footerLabel}>Wishlist</Text>
        </TouchableOpacity>
        <TouchableOpacity style={styles.footerBtn} onPress={() => navigation.navigate("Cart")}>
          <Ionicons name="cart-outline" size={22} color="#fff" />
          <Text style={styles.footerLabel}>Cart</Text>
        </TouchableOpacity>
        <TouchableOpacity style={styles.footerBtn}onPress={() => navigation.navigate("Profile")}>
          <Ionicons name="person-outline" size={22} color="#fff" />
          <Text style={styles.footerLabel}>Profile</Text>
        </TouchableOpacity>
      </View>

      {/* CART BAR */}
      {totalItems > 0 && (
        <View style={styles.cartBar}>
          <View style={styles.cartBarLeft}>
            <View style={styles.cartCountBadge}>
              <Text style={styles.cartCountText}>{totalItems}</Text>
            </View>
            <Text style={styles.cartBarText}>{totalItems} {totalItems === 1 ? "item" : "items"} added</Text>
          </View>
          <TouchableOpacity style={styles.checkoutBtn} onPress={() => navigation.navigate("Cart")}>
            <Text style={styles.checkoutText}>Checkout</Text>
            <Ionicons name="arrow-forward" size={14} color="#ff5722" />
          </TouchableOpacity>
        </View>
      )}

      {/* FILTER BOTTOM SHEET */}
      {showFilter && (
        <Modal transparent animationType="none" onRequestClose={closeFilter}>
          <TouchableOpacity style={styles.modalBackdrop} activeOpacity={1} onPress={closeFilter} />
          <Animated.View style={[styles.filterSheet, { transform: [{ translateY: slideAnim }] }]}>
            <View style={styles.sheetHandle} />
            <View style={styles.sheetHeader}>
              <Text style={styles.sheetTitle}>Filters</Text>
              <TouchableOpacity onPress={resetFilters}>
                <Text style={styles.resetText}>Reset all</Text>
              </TouchableOpacity>
            </View>

            <ScrollView showsVerticalScrollIndicator={false}>
              {/* CATEGORY */}
              <Text style={styles.filterLabel}>Category</Text>
              <View style={styles.optionsWrap}>
                {CATEGORIES.map((cat) => (
                  <TouchableOpacity
                    key={cat}
                    style={[styles.optionChip, selectedCategory === cat && styles.optionChipActive]}
                    onPress={() => setSelectedCategory(cat)}
                  >
                    <Text style={[styles.optionChipText, selectedCategory === cat && styles.optionChipTextActive]}>{cat}</Text>
                  </TouchableOpacity>
                ))}
              </View>

              {/* SORT */}
              <Text style={styles.filterLabel}>Sort By</Text>
              {SORT_OPTIONS.map((opt) => (
                <TouchableOpacity key={opt.value} style={styles.radioRow} onPress={() => setSelectedSort(opt.value)}>
                  <View style={[styles.radio, selectedSort === opt.value && styles.radioActive]}>
                    {selectedSort === opt.value && <View style={styles.radioDot} />}
                  </View>
                  <Text style={styles.radioLabel}>{opt.label}</Text>
                </TouchableOpacity>
              ))}

              {/* PRICE RANGE */}
              <Text style={styles.filterLabel}>Price Range</Text>
              {PRICE_RANGES.map((range, idx) => (
                <TouchableOpacity key={idx} style={styles.radioRow} onPress={() => setSelectedPrice(idx)}>
                  <View style={[styles.radio, selectedPrice === idx && styles.radioActive]}>
                    {selectedPrice === idx && <View style={styles.radioDot} />}
                  </View>
                  <Text style={styles.radioLabel}>{range.label}</Text>
                </TouchableOpacity>
              ))}
            </ScrollView>

            <TouchableOpacity style={styles.applyBtn} onPress={applyFilters}>
              <Text style={styles.applyBtnText}>Apply Filters</Text>
            </TouchableOpacity>
          </Animated.View>
        </Modal>
      )}
    </View>
  );
}

const styles = StyleSheet.create({
  container: { flex: 1, backgroundColor: "#faf9f7" },

  // HEADER
  header: { flexDirection: "row", justifyContent: "space-between", marginTop: 54, paddingHorizontal: 18, alignItems: "center" },
  leftSection: { flexDirection: "row", alignItems: "center", flex: 1 },
  titleBox: { marginLeft: 10, flex: 1 },
  iconCircle: { padding: 8, borderRadius: 22, backgroundColor: "#f0ece8", marginRight: 4, shadowColor: "#000", shadowOpacity: 0.06, shadowOffset: { width: 0, height: 2 }, shadowRadius: 4, elevation: 2 },
  cartBadge: { position: "absolute", top: -4, right: -4, backgroundColor: "#ff5722", borderRadius: 10, minWidth: 18, height: 18, justifyContent: "center", alignItems: "center", paddingHorizontal: 4 },
  cartText: { color: "#fff", fontSize: 9, fontFamily: "PoppinsBold" },
  title: { fontSize: 16, fontFamily: "PoppinsSemiBold", color: "#1a1a2e", letterSpacing: 0.2 },
  open: { fontSize: 11, color: "#4caf50", fontFamily: "PoppinsRegular", marginTop: 1 },



  // SEARCH
  searchContainer: { flexDirection: "row", marginHorizontal: 18, marginTop: 18, marginBottom: 4 },
  searchBox: { flex: 1, flexDirection: "row", alignItems: "center", backgroundColor: "#f0ece8", borderRadius: 30, paddingHorizontal: 16, paddingVertical: 2 },
  input: { marginLeft: 8, flex: 1, fontFamily: "PoppinsRegular", fontSize: 13.5, color: "#1a1a2e", paddingVertical: 10 },
  filterBtn: { marginLeft: 10, backgroundColor: "#fff0eb", padding: 12, borderRadius: 18, elevation: 2 },
  filterBtnActive: { backgroundColor: "#ff5722" },
  filterBadge: { position: "absolute", top: -4, right: -4, backgroundColor: "#1a1a2e", borderRadius: 9, width: 18, height: 18, justifyContent: "center", alignItems: "center" },
  filterBadgeText: { color: "#fff", fontSize: 9, fontFamily: "PoppinsBold" },

  // CHIPS
  chipRow: { paddingHorizontal: 16, marginBottom: 8, marginTop: 6 },
  chip: { flexDirection: "row", alignItems: "center", backgroundColor: "#fff0eb", borderWidth: 1, borderColor: "#ff5722", borderRadius: 20, paddingHorizontal: 12, paddingVertical: 5, marginRight: 8 },
  chipText: { fontFamily: "PoppinsMedium", fontSize: 12, color: "#ff5722" },
  chipClear: { backgroundColor: "#eee", borderRadius: 20, paddingHorizontal: 12, paddingVertical: 5, marginRight: 8 },
  chipClearText: { fontFamily: "PoppinsMedium", fontSize: 12, color: "#666" },

  // RESULT HEADER
  resultHeader: { flexDirection: "row", justifyContent: "space-between", alignItems: "center", marginTop: 8, marginBottom: 8, marginHorizontal: 4 },
  resultCount: { fontFamily: "PoppinsRegular", fontSize: 12, color: "#999" },

  // BANNER
  bannerWrapper: { marginHorizontal: 16, marginTop: 14, borderRadius: 24, overflow: "hidden", elevation: 6 },
  bannerSlide: { width: BANNER_WIDTH, height: 170, borderRadius: 24, overflow: "hidden" },
  bannerImage: { width: "100%", height: "100%", resizeMode: "cover" },
  bannerOverlay: { position: "absolute", bottom: 0, left: 0, right: 0, backgroundColor: "rgba(0,0,0,0.42)", paddingVertical: 14, paddingHorizontal: 18 },
  bannerTitle: { color: "#fff", fontSize: 16, fontFamily: "PoppinsSemiBold", letterSpacing: 0.3 },
  bannerSubtitle: { color: "rgba(255,255,255,0.8)", fontSize: 12, fontFamily: "PoppinsRegular", marginTop: 2 },
  dotsRow: { flexDirection: "row", justifyContent: "center", marginTop: 10, marginBottom: 2 },
  dot: { width: 7, height: 7, borderRadius: 4, backgroundColor: "#d9d3ce", marginHorizontal: 3 },
  dotActive: { backgroundColor: "#ff5722", width: 20, borderRadius: 4 },

  // CATEGORIES
  catRow: { marginTop: 20, },
  categoryCard: { backgroundColor: "#fff", paddingVertical: 12, paddingHorizontal: 10, borderRadius: 22, marginRight: 12, alignItems: "center", width: 82, elevation: 3 },
  categoryImg: { width: 60, height: 52, borderRadius: 16, marginBottom: 7 },
  categoryText: { fontSize: 11.5, fontFamily: "PoppinsMedium", color: "#1a1a2e", textAlign: "center" },

  // SECTION
  rowTitle: { marginHorizontal: 18, marginTop: 24, marginBottom: 12 },
  sectionTitle: { fontSize: 18, fontFamily: "PoppinsSemiBold", color: "#1a1a2e" },
  sectionSub: { fontSize: 12, fontFamily: "PoppinsRegular", color: "#999", marginTop: 2 },

  // BEST SELLER CARD
  card: { backgroundColor: "#fff", borderRadius: 22, width: 190, padding: 12, elevation: 4, marginBottom: 4, marginRight: 12 },
  cardImg: { width: "100%", height: 118, borderRadius: 16 },
  cardTitle: { fontSize: 13.5, fontFamily: "PoppinsSemiBold", color: "#1a1a2e", marginTop: 8 },
  cardBottom: { flexDirection: "row", justifyContent: "space-between", alignItems: "center", marginTop: 8 },
  cardPrice: { fontSize: 14, fontFamily: "PoppinsBold", color: "#ff5722" },
  addBtnCompact: { backgroundColor: "#fff0eb", borderWidth: 1.5, borderColor: "#ff5722", borderRadius: 10, paddingHorizontal: 10, paddingVertical: 5 },
  addBtnCompactText: { color: "#ff5722", fontFamily: "PoppinsSemiBold", fontSize: 12 },
  qtyCompactBox: { flexDirection: "row", backgroundColor: "#ff5722", borderRadius: 10, alignItems: "center", paddingHorizontal: 4, paddingVertical: 4 },

  // FOOD CARD
  vegBadgeRow: { flexDirection: "row", alignItems: "center", marginBottom: 4 },
  vegDot: { width: 14, height: 14, borderRadius: 3, borderWidth: 1.5, justifyContent: "center", alignItems: "center", marginRight: 5 },
  vegDotInner: { width: 7, height: 7, borderRadius: 2 },
  foodCategory: { fontFamily: "PoppinsRegular", fontSize: 10.5, color: "#999" },
  foodCard: { flexDirection: "row", padding: 14, backgroundColor: "#fff", marginVertical: 7, borderRadius: 18, alignItems: "stretch", elevation: 3 },
  foodTitle: { fontSize: 15, fontFamily: "PoppinsSemiBold", color: "#1a1a2e" },
  foodPrice: { fontSize: 14.5, fontFamily: "PoppinsBold", color: "#ff5722", marginTop: 3 },
  foodDesc: { fontSize: 12, fontFamily: "PoppinsRegular", color: "#888", marginTop: 5, lineHeight: 17, marginRight: 4, textAlign: "justify", flexShrink: 1 },
  iconRow: { flexDirection: "row", marginTop: 10, alignItems: "center" },
  iconBtn: { width: 34, height: 34, borderRadius: 17, borderWidth: 1, borderColor: "#ede8e4", justifyContent: "center", alignItems: "center", marginRight: 8, backgroundColor: "#faf9f7" },
  imageBox: { width: 120, justifyContent: "center", alignItems: "center" },
  foodImage: { width: 110, height: 115, borderRadius: 18 },
  actionBox: { marginLeft: 6, width: 90, height: 36, justifyContent: "center", alignItems: "center", flexShrink: 1 },
  addInlineBtn: { width: 90, height: 36, backgroundColor: "#fff", borderWidth: 1.5, borderColor: "#ff5722", borderRadius: 12, justifyContent: "center", alignItems: "center" },
  addInlineText: { color: "#ff5722", fontFamily: "PoppinsSemiBold", fontSize: 12.5, letterSpacing: 0.5 },
  qtyInlineBox: { width: 90, height: 36, flexDirection: "row", backgroundColor: "#ff5722", borderRadius: 12, justifyContent: "space-around", alignItems: "center" },
  qtyBtnText: { color: "#fff", fontSize: 17, fontFamily: "PoppinsMedium", marginHorizontal: 6 },
  qtyText: { color: "#fff", fontFamily: "PoppinsBold", fontSize: 14 },

  // EMPTY
  emptyBox: { alignItems: "center", marginTop: 50 },
  emptyEmoji: { fontSize: 48 },
  emptyTitle: { fontFamily: "PoppinsSemiBold", fontSize: 16, color: "#1a1a2e", marginTop: 12 },
  emptySubtitle: { fontFamily: "PoppinsRegular", fontSize: 13, color: "#999", marginTop: 5 },

  // FOOTER
  footer: { position: "absolute", bottom: 0, left: 0, right: 0, height: 65, backgroundColor: "#1a1a2e", flexDirection: "row", justifyContent: "space-around", alignItems: "center", paddingBottom: 4, elevation: 16 },
  footerBtn: { alignItems: "center", justifyContent: "center", paddingHorizontal: 14 },
  footerLabel: { color: "rgba(255,255,255,0.7)", fontFamily: "PoppinsRegular", fontSize: 10, marginTop: 3 },

  // CART BAR
  cartBar: { position: "absolute", bottom: 72, left: 14, right: 14, height: 56, backgroundColor: "#1a1a2e", flexDirection: "row", justifyContent: "space-between", alignItems: "center", paddingHorizontal: 16, borderRadius: 32, elevation: 10 },
  cartBarLeft: { flexDirection: "row", alignItems: "center", gap: 10 },
  cartCountBadge: { width: 26, height: 26, borderRadius: 13, backgroundColor: "#ff5722", justifyContent: "center", alignItems: "center" },
  cartCountText: { color: "#fff", fontFamily: "PoppinsBold", fontSize: 11 },
  cartBarText: { color: "rgba(255,255,255,0.85)", fontFamily: "PoppinsMedium", fontSize: 13 },
  checkoutBtn: { flexDirection: "row", alignItems: "center", backgroundColor: "#fff", paddingHorizontal: 16, paddingVertical: 8, borderRadius: 20, gap: 5 },
  checkoutText: { color: "#ff5722", fontFamily: "PoppinsSemiBold", fontSize: 13 },

  // FILTER SHEET
  modalBackdrop: { position: "absolute", top: 0, left: 0, right: 0, bottom: 0, backgroundColor: "rgba(0,0,0,0.45)" },
  filterSheet: { position: "absolute", bottom: 0, left: 0, right: 0, backgroundColor: "#fff", borderTopLeftRadius: 28, borderTopRightRadius: 28, paddingHorizontal: 22, paddingBottom: 36, maxHeight: height * 0.78 },
  sheetHandle: { width: 40, height: 4, backgroundColor: "#e0dbd6", borderRadius: 2, alignSelf: "center", marginTop: 12, marginBottom: 4 },
  sheetHeader: { flexDirection: "row", justifyContent: "space-between", alignItems: "center", marginVertical: 14 },
  sheetTitle: { fontFamily: "PoppinsSemiBold", fontSize: 18, color: "#1a1a2e" },
  resetText: { fontFamily: "PoppinsMedium", fontSize: 13, color: "#ff5722" },
  filterLabel: { fontFamily: "PoppinsSemiBold", fontSize: 14, color: "#1a1a2e", marginTop: 18, marginBottom: 10 },
  optionsWrap: { flexDirection: "row", flexWrap: "wrap", gap: 8 },
  optionChip: { paddingHorizontal: 14, paddingVertical: 7, borderRadius: 20, borderWidth: 1.5, borderColor: "#e0dbd6", backgroundColor: "#faf9f7" },
  optionChipActive: { backgroundColor: "#ff5722", borderColor: "#ff5722" },
  optionChipText: { fontFamily: "PoppinsMedium", fontSize: 13, color: "#555" },
  optionChipTextActive: { color: "#fff" },
  radioRow: { flexDirection: "row", alignItems: "center", paddingVertical: 10, borderBottomWidth: 0.5, borderColor: "#f0ece8" },
  radio: { width: 20, height: 20, borderRadius: 10, borderWidth: 2, borderColor: "#ccc", justifyContent: "center", alignItems: "center", marginRight: 12 },
  radioActive: { borderColor: "#ff5722" },
  radioDot: { width: 10, height: 10, borderRadius: 5, backgroundColor: "#ff5722" },
  radioLabel: { fontFamily: "PoppinsRegular", fontSize: 14, color: "#1a1a2e" },
  applyBtn: { backgroundColor: "#ff5722", borderRadius: 18, paddingVertical: 15, alignItems: "center", marginTop: 20 },
  applyBtnText: { color: "#fff", fontFamily: "PoppinsSemiBold", fontSize: 15, letterSpacing: 0.4 },
});
</file>

<file path="index.js">
import { registerRootComponent } from 'expo';

import App from './App';

// registerRootComponent calls AppRegistry.registerComponent('main', () => App);
// It also ensures that whether you load the app in Expo Go or in a native build,
// the environment is set up appropriately
registerRootComponent(App);
</file>

<file path="Login.js">
import React, { useState } from "react";
import {
  View, Text, Image, StyleSheet, TextInput,
  TouchableOpacity, ScrollView, KeyboardAvoidingView,
  Platform, ActivityIndicator, Alert,
} from "react-native";
import { Ionicons } from "@expo/vector-icons";
import logotp from "./assets/logotp.png";
import { login } from "./api";
export default function Login({ navigation }) {
  const [passwordVisible, setPasswordVisible] = useState(false);
  const [mobile, setMobile] = useState("");
  const [password, setPassword] = useState("");
  const [loading, setLoading] = useState(false);
  const handleLogin = async () => {
    if (!mobile || !password) {
      Alert.alert("Error", "Please enter all details");
      return;
    }
    if (!/^[0-9]{10}$/.test(mobile)) {
      Alert.alert("Error", "Enter valid 10-digit mobile number");
      return;
    }
    if (password.length < 6) {
      Alert.alert("Error", "Password must be at least 6 characters");
      return;
    }
    setLoading(true);
    try {
      const response = await login(mobile, password);
      if (response.success) {
        navigation.navigate("Success");
      } else {
        Alert.alert("Login Failed", response.message || "Invalid credentials");
      }
    } catch (error) {
      Alert.alert("Error", "Could not connect to server. Check your network.");
    } finally {
      setLoading(false);
    }
  };
  return (
    <KeyboardAvoidingView
      style={{ flex: 1 }}
      behavior={Platform.OS === "ios" ? "padding" : "height"}
    >
      <ScrollView
        contentContainerStyle={{ flexGrow: 1, paddingBottom: 30 }}
        keyboardShouldPersistTaps="handled"
        showsVerticalScrollIndicator={false}
      >
        <View style={styles.container}>
          <View style={styles.content}>
            <View style={styles.imageContainer}>
              <Image source={logotp} style={styles.image} />
            </View>
            <View style={styles.tabs}>
              <View style={styles.activeTabWrap}>
                <Text style={styles.activeTab}>Login</Text>
              </View>
              <TouchableOpacity onPress={() => navigation.navigate("Signup")}>
                <Text style={styles.inactiveTab}>Sign up</Text>
              </TouchableOpacity>
            </View>
            <View style={styles.inputWrapper}>
              <Ionicons name="call-outline" size={18} color="#aaa" style={styles.inputIcon} />
              <TextInput
                placeholder="Mobile Number"
                placeholderTextColor="#bbb"
                style={styles.input}
                value={mobile}
                onChangeText={(text) => {
                  const filtered = text.replace(/[^0-9]/g, "");
                  if (filtered.length <= 10) setMobile(filtered);
                }}
                keyboardType="numeric"
                maxLength={10}
              />
            </View>
            <View style={styles.inputWrapper}>
              <Ionicons name="lock-closed-outline" size={18} color="#aaa" style={styles.inputIcon} />
              <TextInput
                placeholder="Password"
                placeholderTextColor="#bbb"
                style={[styles.input, { flex: 1 }]}
                secureTextEntry={!passwordVisible}
                value={password}
                onChangeText={setPassword}
              />
              <TouchableOpacity onPress={() => setPasswordVisible(!passwordVisible)} style={{ paddingRight: 4 }}>
                <Ionicons name={passwordVisible ? "eye" : "eye-off"} size={18} color="#aaa" />
              </TouchableOpacity>
            </View>
            <TouchableOpacity onPress={() => navigation.navigate("Otpscreen")} style={{ alignSelf: "flex-end" }}>
              <Text style={styles.forgot}>Forgot password?</Text>
            </TouchableOpacity>
            <TouchableOpacity style={styles.loginBtn} onPress={handleLogin} disabled={loading}>
              {loading ? (
                <ActivityIndicator color="#fff" />
              ) : (
                <>
                  <Text style={styles.loginText}>Login</Text>
                  <Ionicons name="arrow-forward" size={16} color="#fff" style={{ marginLeft: 8 }} />
                </>
              )}
            </TouchableOpacity>
            <Text style={styles.signupLink}>
              Don't have an account?{" "}
              <Text style={styles.signupLinkAccent} onPress={() => navigation.navigate("Signup")}>
                Sign up
              </Text>
            </Text>
          </View>
        </View>
      </ScrollView>
    </KeyboardAvoidingView>
  );
}
const styles = StyleSheet.create({
  container: { flex: 1, backgroundColor: "#faf9f7", padding: 24 },
  content: { flex: 1, paddingTop: 30 },
  imageContainer: { alignItems: "center", marginBottom: 8 },
  image: { width: 220, height: 160, resizeMode: "contain" },
  tabs: { flexDirection: "row", justifyContent: "center", alignItems: "center", gap: 32, marginBottom: 28 },
  activeTabWrap: { borderBottomWidth: 2, borderColor: "#ff5722", paddingBottom: 4 },
  activeTab: { fontFamily: "PoppinsSemiBold", fontSize: 16, color: "#ff5722" },
  inactiveTab: { fontFamily: "PoppinsMedium", fontSize: 16, color: "#bbb", paddingBottom: 4 },
  inputWrapper: {
    flexDirection: "row", alignItems: "center", backgroundColor: "#fff",
    borderRadius: 14, paddingHorizontal: 14, paddingVertical: 4,
    marginBottom: 14, borderWidth: 1, borderColor: "#ede8e4", elevation: 1,
  },
  inputIcon: { marginRight: 10 },
  input: { flex: 1, fontFamily: "PoppinsRegular", fontSize: 14, color: "#1a1a2e", paddingVertical: 13 },
  forgot: { fontFamily: "PoppinsMedium", color: "#ff5722", fontSize: 12.5, marginBottom: 22, marginTop: 2 },
  loginBtn: {
    backgroundColor: "#ff5722", paddingVertical: 16, borderRadius: 18,
    alignItems: "center", flexDirection: "row", justifyContent: "center",
    elevation: 4, marginBottom: 20, minHeight: 54,
  },
  loginText: { color: "#fff", fontFamily: "PoppinsSemiBold", fontSize: 15, letterSpacing: 0.4 },
  signupLink: { fontFamily: "PoppinsRegular", fontSize: 13, color: "#888", textAlign: "center", marginTop: 4 },
  signupLinkAccent: { fontFamily: "PoppinsSemiBold", color: "#ff5722" },
});
</file>

<file path="Meals.js">
import React, { useState } from "react";
import {
  View,
  Text,
  StyleSheet,
  TextInput,
  TouchableOpacity,
  FlatList,
  Image,
  Share,
} from "react-native";
import { Ionicons } from "@expo/vector-icons";
import { allFoodItems } from "./Data";
import { useContext } from "react";
import { CartContext } from "./CartContext";
import { WishlistContext } from "./WishlistContext";

export default function MealsScreen({ navigation }) {
  const { cart, addToCart, removeFromCart } = useContext(CartContext);
  const { toggleWishlist, isWishlisted } = useContext(WishlistContext);
  const [searchText, setSearchText] = useState("");

  const totalItems = cart.reduce((sum, i) => sum + i.quantity, 0);

  const baseItems = allFoodItems.filter((item) => item.category === "Meals");
  const filteredItems = baseItems.filter((item) =>
    item.name.toLowerCase().includes(searchText.toLowerCase())
  );

  const shareItem = async (item) => {
    try {
      await Share.share({
        message: `${item.name} - Rs.${item.price}\nTry this delicious food!`,
      });
    } catch (error) {
      console.log(error);
    }
  };

  const renderItem = ({ item }) => {
    const cartItem = cart.find((i) => i.id === item.id);
    const wishlisted = isWishlisted(item.id);

    return (
      <View style={styles.foodCard}>
        {/* LEFT */}
        <View style={{ flex: 1, paddingRight: 10 }}>
          <Text style={styles.foodTitle}>{item.name}</Text>
          <Text style={styles.foodPrice}>Rs.{item.price}</Text>
          <Text style={styles.foodDesc}>{item.description}</Text>

          <View style={styles.iconRow}>
            {/* WISHLIST */}
            <TouchableOpacity style={styles.iconBtn} onPress={() => toggleWishlist(item)}>
              <Ionicons
                name={wishlisted ? "heart" : "heart-outline"}
                size={17}
                color="#ff5722"
              />
            </TouchableOpacity>

            {/* SHARE */}
            <TouchableOpacity style={styles.iconBtn} onPress={() => shareItem(item)}>
              <Ionicons name="share-social-outline" size={17} color="#444" />
            </TouchableOpacity>

            {/* ADD / QTY */}
            <View style={styles.actionBox}>
              {!cartItem ? (
                <TouchableOpacity style={styles.addInlineBtn} onPress={() => addToCart(item)}>
                  <Text style={styles.addInlineText}>ADD +</Text>
                </TouchableOpacity>
              ) : (
                <View style={styles.qtyInlineBox}>
                  <TouchableOpacity onPress={() => removeFromCart(item.id)}>
                    <Text style={styles.qtyBtnText}>-</Text>
                  </TouchableOpacity>
                  <Text style={styles.qtyText}>{cartItem.quantity}</Text>
                  <TouchableOpacity onPress={() => addToCart(item)}>
                    <Text style={styles.qtyBtnText}>+</Text>
                  </TouchableOpacity>
                </View>
              )}
            </View>
          </View>
        </View>

        {/* RIGHT */}
        <View style={styles.imageBox}>
          <Image source={item.image} style={styles.foodImage} />
        </View>
      </View>
    );
  };

  return (
    <View style={styles.container}>

      {/* HEADER */}
      <View style={styles.header}>
        <TouchableOpacity style={styles.iconCircle} onPress={() => navigation.goBack()}>
          <Ionicons name="arrow-back" size={22} color="#1a1a2e" />
        </TouchableOpacity>
        <Text style={styles.title}>Meals Menu</Text>
        <TouchableOpacity style={styles.iconCircle} onPress={() => navigation.navigate("Cart")}>
          <Ionicons name="cart-outline" size={20} color="#1a1a2e" />
          {cart.length > 0 && (
            <View style={styles.cartBadge}>
              <Text style={styles.cartText}>{totalItems}</Text>
            </View>
          )}
        </TouchableOpacity>
      </View>

      {/* SEARCH */}
      <View style={styles.searchContainer}>
        <Ionicons name="search" size={18} color="#aaa" />
        <TextInput
          placeholder="Search meals..."
          placeholderTextColor="#bbb"
          value={searchText}
          onChangeText={setSearchText}
          style={styles.input}
        />
        {searchText.length > 0 && (
          <TouchableOpacity onPress={() => setSearchText("")}>
            <Ionicons name="close-circle" size={18} color="#bbb" />
          </TouchableOpacity>
        )}
      </View>

      {/* COUNT */}
      <View style={styles.countRow}>
        <Text style={styles.countText}>{filteredItems.length} items</Text>
      </View>

      {/* LIST */}
      <FlatList
        data={filteredItems}
        keyExtractor={(item) => item.id.toString()}
        renderItem={renderItem}
        showsVerticalScrollIndicator={false}
        contentContainerStyle={{ paddingBottom: totalItems > 0 ? 130 : 30, paddingTop: 4 }}
        ListEmptyComponent={
          <View style={styles.emptyBox}>
            <Text style={styles.emptyEmoji}>🍽</Text>
            <Text style={styles.emptyTitle}>Nothing found</Text>
            <Text style={styles.emptySubtitle}>Try a different search</Text>
          </View>
        }
      />

      {/* CART BAR */}
      {totalItems > 0 && (
        <View style={styles.cartBar}>
          <View style={styles.cartBarLeft}>
            <View style={styles.cartCountBadge}>
              <Text style={styles.cartCountText}>{totalItems}</Text>
            </View>
            <Text style={styles.cartBarText}>
              {totalItems} {totalItems === 1 ? "item" : "items"} added
            </Text>
          </View>
          <TouchableOpacity style={styles.checkoutBtn} onPress={() => navigation.navigate("Cart")}>
            <Text style={styles.checkoutText}>Checkout</Text>
            <Ionicons name="arrow-forward" size={14} color="#ff5722" />
          </TouchableOpacity>
        </View>
      )}
    </View>
  );
}

const styles = StyleSheet.create({
  container: { flex: 1, backgroundColor: "#faf9f7" },

  // HEADER
  header: {
    flexDirection: "row",
    justifyContent: "space-between",
    alignItems: "center",
    marginTop: 50,
    paddingHorizontal: 16,
    paddingVertical: 14,
    backgroundColor: "#fff",
    elevation: 3,
    shadowColor: "#000",
    shadowOpacity: 0.07,
    shadowOffset: { width: 0, height: 2 },
    shadowRadius: 6,
  },
  title: {
    fontSize: 17,
    fontFamily: "PoppinsSemiBold",
    color: "#1a1a2e",
    letterSpacing: 0.2,
  },
  iconCircle: {
    padding: 8,
    borderRadius: 22,
    backgroundColor: "#f0ece8",
    shadowColor: "#000",
    shadowOpacity: 0.05,
    shadowOffset: { width: 0, height: 1 },
    shadowRadius: 3,
    elevation: 1,
  },
  cartBadge: {
    position: "absolute",
    top: -4,
    right: -4,
    backgroundColor: "#ff5722",
    borderRadius: 10,
    minWidth: 18,
    height: 18,
    justifyContent: "center",
    alignItems: "center",
    paddingHorizontal: 4,
  },
  cartText: { color: "#fff", fontSize: 9, fontFamily: "PoppinsBold" },

  // SEARCH
  searchContainer: {
    flexDirection: "row",
    alignItems: "center",
    backgroundColor: "#f0ece8",
    marginHorizontal: 16,
    marginTop: 14,
    marginBottom: 4,
    paddingHorizontal: 16,
    paddingVertical: 2,
    borderRadius: 30,
  },
  input: {
    marginLeft: 8,
    flex: 1,
    fontFamily: "PoppinsRegular",
    fontSize: 13.5,
    color: "#1a1a2e",
    paddingVertical: 10,
  },

  // COUNT
  countRow: {
    paddingHorizontal: 20,
    paddingTop: 10,
    paddingBottom: 2,
  },
  countText: {
    fontFamily: "PoppinsRegular",
    fontSize: 12,
    color: "#999",
  },

  // FOOD CARD
  foodCard: {
    flexDirection: "row",
    padding: 14,
    backgroundColor: "#fff",
    marginHorizontal: 14,
    marginVertical: 7,
    borderRadius: 18,
    alignItems: "stretch",
    elevation: 3,
    shadowColor: "#000",
    shadowOpacity: 0.07,
    shadowOffset: { width: 0, height: 3 },
    shadowRadius: 8,
  },
  foodTitle: {
    fontSize: 15,
    fontFamily: "PoppinsSemiBold",
    color: "#1a1a2e",
    letterSpacing: 0.1,
  },
  foodPrice: {
    fontSize: 14.5,
    fontFamily: "PoppinsBold",
    color: "#ff5722",
    marginTop: 3,
  },
  foodDesc: {
    fontSize: 12,
    fontFamily: "PoppinsRegular",
    color: "#888",
    marginTop: 5,
    lineHeight: 17,
    marginRight: 4,
    textAlign: "justify",
    flexShrink: 1,
  },
  iconRow: {
    flexDirection: "row",
    marginTop: 10,
    alignItems: "center",
    flexWrap: "nowrap",
  },
  iconBtn: {
    width: 34,
    height: 34,
    borderRadius: 17,
    borderWidth: 1,
    borderColor: "#ede8e4",
    justifyContent: "center",
    alignItems: "center",
    marginRight: 8,
    backgroundColor: "#faf9f7",
  },
  imageBox: {
    width: 120,
    justifyContent: "center",
    alignItems: "center",
  },
  foodImage: {
    width: 110,
    height: 115,
    borderRadius: 18,
  },
  actionBox: {
    marginLeft: 6,
    width: 90,
    height: 36,
    justifyContent: "center",
    alignItems: "center",
    flexShrink: 1,
  },
  addInlineBtn: {
    width: 90,
    height: 36,
    backgroundColor: "#fff",
    borderWidth: 1.5,
    borderColor: "#ff5722",
    borderRadius: 12,
    justifyContent: "center",
    alignItems: "center",
  },
  addInlineText: {
    color: "#ff5722",
    fontFamily: "PoppinsSemiBold",
    fontSize: 12.5,
    letterSpacing: 0.5,
  },
  qtyInlineBox: {
    width: 90,
    height: 36,
    flexDirection: "row",
    backgroundColor: "#ff5722",
    borderRadius: 12,
    justifyContent: "space-around",
    alignItems: "center",
  },
  qtyBtnText: {
    color: "#fff",
    fontSize: 17,
    fontFamily: "PoppinsMedium",
    marginHorizontal: 6,
  },
  qtyText: {
    color: "#fff",
    fontFamily: "PoppinsBold",
    fontSize: 14,
  },

  // EMPTY STATE
  emptyBox: { alignItems: "center", marginTop: 60 },
  emptyEmoji: { fontSize: 48 },
  emptyTitle: { fontFamily: "PoppinsSemiBold", fontSize: 16, color: "#1a1a2e", marginTop: 12 },
  emptySubtitle: { fontFamily: "PoppinsRegular", fontSize: 13, color: "#999", marginTop: 5 },

  // CART BAR
  cartBar: {
    position: "absolute",
    bottom: 16,
    left: 14,
    right: 14,
    height: 56,
    backgroundColor: "#1a1a2e",
    flexDirection: "row",
    justifyContent: "space-between",
    alignItems: "center",
    paddingHorizontal: 16,
    borderRadius: 32,
    elevation: 10,
    shadowColor: "#000",
    shadowOpacity: 0.22,
    shadowOffset: { width: 0, height: 5 },
    shadowRadius: 10,
  },
  cartBarLeft: { flexDirection: "row", alignItems: "center", gap: 10 },
  cartCountBadge: {
    width: 26,
    height: 26,
    borderRadius: 13,
    backgroundColor: "#ff5722",
    justifyContent: "center",
    alignItems: "center",
  },
  cartCountText: { color: "#fff", fontFamily: "PoppinsBold", fontSize: 11 },
  cartBarText: { color: "rgba(255,255,255,0.85)", fontFamily: "PoppinsMedium", fontSize: 13 },
  checkoutBtn: {
    flexDirection: "row",
    alignItems: "center",
    backgroundColor: "#fff",
    paddingHorizontal: 16,
    paddingVertical: 8,
    borderRadius: 20,
    gap: 5,
  },
  checkoutText: { color: "#ff5722", fontFamily: "PoppinsSemiBold", fontSize: 13 },
});
</file>

<file path="Menu.js">
import { View, Text, StyleSheet, Image, Pressable } from "react-native";
import chicken from "./assets/chicken.jpg";
import mutton from "./assets/mutton.jpg";
import FoodCard from "../components/FoodCard";

<FlatList
  data={biryaniItems}
  renderItem={({ item }) => (
    <FoodCard item={item} addToCart={addToCart} />
  )}
/>

export default function Menu({ navigation }) {
  return (
    <View style={styles.container}>

      <Text style={styles.title}>Our Menu</Text>

      <View style={styles.row}>
        <View style={styles.card}>
          <Image source={chicken} style={styles.img}/>
          <Text>Chicken Curry</Text>
        </View>

        <View style={styles.card}>
          <Image source={mutton} style={styles.img}/>
          <Text>Mutton Curry</Text>
        </View>
      </View>

       <Pressable
        style={styles.button}
        onPress={() => navigation.navigate("Home")}
      >
        <Text style={{color:"#fff"}}>Home</Text>
      </Pressable>

    </View>
  );
}

const styles = StyleSheet.create({
  container:{
    flex:1,
    alignItems:"center",
    paddingTop:50
  },

  title:{
    fontSize:20,
    marginBottom:20
  },

  row:{
    flexDirection:"row"
  },

  card:{
    alignItems:"center",
    margin:20
  },

  img:{
    width:100,
    height:100
  },

  button:{
    backgroundColor:"#ff5a3c",
    padding:10,
    borderRadius:5,
    marginTop:20
  }
});
</file>

<file path="Onboarding.js">
import React, { useRef, useState } from "react";
import {
  View,
  Text,
  Image,
  StyleSheet,
  ScrollView,
  Dimensions,
  TouchableOpacity,
  Pressable,
  Animated,
  StatusBar,
} from "react-native";

import { LinearGradient } from "expo-linear-gradient";

import Banner from "./assets/home.png";
import Food from "./assets/food.png";
import Foood from "./assets/foood.png";
import Biryani from "./assets/biryani.png";

const { width, height } = Dimensions.get("window");

const slides = [
  {
    id: 0,
    fullImage: true,
    source: Banner,
  },
  {
    id: 1,
    title: "Taste the\nTradition",
    subtitle: "Delicious food, crafted with love and\ndelivered fresh to your home.",
    image: Food,
    tag: "FRESH & HOT",
  },
  {
    id: 2,
    title: "Explore Our\nKitchen",
    subtitle: "From biryanis to desserts — a world of\nauthentic flavours awaits you.",
    image: Biryani,
    tag: "30+ DISHES",
  },
  {
    id: 3,
    title: "Order in\nSeconds",
    subtitle: "Fast service, authentic taste — your\nfavourite meal at your doorstep.",
    image: Foood,
    tag: "QUICK SERVICE",
    isLast: true,
  },
];

export default function Onboarding({ navigation }) {
  const scrollRef = useRef(null);
  const [screen, setScreen] = useState(0);

  const goToLast = () => {
    scrollRef.current?.scrollTo({ x: width * 3, animated: true });
    setScreen(3);
  };

  const goNext = () => {
    const next = Math.min(screen + 1, 3);
    scrollRef.current?.scrollTo({ x: width * next, animated: true });
    setScreen(next);
  };

  return (
    <View style={styles.root}>
      <StatusBar barStyle="light-content" translucent backgroundColor="transparent" />

      <ScrollView
        ref={scrollRef}
        horizontal
        pagingEnabled
        scrollEnabled
        showsHorizontalScrollIndicator={false}
        onMomentumScrollEnd={(e) => {
          const index = Math.round(e.nativeEvent.contentOffset.x / width);
          setScreen(index);
        }}
      >
        {slides.map((slide) => {
          if (slide.fullImage) {
            return (
              <View key={slide.id} style={{ width, height }}>
                <Image source={slide.source} style={styles.fullImage} />
                {/* Gradient overlay for full image slide */}
                <LinearGradient
                  colors={["transparent", "rgba(26,26,46,0.7)"]}
                  style={styles.fullOverlay}
                />
                <View style={styles.fullTextBox}>
                  <Text style={styles.brandName}>Annapurna</Text>
                  <Text style={styles.brandTagline}>Restaurant</Text>
                </View>
              </View>
            );
          }

          return (
            <View key={slide.id} style={{ width, height }}>
              {/* Rich dark background */}
              <LinearGradient
                colors={["#1a1a2e", "#16213e"]}
                style={StyleSheet.absoluteFillObject}
              />

              {/* Skip button */}
              {!slide.isLast && (
                <TouchableOpacity style={styles.skipBtn} onPress={goToLast}>
                  <Text style={styles.skipText}>Skip</Text>
                </TouchableOpacity>
              )}

              {/* Tag pill */}
              <View style={styles.tagPill}>
                <View style={styles.tagDot} />
                <Text style={styles.tagText}>{slide.tag}</Text>
              </View>

              {/* Image */}
              <View style={styles.imageWrapper}>
                <Image source={slide.image} style={styles.slideImage} />
              </View>

              {/* Text content */}
              <View style={styles.textBox}>
                <Text style={styles.slideTitle}>{slide.title}</Text>
                <Text style={styles.slideSubtitle}>{slide.subtitle}</Text>
              </View>

              {/* Bottom row */}
              <View style={styles.bottomRow}>
                {/* Dots */}
                <View style={styles.dotsRow}>
                  {[1, 2, 3].map((i) => (
                    <View
                      key={i}
                      style={[styles.dot, screen === i && styles.dotActive]}
                    />
                  ))}
                </View>

                {/* CTA */}
                {slide.isLast ? (
                  <Pressable
                    style={styles.getStartedBtn}
                    onPress={() => navigation.navigate("Login")}
                  >
                    <LinearGradient
                      colors={["#ff7043", "#ff5722"]}
                      start={{ x: 0, y: 0 }}
                      end={{ x: 1, y: 0 }}
                      style={styles.getStartedGradient}
                    >
                      <Text style={styles.getStartedText}>Get Started</Text>
                      <Text style={styles.getStartedArrow}>→</Text>
                    </LinearGradient>
                  </Pressable>
                ) : (
                  <TouchableOpacity style={styles.nextBtn} onPress={goNext}>
                    <LinearGradient
                      colors={["#ff7043", "#ff5722"]}
                      start={{ x: 0, y: 0 }}
                      end={{ x: 1, y: 0 }}
                      style={styles.nextBtnGrad}
                    >
                      <Text style={styles.nextArrow}>→</Text>
                    </LinearGradient>
                  </TouchableOpacity>
                )}
              </View>
            </View>
          );
        })}
      </ScrollView>

      {/* Dots for full image screen */}
      {screen === 0 && (
        <View style={styles.firstDots}>
          {[0, 1, 2, 3].map((i) => (
            <View key={i} style={[styles.dot, screen === i && styles.dotActive]} />
          ))}
        </View>
      )}
    </View>
  );
}

const styles = StyleSheet.create({
  root: {
    flex: 1,
    backgroundColor: "#1a1a2e",
  },

  // FULL IMAGE SLIDE
  fullImage: {
    width: "100%",
    height: "100%",
    resizeMode: "cover",
  },
  fullOverlay: {
    position: "absolute",
    bottom: 0,
    left: 0,
    right: 0,
    height: height * 0.45,
  },
  fullTextBox: {
    position: "absolute",
    bottom: height * 0.12,
    left: 28,
  },
  brandName: {
    fontFamily: "PoppinsBold",
    fontSize: 42,
    color: "#fff",
    letterSpacing: 1.5,
  },
  brandTagline: {
    fontFamily: "PoppinsRegular",
    fontSize: 22,
    color: "rgba(255,255,255,0.75)",
    letterSpacing: 6,
    marginTop: -6,
  },

  // SKIP
  skipBtn: {
    position: "absolute",
    top: 58,
    right: 24,
    zIndex: 10,
    paddingVertical: 6,
    paddingHorizontal: 16,
    borderRadius: 20,
    backgroundColor: "rgba(255,255,255,0.1)",
    borderWidth: 1,
    borderColor: "rgba(255,255,255,0.15)",
  },
  skipText: {
    fontFamily: "PoppinsMedium",
    fontSize: 13,
    color: "rgba(255,255,255,0.7)",
    letterSpacing: 0.3,
  },

  // TAG
  tagPill: {
    flexDirection: "row",
    alignItems: "center",
    position: "absolute",
    top: 64,
    left: 28,
    backgroundColor: "rgba(255,87,34,0.15)",
    borderWidth: 1,
    borderColor: "rgba(255,87,34,0.35)",
    paddingHorizontal: 12,
    paddingVertical: 6,
    borderRadius: 20,
  },
  tagDot: {
    width: 6,
    height: 6,
    borderRadius: 3,
    backgroundColor: "#ff5722",
    marginRight: 7,
  },
  tagText: {
    fontFamily: "PoppinsSemiBold",
    fontSize: 10,
    color: "#ff7043",
    letterSpacing: 1.5,
  },

  // IMAGE
  imageWrapper: {
    alignItems: "center",
    marginTop: height * 0.22,
  },
  slideImage: {
    width: 280,
    height: 240,
    resizeMode: "contain",
  },

  // TEXT
  textBox: {
    paddingHorizontal: 28,
    marginTop: 40,
  },
  slideTitle: {
    fontFamily: "PoppinsBold",
    fontSize: 36,
    color: "#fff",
    lineHeight: 44,
    letterSpacing: 0.3,
  },
  slideSubtitle: {
    fontFamily: "PoppinsRegular",
    fontSize: 14.5,
    color: "rgba(255,255,255,0.55)",
    marginTop: 12,
    lineHeight: 22,
    letterSpacing: 0.2,
  },

  // BOTTOM ROW
  bottomRow: {
    position: "absolute",
    bottom: 48,
    left: 28,
    right: 28,
    flexDirection: "row",
    justifyContent: "space-between",
    alignItems: "center",
  },
  dotsRow: {
    flexDirection: "row",
    alignItems: "center",
  },
  dot: {
    width: 7,
    height: 7,
    borderRadius: 3.5,
    backgroundColor: "rgba(255,255,255,0.2)",
    marginRight: 6,
  },
  dotActive: {
    backgroundColor: "#ff5722",
    width: 22,
    borderRadius: 4,
  },

  // NEXT BUTTON
  nextBtn: {
    borderRadius: 22,
    overflow: "hidden",
  },
  nextBtnGrad: {
    width: 52,
    height: 52,
    borderRadius: 26,
    justifyContent: "center",
    alignItems: "center",
  },
  nextArrow: {
    color: "#fff",
    fontSize: 20,
    fontFamily: "PoppinsSemiBold",
  },

  // GET STARTED BUTTON
  getStartedBtn: {
    borderRadius: 28,
    overflow: "hidden",
  },
  getStartedGradient: {
    flexDirection: "row",
    alignItems: "center",
    paddingHorizontal: 28,
    paddingVertical: 16,
    borderRadius: 28,
    gap: 10,
  },
  getStartedText: {
    fontFamily: "PoppinsSemiBold",
    fontSize: 15,
    color: "#fff",
    letterSpacing: 0.4,
  },
  getStartedArrow: {
    color: "#fff",
    fontSize: 18,
    fontFamily: "PoppinsSemiBold",
  },

  // FIRST SCREEN DOTS
  firstDots: {
    position: "absolute",
    bottom: 44,
    alignSelf: "center",
    flexDirection: "row",
  },
});
</file>

<file path="OrderHistoryScreen.js">
import React, { useState, useEffect } from "react";
import { View, Text, StyleSheet, TouchableOpacity, ScrollView, ActivityIndicator } from "react-native";
import { Ionicons } from "@expo/vector-icons";
import { getOrderHistory } from "./api";
export default function OrderHistoryScreen({ navigation }) {
  const [orders, setOrders] = useState([]);
  const [loading, setLoading] = useState(true);
  const [expanded, setExpanded] = useState(null);
  useEffect(() => {
    fetchOrders();
  }, []);
  const fetchOrders = async () => {
    try {
      const data = await getOrderHistory();
      setOrders(data);
    } catch (e) {
      console.log("Order history error:", e);
    } finally {
      setLoading(false);
    }
  };
  const toggle = (id) => setExpanded((prev) => (prev === id ? null : id));
  const getStatusColor = (status) => {
    switch (status?.toLowerCase()) {
      case "delivered": return { color: "#43a047", bg: "#e8f5e9", icon: "checkmark-circle" };
      case "cancelled": return { color: "#e53935", bg: "#ffebee", icon: "close-circle" };
      case "placed": return { color: "#1e88e5", bg: "#e3f2fd", icon: "time" };
      default: return { color: "#fb8c00", bg: "#fff3e0", icon: "hourglass" };
    }
  };
  const formatDate = (dateString) => {
    if (!dateString) return "";
    const d = new Date(dateString);
    return d.toLocaleDateString("en-IN", { day: "numeric", month: "short", year: "numeric" });
  };
  const formatTime = (dateString) => {
    if (!dateString) return "";
    const d = new Date(dateString);
    return d.toLocaleTimeString("en-IN", { hour: "2-digit", minute: "2-digit" });
  };
  if (loading) {
    return (
      <View style={[styles.container, { justifyContent: "center", alignItems: "center" }]}>
        <ActivityIndicator size="large" color="#ff5722" />
        <Text style={{ fontFamily: "PoppinsRegular", color: "#888", marginTop: 12 }}>Loading orders...</Text>
      </View>
    );
  }
  return (
    <View style={styles.container}>
      <View style={styles.header}>
        <TouchableOpacity style={styles.backBtn} onPress={() => navigation.goBack()}>
          <Ionicons name="arrow-back" size={20} color="#1a1a2e" />
        </TouchableOpacity>
        <Text style={styles.headerTitle}>Order History</Text>
        <View style={{ width: 36 }} />
      </View>
      <ScrollView showsVerticalScrollIndicator={false} contentContainerStyle={{ paddingBottom: 40 }}>
        {orders.length === 0 ? (
          <View style={{ alignItems: "center", marginTop: 80 }}>
            <Ionicons name="receipt-outline" size={60} color="#ddd" />
            <Text style={{ fontFamily: "PoppinsSemiBold", fontSize: 16, color: "#aaa", marginTop: 16 }}>
              No orders yet
            </Text>
            <Text style={{ fontFamily: "PoppinsRegular", fontSize: 13, color: "#bbb", marginTop: 8 }}>
              Your order history will appear here
            </Text>
          </View>
        ) : (
          orders.map((order) => {
            const isOpen = expanded === order.orderId;
            const sc = getStatusColor(order.status);
            return (
              <View key={order.orderId} style={styles.card}>
                <TouchableOpacity style={styles.cardTop} onPress={() => toggle(order.orderId)} activeOpacity={0.75}>
                  <View style={styles.orderMeta}>
                    <Text style={styles.orderId}>{order.orderId}</Text>
                    <View style={styles.dateRow}>
                      <Ionicons name="calendar-outline" size={12} color="#aaa" />
                      <Text style={styles.dateText}>{formatDate(order.createdAt)} · {formatTime(order.createdAt)}</Text>
                    </View>
                    {order.deliveryAddress && (
                      <View style={styles.locationRow}>
                        <Ionicons name="location-outline" size={12} color="#aaa" />
                        <Text style={styles.locationText}>{order.deliveryAddress}</Text>
                      </View>
                    )}
                  </View>
                  <View style={styles.orderRight}>
                    <View style={[styles.statusChip, { backgroundColor: sc.bg }]}>
                      <Ionicons name={sc.icon} size={13} color={sc.color} />
                      <Text style={[styles.statusText, { color: sc.color }]}>{order.status}</Text>
                    </View>
                    <Text style={styles.totalAmt}>₹{order.total?.toFixed(0)}</Text>
                    <Ionicons name={isOpen ? "chevron-up" : "chevron-down"} size={16} color="#ccc" style={{ marginTop: 8 }} />
                  </View>
                </TouchableOpacity>
                {isOpen && (
                  <View style={styles.expandedBody}>
                    <Text style={styles.itemsTitle}>Items Ordered</Text>
                    {order.items?.map((item, idx) => (
                      <View key={idx} style={styles.itemRow}>
                        <View style={styles.itemDot} />
                        <Text style={styles.itemName}>{item.name} ×{item.quantity}</Text>
                        <Text style={styles.itemPrice}>₹{item.total?.toFixed(0)}</Text>
                      </View>
                    ))}
                    <View style={styles.billBox}>
                      <View style={styles.billRow}>
                        <Text style={styles.billKey}>Subtotal</Text>
                        <Text style={styles.billVal}>₹{order.subtotal?.toFixed(0)}</Text>
                      </View>
                      <View style={styles.billRow}>
                        <Text style={styles.billKey}>Tax</Text>
                        <Text style={styles.billVal}>₹{order.tax?.toFixed(0)}</Text>
                      </View>
                      <View style={styles.billRow}>
                        <Text style={styles.billKey}>Delivery Fee</Text>
                        <Text style={styles.billVal}>{order.deliveryFee === 0 ? "Free" : `₹${order.deliveryFee}`}</Text>
                      </View>
                      <View style={[styles.billRow, styles.billTotal]}>
                        <Text style={styles.billTotalKey}>Total Paid</Text>
                        <Text style={styles.billTotalVal}>₹{order.total?.toFixed(0)}</Text>
                      </View>
                    </View>
                    {order.paymentMethod && (
                      <View style={{ flexDirection: "row", alignItems: "center", gap: 6, marginBottom: 8 }}>
                        <Ionicons name="card-outline" size={14} color="#888" />
                        <Text style={{ fontFamily: "PoppinsRegular", fontSize: 12, color: "#888" }}>
                          Paid via {order.paymentMethod}
                        </Text>
                      </View>
                    )}
                    {order.status !== "Cancelled" && (
                      <TouchableOpacity style={styles.reorderBtn}>
                        <Ionicons name="refresh-outline" size={16} color="#ff5722" />
                        <Text style={styles.reorderText}>Reorder</Text>
                      </TouchableOpacity>
                    )}
                  </View>
                )}
              </View>
            );
          })
        )}
      </ScrollView>
    </View>
  );
}
const styles = StyleSheet.create({
  container: { flex: 1, backgroundColor: "#faf9f7" },
  header: { flexDirection: "row", justifyContent: "space-between", alignItems: "center", marginTop: 54, paddingHorizontal: 18, marginBottom: 20 },
  backBtn: { width: 36, height: 36, borderRadius: 18, backgroundColor: "#f0ece8", justifyContent: "center", alignItems: "center" },
  headerTitle: { fontSize: 20, fontFamily: "PoppinsSemiBold", color: "#1a1a2e" },
  card: { backgroundColor: "#fff", marginHorizontal: 16, borderRadius: 18, marginBottom: 14, elevation: 2, overflow: "hidden" },
  cardTop: { flexDirection: "row", justifyContent: "space-between", padding: 16 },
  orderMeta: { flex: 1 },
  orderId: { fontFamily: "PoppinsSemiBold", fontSize: 14, color: "#1a1a2e", marginBottom: 5 },
  dateRow: { flexDirection: "row", alignItems: "center", gap: 5, marginBottom: 4 },
  dateText: { fontFamily: "PoppinsRegular", fontSize: 12, color: "#888" },
  locationRow: { flexDirection: "row", alignItems: "center", gap: 5 },
  locationText: { fontFamily: "PoppinsRegular", fontSize: 12, color: "#888" },
  orderRight: { alignItems: "flex-end" },
  statusChip: { flexDirection: "row", alignItems: "center", gap: 4, paddingHorizontal: 10, paddingVertical: 4, borderRadius: 20, marginBottom: 6 },
  statusText: { fontFamily: "PoppinsMedium", fontSize: 11 },
  totalAmt: { fontFamily: "PoppinsBold", fontSize: 16, color: "#1a1a2e" },
  expandedBody: { borderTopWidth: 1, borderTopColor: "#f0ece8", paddingHorizontal: 16, paddingBottom: 16, paddingTop: 12 },
  itemsTitle: { fontFamily: "PoppinsSemiBold", fontSize: 13, color: "#1a1a2e", marginBottom: 8 },
  itemRow: { flexDirection: "row", alignItems: "center", paddingVertical: 5, gap: 8 },
  itemDot: { width: 6, height: 6, borderRadius: 3, backgroundColor: "#ff5722" },
  itemName: { flex: 1, fontFamily: "PoppinsRegular", fontSize: 13, color: "#444" },
  itemPrice: { fontFamily: "PoppinsMedium", fontSize: 13, color: "#1a1a2e" },
  billBox: { backgroundColor: "#faf9f7", borderRadius: 12, padding: 12, marginTop: 12, marginBottom: 12 },
  billRow: { flexDirection: "row", justifyContent: "space-between", paddingVertical: 4 },
  billKey: { fontFamily: "PoppinsRegular", fontSize: 13, color: "#888" },
  billVal: { fontFamily: "PoppinsMedium", fontSize: 13, color: "#444" },
  billTotal: { borderTopWidth: 1, borderTopColor: "#e0dbd6", marginTop: 6, paddingTop: 8 },
  billTotalKey: { fontFamily: "PoppinsSemiBold", fontSize: 14, color: "#1a1a2e" },
  billTotalVal: { fontFamily: "PoppinsBold", fontSize: 14, color: "#ff5722" },
  reorderBtn: { flexDirection: "row", alignItems: "center", justifyContent: "center", borderWidth: 1.5, borderColor: "#ff5722", borderRadius: 12, paddingVertical: 10, gap: 6, backgroundColor: "#fff0eb" },
  reorderText: { fontFamily: "PoppinsSemiBold", fontSize: 13, color: "#ff5722" },
});
</file>

<file path="OrderSuccess.js">
import React, { useEffect, useRef } from "react";
import {
  View,
  Text,
  StyleSheet,
  Animated,
  Easing,
} from "react-native";
import { Ionicons } from "@expo/vector-icons";

export default function OrderSuccess({ navigation }) {
  const scaleAnim   = useRef(new Animated.Value(0)).current;
  const fadeAnim    = useRef(new Animated.Value(0)).current;
  const slideAnim   = useRef(new Animated.Value(30)).current;

  useEffect(() => {
    // Animate icon pop
    Animated.spring(scaleAnim, {
      toValue: 1,
      tension: 60,
      friction: 6,
      useNativeDriver: true,
    }).start();

    // Fade + slide text in
    Animated.parallel([
      Animated.timing(fadeAnim, {
        toValue: 1,
        duration: 600,
        delay: 300,
        useNativeDriver: true,
      }),
      Animated.timing(slideAnim, {
        toValue: 0,
        duration: 500,
        delay: 300,
        easing: Easing.out(Easing.exp),
        useNativeDriver: true,
      }),
    ]).start();

    // Auto redirect after 4 seconds
    const timer = setTimeout(() => {
      navigation.navigate("Home");
    }, 4000);
    return () => clearTimeout(timer);
  }, []);

  const steps = [
    { icon: "checkmark-circle", label: "Order Placed",      color: "#4caf50" },
    { icon: "restaurant",       label: "Preparing Food",    color: "#ff9800" },
    { icon: "bicycle",          label: "Out for Delivery",  color: "#2196f3" },
    { icon: "home",             label: "Delivered",         color: "#9e9e9e" },
  ];

  return (
    <View style={styles.container}>

      {/* SUCCESS ICON */}
      <Animated.View style={[styles.iconWrapper, { transform: [{ scale: scaleAnim }] }]}>
        <View style={styles.iconOuter}>
          <View style={styles.iconInner}>
            <Ionicons name="checkmark" size={50} color="#fff" />
          </View>
        </View>
      </Animated.View>

      {/* TEXT */}
      <Animated.View style={{ opacity: fadeAnim, transform: [{ translateY: slideAnim }], alignItems: "center" }}>
        <Text style={styles.title}>Order Placed! 🎉</Text>
        <Text style={styles.subtitle}>
          Your order has been placed successfully.{"\n"}Sit back and relax!
        </Text>

        {/* ORDER ID */}
        <View style={styles.orderIdBox}>
          <Text style={styles.orderIdLabel}>Order ID</Text>
          <Text style={styles.orderIdValue}>#ANN{Math.floor(10000 + Math.random() * 90000)}</Text>
        </View>

        {/* ESTIMATED TIME */}
        <View style={styles.etaCard}>
          <Ionicons name="time-outline" size={22} color="#ff5722" />
          <View style={{ marginLeft: 12 }}>
            <Text style={styles.etaLabel}>Estimated Delivery</Text>
            <Text style={styles.etaValue}>30 – 45 minutes</Text>
          </View>
        </View>

        {/* DELIVERY STEPS */}
        <View style={styles.stepsRow}>
          {steps.map((step, i) => (
            <View key={i} style={styles.stepItem}>
              <View style={[styles.stepIcon, { backgroundColor: i === 0 ? step.color : "#f0ece8" }]}>
                <Ionicons
                  name={step.icon}
                  size={16}
                  color={i === 0 ? "#fff" : "#bbb"}
                />
              </View>
              {i < steps.length - 1 && (
                <View style={[styles.stepLine, { backgroundColor: i === 0 ? "#4caf50" : "#e0dbd6" }]} />
              )}
              <Text style={[styles.stepLabel, i === 0 && { color: "#4caf50", fontFamily: "PoppinsSemiBold" }]}>
                {step.label}
              </Text>
            </View>
          ))}
        </View>

        <Text style={styles.redirectText}>Returning to home in a moment...</Text>
      </Animated.View>

    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#faf9f7",
    justifyContent: "center",
    alignItems: "center",
    padding: 28,
  },

  // ICON
  iconWrapper: {
    marginBottom: 28,
  },
  iconOuter: {
    width: 120,
    height: 120,
    borderRadius: 60,
    backgroundColor: "rgba(76,175,80,0.12)",
    justifyContent: "center",
    alignItems: "center",
  },
  iconInner: {
    width: 90,
    height: 90,
    borderRadius: 45,
    backgroundColor: "#4caf50",
    justifyContent: "center",
    alignItems: "center",
    elevation: 8,
  },

  // TEXT
  title: {
    fontFamily: "PoppinsBold",
    fontSize: 26,
    color: "#1a1a2e",
    marginBottom: 10,
    textAlign: "center",
  },
  subtitle: {
    fontFamily: "PoppinsRegular",
    fontSize: 14,
    color: "#888",
    textAlign: "center",
    lineHeight: 22,
    marginBottom: 22,
  },

  // ORDER ID
  orderIdBox: {
    flexDirection: "row",
    alignItems: "center",
    backgroundColor: "#fff",
    borderRadius: 14,
    paddingVertical: 10,
    paddingHorizontal: 20,
    marginBottom: 16,
    elevation: 2,
    gap: 10,
  },
  orderIdLabel: {
    fontFamily: "PoppinsRegular",
    fontSize: 13,
    color: "#aaa",
  },
  orderIdValue: {
    fontFamily: "PoppinsBold",
    fontSize: 14,
    color: "#ff5722",
  },

  // ETA
  etaCard: {
    flexDirection: "row",
    alignItems: "center",
    backgroundColor: "#fff",
    borderRadius: 18,
    padding: 16,
    width: "100%",
    marginBottom: 22,
    elevation: 3,
    borderLeftWidth: 3,
    borderLeftColor: "#ff5722",
  },
  etaLabel: {
    fontFamily: "PoppinsRegular",
    fontSize: 12,
    color: "#aaa",
  },
  etaValue: {
    fontFamily: "PoppinsSemiBold",
    fontSize: 15,
    color: "#1a1a2e",
    marginTop: 2,
  },

  // STEPS
  stepsRow: {
    flexDirection: "row",
    alignItems: "flex-start",
    justifyContent: "center",
    marginBottom: 30,
    width: "100%",
  },
  stepItem: {
    alignItems: "center",
    flex: 1,
    position: "relative",
  },
  stepIcon: {
    width: 36,
    height: 36,
    borderRadius: 18,
    justifyContent: "center",
    alignItems: "center",
    marginBottom: 6,
  },
  stepLine: {
    position: "absolute",
    top: 18,
    left: "50%",
    width: "100%",
    height: 2,
    zIndex: -1,
  },
  stepLabel: {
    fontFamily: "PoppinsRegular",
    fontSize: 10,
    color: "#bbb",
    textAlign: "center",
  },

  redirectText: {
    fontFamily: "PoppinsRegular",
    fontSize: 12,
    color: "#ccc",
    letterSpacing: 0.3,
  },
});
</file>

<file path="Otpscreen.js">
import React, { useState, useRef } from "react";
import { View, Text, Image, StyleSheet, TextInput, TouchableOpacity, ActivityIndicator, Alert } from "react-native";
import logotp from "./assets/logotp.png";
import { sendOtp, verifyOtp, register } from "./api"; export default function OTP({ navigation, route }) {
  const { mobile, username, password } = route.params;
  const [otp, setOtp] = useState(["", "", "", "", "", ""]);
  const [loading, setLoading] = useState(false);
  const inputs = useRef([]); const handleChange = (text, index) => {
    let newOtp = [...otp];
    newOtp[index] = text;
    setOtp(newOtp);
    if (text && index < 5) inputs.current[index + 1].focus();
  };
  const handleKeyPress = (e, index) => {
    if (e.nativeEvent.key === "Backspace" && otp[index] === "" && index > 0) {
      inputs.current[index - 1].focus();
    }
  };
  const handleResend = async () => {
    if (!mobile) { Alert.alert("Error", "Mobile number not found"); return; }
    try {
      await sendOtp(mobile);
      Alert.alert("OTP Sent", "A new OTP has been sent to your mobile number.");
    } catch (e) {
      Alert.alert("Error", "Failed to resend OTP");
    }
  };
  const handleSubmit = async () => {
    if (otp.includes("")) { Alert.alert("Error", "Please enter complete OTP"); return; }
    const otpString = otp.join("");
    setLoading(true);
    try {
      // If no mobile (came from forgot password), just navigate to success
      if (!mobile) {
        navigation.navigate("Success");
        return;
      }
      const response = await verifyOtp(mobile, otpString); if (response.success) {
        // 🔥 REGISTER USER AFTER OTP SUCCESS
        await register(username, mobile, password);
        navigation.navigate("Successup");
      } else {
        Alert.alert("Invalid OTP", response.message || "Please try again");
      }
    } catch (error) {
      Alert.alert("Error", "Could not verify OTP");
    } finally {
      setLoading(false);
    }
  };
  return (
    <View style={styles.container}>
      <View style={styles.imageContainer}>
        <Image source={logotp} style={styles.image} />
      </View>
      <View style={styles.otpIconCircle}>
        <Text style={styles.otpEmoji}>🔐</Text>
      </View>
      <Text style={styles.title}>Verify OTP</Text>
      <Text style={styles.subtitle}>
        Enter the 6-digit code sent to{"\n"}{mobile || "your registered number"}.
      </Text>
      <View style={styles.otpContainer}>
        {otp.map((digit, index) => (
          <TextInput
            key={index}
            ref={(ref) => (inputs.current[index] = ref)}
            style={[styles.otpBox, digit !== "" && styles.otpBoxFilled]}
            maxLength={1} keyboardType="numeric" value={digit}
            onChangeText={(text) => handleChange(text, index)}
            onKeyPress={(e) => handleKeyPress(e, index)}
          />
        ))}
      </View>
      <Text style={styles.resend}>
        Didn't receive the code?{" "}
        <Text style={styles.resendAccent} onPress={handleResend}>Resend</Text>
      </Text>
      <TouchableOpacity style={styles.button} onPress={handleSubmit} disabled={loading}>
        {loading ? <ActivityIndicator color="#fff" /> : (
          <>
            <Text style={styles.buttonText}>Verify & Continue</Text>
            <Text style={{ color: "#fff", fontSize: 16, marginLeft: 8 }}>→</Text>
          </>
        )}
      </TouchableOpacity>
    </View>
  );
}
const styles = StyleSheet.create({
  container: { flex: 1, alignItems: "center", justifyContent: "center", backgroundColor: "#faf9f7", padding: 24 },
  imageContainer: { alignItems: "center", marginBottom: 4 },
  image: { width: 180, height: 130, resizeMode: "contain" },
  otpIconCircle: { width: 68, height: 68, borderRadius: 34, backgroundColor: "#fff0eb", justifyContent: "center", alignItems: "center", marginBottom: 18 },
  otpEmoji: { fontSize: 30 },
  title: { fontSize: 22, fontFamily: "PoppinsBold", color: "#1a1a2e", marginBottom: 8 },
  subtitle: { fontSize: 13.5, fontFamily: "PoppinsRegular", color: "#888", marginBottom: 28, textAlign: "center", lineHeight: 20, paddingHorizontal: 10 },
  otpContainer: { flexDirection: "row", gap: 10, marginBottom: 24 },
  otpBox: { width: 46, height: 54, borderWidth: 1.5, borderColor: "#ede8e4", backgroundColor: "#fff", borderRadius: 14, textAlign: "center", fontSize: 20, fontFamily: "PoppinsBold", color: "#1a1a2e", elevation: 2 },
  otpBoxFilled: { borderColor: "#ff5722", backgroundColor: "#fff0eb" },
  resend: { fontFamily: "PoppinsRegular", fontSize: 13, color: "#888", marginBottom: 28 },
  resendAccent: { fontFamily: "PoppinsSemiBold", color: "#ff5722" },
  button: { backgroundColor: "#ff5722", paddingVertical: 16, paddingHorizontal: 40, borderRadius: 18, alignItems: "center", flexDirection: "row", justifyContent: "center", elevation: 4, width: "100%", minHeight: 54 },
  buttonText: { color: "#fff", fontFamily: "PoppinsSemiBold", fontSize: 15, letterSpacing: 0.4 },
});
</file>

<file path="package.json">
{
  "name": "annapurnaresturant",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "expo start",
    "android": "expo start --android",
    "ios": "expo start --ios",
    "web": "expo start --web"
  },
  "dependencies": {
    "@react-native-async-storage/async-storage": "2.2.0",
    "@react-native-community/datetimepicker": "8.4.4",
    "@react-navigation/native": "^7.2.1",
    "@react-navigation/native-stack": "^7.14.9",
    "expo": "~54.0.33",
    "expo-linear-gradient": "~15.0.8",
    "expo-status-bar": "~3.0.9",
    "react": "19.1.0",
    "react-dom": "19.1.0",
    "react-native": "0.81.5",
    "react-native-safe-area-context": "~5.6.2",
    "react-native-screens": "~4.16.0",
    "react-native-web": "^0.21.0"
  },
  "private": true
}
</file>

<file path="PrivacyPolicyScreen.js">
import React from "react";
import { View, Text, StyleSheet, ScrollView, TouchableOpacity } from "react-native";
import { Ionicons } from "@expo/vector-icons";

export default function PrivacyPolicyScreen({ navigation }) {
  return (
    <View style={styles.container}>
      <ScrollView showsVerticalScrollIndicator={false}>
        
        {/* HEADER */}
        <View style={styles.header}>
          <TouchableOpacity
            style={styles.backBtn}
            onPress={() => navigation.goBack()}
          >
            <Ionicons name="arrow-back" size={20} color="#1a1a2e" />
          </TouchableOpacity>
          <Text style={styles.headerTitle}>Privacy Policy</Text>
          <View style={{ width: 36 }} />
        </View>

        {/* INTRO CARD */}
        <View style={styles.card}>
          <View style={styles.iconWrapper}>
            <Ionicons name="shield-checkmark-outline" size={26} color="#ff5722" />
          </View>

          <Text style={styles.title}>Your Privacy Matters</Text>

          <Text style={styles.text}>
            We respect your privacy. Your personal data is securely stored and
            never shared with third parties.
          </Text>
        </View>

        {/* DETAILS CARD */}
        <View style={styles.section}>
          <View style={styles.sectionCard}>
            
            <View style={styles.item}>
              <View style={styles.row}>
                <View style={styles.iconBox}>
                  <Ionicons name="lock-closed-outline" size={18} color="#ff5722" />
                </View>
                <Text style={styles.heading}>Data Protection</Text>
              </View>
              <Text style={styles.description}>
                Your personal information is encrypted and protected with high security standards.
              </Text>
            </View>

            <View style={styles.divider} />

            <View style={styles.item}>
              <View style={styles.row}>
                <View style={styles.iconBox}>
                  <Ionicons name="analytics-outline" size={18} color="#ff5722" />
                </View>
                <Text style={styles.heading}>Minimal Data Collection</Text>
              </View>
              <Text style={styles.description}>
                We only collect essential data required for order processing and improving your experience.
              </Text>
            </View>

            <View style={styles.divider} />

            <View style={styles.item}>
              <View style={styles.row}>
                <View style={styles.iconBox}>
                  <Ionicons name="people-outline" size={18} color="#ff5722" />
                </View>
                <Text style={styles.heading}>No Third-Party Sharing</Text>
              </View>
              <Text style={styles.description}>
                Your data is never sold or shared with external parties.
              </Text>
            </View>

          </View>
        </View>
      </ScrollView>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#faf9f7",
  },

  /* HEADER */
  header: {
    flexDirection: "row",
    alignItems: "center",
    justifyContent: "space-between",
    marginTop: 54,
    paddingHorizontal: 18,
    marginBottom: 20,
  },

  backBtn: {
    width: 36,
    height: 36,
    borderRadius: 18,
    backgroundColor: "#f0ece8",
    justifyContent: "center",
    alignItems: "center",
  },

  headerTitle: {
    fontSize: 22,
    fontFamily: "PoppinsSemiBold",
    color: "#1a1a2e",
  },

  /* INTRO CARD */
  card: {
    backgroundColor: "#fff",
    marginHorizontal: 16,
    borderRadius: 22,
    padding: 22,
    alignItems: "center",
    elevation: 3,
    marginBottom: 18,
  },

  iconWrapper: {
    width: 60,
    height: 60,
    borderRadius: 30,
    backgroundColor: "#fff0eb",
    justifyContent: "center",
    alignItems: "center",
    marginBottom: 12,
  },

  title: {
    fontFamily: "PoppinsSemiBold",
    fontSize: 18,
    color: "#1a1a2e",
    marginBottom: 6,
  },

  text: {
    fontFamily: "PoppinsRegular",
    fontSize: 13,
    color: "#888",
    textAlign: "center",
  },

  /* SECTION */
  section: {
    marginHorizontal: 16,
    marginBottom: 20,
  },

  sectionCard: {
    backgroundColor: "#fff",
    borderRadius: 18,
    elevation: 3,
    overflow: "hidden",
  },

  item: {
    padding: 16,
  },

  divider: {
    height: 1,
    backgroundColor: "#f0ece8",
  },

  row: {
    flexDirection: "row",
    alignItems: "center",
    gap: 12,
    marginBottom: 6,
  },

  iconBox: {
    width: 32,
    height: 32,
    borderRadius: 10,
    backgroundColor: "#fff0eb",
    justifyContent: "center",
    alignItems: "center",
  },

  heading: {
    fontFamily: "PoppinsMedium",
    fontSize: 14,
    color: "#1a1a2e",
  },

  description: {
    fontFamily: "PoppinsRegular",
    fontSize: 13,
    color: "#888",
    marginLeft: 44,
  },
});
</file>

<file path="Profile.js">
import React, { useState, useEffect, useContext } from "react";
import { View, Text, StyleSheet, TouchableOpacity, ScrollView, Switch, ActivityIndicator } from "react-native";
import { Ionicons } from "@expo/vector-icons";
import { WishlistContext } from "./WishlistContext";
import { getProfile, logout } from "./api";
export default function ProfileScreen({ navigation, route }) {
  const { wishlist } = useContext(WishlistContext);
  const [profile, setProfile] = useState(null);
  const [loading, setLoading] = useState(true);
  const [notificationsEnabled, setNotificationsEnabled] = useState(true);
  useEffect(() => {
    loadProfile();
  }, []);
  // Refresh when returning from EditProfile
  useEffect(() => {
    if (route?.params?.updatedProfile) {
      setProfile((prev) => prev ? { ...prev, ...route.params.updatedProfile } : route.params.updatedProfile);
    }
  }, [route?.params?.updatedProfile]);
  const loadProfile = async () => {
    try {
      const data = await getProfile();
      setProfile(data);
    } catch (e) {
      // Fallback to defaults if not logged in
      setProfile({ name: "Annapurna User", mobile: "", email: "", orderCount: 0, wishlistCount: 0 });
    } finally {
      setLoading(false);
    }
  };
  const handleLogout = async () => {
    await logout();
    navigation.reset({ index: 0, routes: [{ name: "Login" }] });
  };
  const menuItems = [
    {
      section: "Account",
      items: [
        { icon: "person-outline", label: "Edit Profile", onPress: () => navigation.navigate("EditProfile", { profile }) },
        { icon: "location-outline", label: "Saved Addresses", onPress: () => navigation.navigate("SavedAddresses") },
        { icon: "heart-outline", label: "My Wishlist", onPress: () => navigation.navigate("Wishlist") },
        { icon: "receipt-outline", label: "Order History", onPress: () => navigation.navigate("OrderHistory") },
      ],
    },
    {
      section: "Preferences",
      items: [
        { icon: "notifications-outline", label: "Notifications", toggle: true },
        { icon: "language-outline", label: "Language", value: "English", onPress: () => { } },
      ],
    },
    {
      section: "Support",
      items: [
        { icon: "help-circle-outline", label: "Help & FAQ", onPress: () => navigation.navigate("HelpFAQ") },
        { icon: "chatbubble-outline", label: "Contact Us", onPress: () => navigation.navigate("ContactUs") },
        { icon: "star-outline", label: "Rate the App", onPress: () => navigation.navigate("RateApp") },
        { icon: "document-text-outline", label: "Privacy Policy", onPress: () => navigation.navigate("PrivacyPolicy") },
      ],
    },
  ];
  if (loading) {
    return (
      <View style={[styles.container, { justifyContent: "center", alignItems: "center" }]}>
        <ActivityIndicator size="large" color="#ff5722" />
      </View>
    );
  }
  const displayName = profile?.name || "Annapurna User";
  const orderCount = profile?.orderCount || 0;
  return (
    <View style={styles.container}>
      <ScrollView showsVerticalScrollIndicator={false} contentContainerStyle={{ paddingBottom: 100 }}>
        <View style={styles.header}>
          <Text style={styles.headerTitle}>Profile</Text>
          <TouchableOpacity style={styles.iconCircle} onPress={() => navigation.navigate("Cart")}>
            <Ionicons name="cart-outline" size={20} color="#1a1a2e" />
          </TouchableOpacity>
        </View>
        <View style={styles.profileCard}>
          <View style={styles.avatarWrapper}>
            <View style={styles.avatar}>
              <Text style={styles.avatarText}>{displayName.charAt(0).toUpperCase()}</Text>
            </View>
            <TouchableOpacity style={styles.editAvatarBtn} onPress={() => navigation.navigate("EditProfile", { profile })}>
              <Ionicons name="camera" size={14} color="#fff" />
            </TouchableOpacity>
          </View>
          <View style={styles.profileInfo}>
            <Text style={styles.profileName}>{displayName}</Text>
            <Text style={styles.profilePhone}>{profile?.mobile || ""}</Text>
            {!!profile?.email && <Text style={styles.profileEmail}>{profile.email}</Text>}
            <View style={styles.memberBadge}>
              <Ionicons name="star" size={11} color="#ff5722" />
              <Text style={styles.memberText}>Regular Member</Text>
            </View>
          </View>
        </View>
        <View style={styles.statsRow}>
          <View style={styles.statBox}>
            <Text style={styles.statNumber}>{orderCount}</Text>
            <Text style={styles.statLabel}>Orders</Text>
          </View>
          <View style={styles.statDivider} />
          <View style={styles.statBox}>
            <Text style={styles.statNumber}>{wishlist.length}</Text>
            <Text style={styles.statLabel}>Wishlist</Text>
          </View>
          <View style={styles.statDivider} />
          <View style={styles.statBox}>
            <Text style={styles.statNumber}>₹0</Text>
            <Text style={styles.statLabel}>Saved</Text>
          </View>
        </View>
        {menuItems.map((section, si) => (
          <View key={si} style={styles.section}>
            <Text style={styles.sectionLabel}>{section.section}</Text>
            <View style={styles.sectionCard}>
              {section.items.map((item, ii) => (
                <TouchableOpacity
                  key={ii}
                  style={[styles.menuRow, ii < section.items.length - 1 && styles.menuRowBorder]}
                  onPress={item.onPress}
                  activeOpacity={item.toggle ? 1 : 0.6}
                >
                  <View style={styles.menuLeft}>
                    <View style={styles.menuIconBox}>
                      <Ionicons name={item.icon} size={18} color="#ff5722" />
                    </View>
                    <Text style={styles.menuLabel}>{item.label}</Text>
                  </View>
                  <View style={styles.menuRight}>
                    {item.toggle ? (
                      <Switch value={notificationsEnabled} onValueChange={setNotificationsEnabled}
                        trackColor={{ false: "#e0dbd6", true: "#ffcabc" }}
                        thumbColor={notificationsEnabled ? "#ff5722" : "#bbb"} />
                    ) : item.value ? (
                      <Text style={styles.menuValue}>{item.value}</Text>
                    ) : (
                      <Ionicons name="chevron-forward" size={16} color="#ccc" />
                    )}
                  </View>
                </TouchableOpacity>
              ))}
            </View>
          </View>
        ))}
        <TouchableOpacity style={styles.logoutBtn} onPress={handleLogout}>
          <Ionicons name="log-out-outline" size={20} color="#ff5722" />
          <Text style={styles.logoutText}>Log Out</Text>
        </TouchableOpacity>
        <Text style={styles.version}>Annapurna Restaurant</Text>
      </ScrollView>
      <View style={styles.footer}>
        <TouchableOpacity style={styles.footerBtn} onPress={() => navigation.navigate("Home")}>
          <Ionicons name="home-outline" size={22} color="rgba(255,255,255,0.6)" />
          <Text style={styles.footerLabel}>Home</Text>
        </TouchableOpacity>
        <TouchableOpacity style={styles.footerBtn} onPress={() => navigation.navigate("Wishlist")}>
          <Ionicons name="heart-outline" size={22} color="rgba(255,255,255,0.6)" />
          <Text style={styles.footerLabel}>Wishlist</Text>
        </TouchableOpacity>
        <TouchableOpacity style={styles.footerBtn} onPress={() => navigation.navigate("Cart")}>
          <Ionicons name="cart-outline" size={22} color="rgba(255,255,255,0.6)" />
          <Text style={styles.footerLabel}>Cart</Text>
        </TouchableOpacity>
        <TouchableOpacity style={styles.footerBtn}>
          <Ionicons name="person" size={22} color="#ff5722" />
          <Text style={[styles.footerLabel, { color: "#ff5722" }]}>Profile</Text>
        </TouchableOpacity>
      </View>
    </View>
  );
}
const styles = StyleSheet.create({
  container: { flex: 1, backgroundColor: "#faf9f7" },
  header: { flexDirection: "row", justifyContent: "space-between", alignItems: "center", marginTop: 54, paddingHorizontal: 18, marginBottom: 20 },
  headerTitle: { fontSize: 22, fontFamily: "PoppinsSemiBold", color: "#1a1a2e" },
  iconCircle: { padding: 8, borderRadius: 22, backgroundColor: "#f0ece8", elevation: 2 },
  profileCard: { flexDirection: "row", alignItems: "center", backgroundColor: "#fff", marginHorizontal: 16, borderRadius: 22, padding: 18, elevation: 3, marginBottom: 14 },
  avatarWrapper: { position: "relative", marginRight: 16 },
  avatar: { width: 70, height: 70, borderRadius: 35, backgroundColor: "#ff5722", justifyContent: "center", alignItems: "center" },
  avatarText: { fontFamily: "PoppinsBold", fontSize: 28, color: "#fff" },
  editAvatarBtn: { position: "absolute", bottom: 0, right: 0, width: 24, height: 24, borderRadius: 12, backgroundColor: "#1a1a2e", justifyContent: "center", alignItems: "center", borderWidth: 2, borderColor: "#fff" },
  profileInfo: { flex: 1 },
  profileName: { fontFamily: "PoppinsSemiBold", fontSize: 16, color: "#1a1a2e" },
  profilePhone: { fontFamily: "PoppinsRegular", fontSize: 13, color: "#888", marginTop: 2 },
  profileEmail: { fontFamily: "PoppinsRegular", fontSize: 12, color: "#aaa", marginTop: 1 },
  memberBadge: { flexDirection: "row", alignItems: "center", marginTop: 6, backgroundColor: "#fff0eb", alignSelf: "flex-start", paddingHorizontal: 10, paddingVertical: 3, borderRadius: 20, gap: 4 },
  memberText: { fontFamily: "PoppinsMedium", fontSize: 11, color: "#ff5722" },
  statsRow: { flexDirection: "row", backgroundColor: "#1a1a2e", marginHorizontal: 16, borderRadius: 22, paddingVertical: 18, marginBottom: 22, elevation: 4 },
  statBox: { flex: 1, alignItems: "center" },
  statNumber: { fontFamily: "PoppinsBold", fontSize: 20, color: "#fff" },
  statLabel: { fontFamily: "PoppinsRegular", fontSize: 12, color: "rgba(255,255,255,0.55)", marginTop: 3 },
  statDivider: { width: 1, backgroundColor: "rgba(255,255,255,0.12)", marginVertical: 4 },
  section: { marginHorizontal: 16, marginBottom: 18 },
  sectionLabel: { fontFamily: "PoppinsSemiBold", fontSize: 12, color: "#aaa", letterSpacing: 1, textTransform: "uppercase", marginBottom: 8, marginLeft: 4 },
  sectionCard: { backgroundColor: "#fff", borderRadius: 18, elevation: 2, overflow: "hidden" },
  menuRow: { flexDirection: "row", justifyContent: "space-between", alignItems: "center", paddingVertical: 14, paddingHorizontal: 16 },
  menuRowBorder: { borderBottomWidth: 1, borderBottomColor: "#f0ece8" },
  menuLeft: { flexDirection: "row", alignItems: "center", gap: 14 },
  menuIconBox: { width: 36, height: 36, borderRadius: 10, backgroundColor: "#fff0eb", justifyContent: "center", alignItems: "center" },
  menuLabel: { fontFamily: "PoppinsMedium", fontSize: 14, color: "#1a1a2e" },
  menuRight: { flexDirection: "row", alignItems: "center" },
  menuValue: { fontFamily: "PoppinsRegular", fontSize: 13, color: "#aaa", marginRight: 6 },
  logoutBtn: { flexDirection: "row", alignItems: "center", justifyContent: "center", marginHorizontal: 16, paddingVertical: 15, borderRadius: 18, borderWidth: 1.5, borderColor: "#ff5722", backgroundColor: "#fff0eb", gap: 8, marginBottom: 16 },
  logoutText: { fontFamily: "PoppinsSemiBold", fontSize: 15, color: "#ff5722" },
  version: { fontFamily: "PoppinsRegular", fontSize: 11, color: "#ccc", textAlign: "center", marginBottom: 10 },
  footer: { position: "absolute", bottom: 0, left: 0, right: 0, height: 65, backgroundColor: "#1a1a2e", flexDirection: "row", justifyContent: "space-around", alignItems: "center", paddingBottom: 4, elevation: 16 },
  footerBtn: { alignItems: "center", justifyContent: "center", paddingHorizontal: 14 },
  footerLabel: { color: "rgba(255,255,255,0.6)", fontFamily: "PoppinsRegular", fontSize: 10, marginTop: 3 },
});
</file>

<file path="ProfileScreen.js">
import React, { useState, useEffect } from "react";
import {
  View,
  Text,
  StyleSheet,
  TouchableOpacity,
  ScrollView,
  Switch,
} from "react-native";
import { Ionicons } from "@expo/vector-icons";

export default function ProfileScreen({ navigation, route }) {
  const [profile, setProfile] = useState({
    name: "Annapurna User",
    phone: "+91 98765 43210",
    email: "",
    dob: "",
    gender: "",
  });

  const [notificationsEnabled, setNotificationsEnabled] = useState(true);

  // Pick up changes saved from EditProfileScreen
  useEffect(() => {
    if (route?.params?.updatedProfile) {
      setProfile((prev) => ({ ...prev, ...route.params.updatedProfile }));
    }
  }, [route?.params?.updatedProfile]);

  const menuItems = [
    {
      section: "Account",
      items: [
        {
          icon: "person-outline",
          label: "Edit Profile",
          onPress: () =>
            navigation.navigate("EditProfile", { profile }),
        },
        {
          icon: "location-outline",
          label: "Saved Addresses",
          onPress: () => navigation.navigate("SavedAddresses"),
        },
        {
          icon: "heart-outline",
          label: "My Wishlist",
          onPress: () => navigation.navigate("Wishlist"),
        },
        {
          icon: "receipt-outline",
          label: "Order History",
          onPress: () => navigation.navigate("OrderHistory"),
        },
      ],
    },
    {
      section: "Preferences",
      items: [
        { icon: "notifications-outline", label: "Notifications", toggle: true },
        {
          icon: "language-outline",
          label: "Language",
          value: "English",
          onPress: () => {},
        },
      ],
    },
    {
      section: "Support",
      items: [
        { icon: "help-circle-outline",   label: "Help & FAQ",      onPress: () => {} },
        { icon: "chatbubble-outline",     label: "Contact Us",      onPress: () => {} },
        { icon: "star-outline",           label: "Rate the App",    onPress: () => {} },
        { icon: "document-text-outline",  label: "Privacy Policy",  onPress: () => {} },
      ],
    },
  ];

  return (
    <View style={styles.container}>
      <ScrollView
        showsVerticalScrollIndicator={false}
        contentContainerStyle={{ paddingBottom: 100 }}
      >
        {/* HEADER */}
        <View style={styles.header}>
          <Text style={styles.headerTitle}>Profile</Text>
          <TouchableOpacity
            style={styles.iconCircle}
            onPress={() => navigation.navigate("Cart")}
          >
            <Ionicons name="cart-outline" size={20} color="#1a1a2e" />
          </TouchableOpacity>
        </View>

        {/* PROFILE CARD */}
        <View style={styles.profileCard}>
          <View style={styles.avatarWrapper}>
            <View style={styles.avatar}>
              <Text style={styles.avatarText}>
                {profile.name.charAt(0).toUpperCase()}
              </Text>
            </View>
            <TouchableOpacity
              style={styles.editAvatarBtn}
              onPress={() => navigation.navigate("EditProfile", { profile })}
            >
              <Ionicons name="camera" size={14} color="#fff" />
            </TouchableOpacity>
          </View>

          <View style={styles.profileInfo}>
            <Text style={styles.profileName}>{profile.name}</Text>
            <Text style={styles.profilePhone}>{profile.phone}</Text>
            {profile.email ? (
              <Text style={styles.profileEmail}>{profile.email}</Text>
            ) : null}
            <View style={styles.memberBadge}>
              <Ionicons name="star" size={11} color="#ff5722" />
              <Text style={styles.memberText}>Regular Member</Text>
            </View>
          </View>
        </View>

        {/* STATS ROW */}
        <View style={styles.statsRow}>
          <View style={styles.statBox}>
            <Text style={styles.statNumber}>12</Text>
            <Text style={styles.statLabel}>Orders</Text>
          </View>
          <View style={styles.statDivider} />
          <View style={styles.statBox}>
            <Text style={styles.statNumber}>4</Text>
            <Text style={styles.statLabel}>Wishlist</Text>
          </View>
          <View style={styles.statDivider} />
          <View style={styles.statBox}>
            <Text style={styles.statNumber}>₹840</Text>
            <Text style={styles.statLabel}>Saved</Text>
          </View>
        </View>

        {/* MENU SECTIONS */}
        {menuItems.map((section, si) => (
          <View key={si} style={styles.section}>
            <Text style={styles.sectionLabel}>{section.section}</Text>
            <View style={styles.sectionCard}>
              {section.items.map((item, ii) => (
                <TouchableOpacity
                  key={ii}
                  style={[
                    styles.menuRow,
                    ii < section.items.length - 1 && styles.menuRowBorder,
                  ]}
                  onPress={item.onPress}
                  activeOpacity={item.toggle ? 1 : 0.6}
                >
                  <View style={styles.menuLeft}>
                    <View style={styles.menuIconBox}>
                      <Ionicons name={item.icon} size={18} color="#ff5722" />
                    </View>
                    <Text style={styles.menuLabel}>{item.label}</Text>
                  </View>

                  <View style={styles.menuRight}>
                    {item.toggle ? (
                      <Switch
                        value={notificationsEnabled}
                        onValueChange={setNotificationsEnabled}
                        trackColor={{ false: "#e0dbd6", true: "#ffcabc" }}
                        thumbColor={notificationsEnabled ? "#ff5722" : "#bbb"}
                      />
                    ) : item.value ? (
                      <Text style={styles.menuValue}>{item.value}</Text>
                    ) : (
                      <Ionicons name="chevron-forward" size={16} color="#ccc" />
                    )}
                  </View>
                </TouchableOpacity>
              ))}
            </View>
          </View>
        ))}

        {/* LOGOUT */}
        <TouchableOpacity
          style={styles.logoutBtn}
          onPress={() => navigation.navigate("Login")}
        >
          <Ionicons name="log-out-outline" size={20} color="#ff5722" />
          <Text style={styles.logoutText}>Log Out</Text>
        </TouchableOpacity>

        <Text style={styles.version}>Annapurna Restaurant</Text>
      </ScrollView>

      {/* FOOTER */}
      <View style={styles.footer}>
        <TouchableOpacity
          style={styles.footerBtn}
          onPress={() => navigation.navigate("Home")}
        >
          <Ionicons name="home-outline" size={22} color="rgba(255,255,255,0.6)" />
          <Text style={styles.footerLabel}>Home</Text>
        </TouchableOpacity>
        <TouchableOpacity
          style={styles.footerBtn}
          onPress={() => navigation.navigate("Wishlist")}
        >
          <Ionicons name="heart-outline" size={22} color="rgba(255,255,255,0.6)" />
          <Text style={styles.footerLabel}>Wishlist</Text>
        </TouchableOpacity>
        <TouchableOpacity
          style={styles.footerBtn}
          onPress={() => navigation.navigate("Cart")}
        >
          <Ionicons name="cart-outline" size={22} color="rgba(255,255,255,0.6)" />
          <Text style={styles.footerLabel}>Cart</Text>
        </TouchableOpacity>
        <TouchableOpacity style={styles.footerBtn}>
          <Ionicons name="person" size={22} color="#ff5722" />
          <Text style={[styles.footerLabel, { color: "#ff5722" }]}>Profile</Text>
        </TouchableOpacity>
      </View>
    </View>
  );
}

const styles = StyleSheet.create({
  container: { flex: 1, backgroundColor: "#faf9f7" },

  // HEADER
  header: {
    flexDirection: "row",
    justifyContent: "space-between",
    alignItems: "center",
    marginTop: 54,
    paddingHorizontal: 18,
    marginBottom: 20,
  },
  headerTitle: { fontSize: 22, fontFamily: "PoppinsSemiBold", color: "#1a1a2e" },
  iconCircle: {
    padding: 8,
    borderRadius: 22,
    backgroundColor: "#f0ece8",
    elevation: 2,
  },

  // PROFILE CARD
  profileCard: {
    flexDirection: "row",
    alignItems: "center",
    backgroundColor: "#fff",
    marginHorizontal: 16,
    borderRadius: 22,
    padding: 18,
    elevation: 3,
    marginBottom: 14,
  },
  avatarWrapper: { position: "relative", marginRight: 16 },
  avatar: {
    width: 70,
    height: 70,
    borderRadius: 35,
    backgroundColor: "#ff5722",
    justifyContent: "center",
    alignItems: "center",
  },
  avatarText: { fontFamily: "PoppinsBold", fontSize: 28, color: "#fff" },
  editAvatarBtn: {
    position: "absolute",
    bottom: 0,
    right: 0,
    width: 24,
    height: 24,
    borderRadius: 12,
    backgroundColor: "#1a1a2e",
    justifyContent: "center",
    alignItems: "center",
    borderWidth: 2,
    borderColor: "#fff",
  },
  profileInfo: { flex: 1 },
  profileName: { fontFamily: "PoppinsSemiBold", fontSize: 16, color: "#1a1a2e" },
  profilePhone: {
    fontFamily: "PoppinsRegular",
    fontSize: 13,
    color: "#888",
    marginTop: 2,
  },
  profileEmail: {
    fontFamily: "PoppinsRegular",
    fontSize: 12,
    color: "#aaa",
    marginTop: 1,
  },
  memberBadge: {
    flexDirection: "row",
    alignItems: "center",
    marginTop: 6,
    backgroundColor: "#fff0eb",
    alignSelf: "flex-start",
    paddingHorizontal: 10,
    paddingVertical: 3,
    borderRadius: 20,
    gap: 4,
  },
  memberText: { fontFamily: "PoppinsMedium", fontSize: 11, color: "#ff5722" },

  // STATS
  statsRow: {
    flexDirection: "row",
    backgroundColor: "#1a1a2e",
    marginHorizontal: 16,
    borderRadius: 22,
    paddingVertical: 18,
    marginBottom: 22,
    elevation: 4,
  },
  statBox: { flex: 1, alignItems: "center" },
  statNumber: { fontFamily: "PoppinsBold", fontSize: 20, color: "#fff" },
  statLabel: {
    fontFamily: "PoppinsRegular",
    fontSize: 12,
    color: "rgba(255,255,255,0.55)",
    marginTop: 3,
  },
  statDivider: {
    width: 1,
    backgroundColor: "rgba(255,255,255,0.12)",
    marginVertical: 4,
  },

  // MENU SECTIONS
  section: { marginHorizontal: 16, marginBottom: 18 },
  sectionLabel: {
    fontFamily: "PoppinsSemiBold",
    fontSize: 12,
    color: "#aaa",
    letterSpacing: 1,
    textTransform: "uppercase",
    marginBottom: 8,
    marginLeft: 4,
  },
  sectionCard: {
    backgroundColor: "#fff",
    borderRadius: 18,
    elevation: 2,
    overflow: "hidden",
  },
  menuRow: {
    flexDirection: "row",
    justifyContent: "space-between",
    alignItems: "center",
    paddingVertical: 14,
    paddingHorizontal: 16,
  },
  menuRowBorder: { borderBottomWidth: 1, borderBottomColor: "#f0ece8" },
  menuLeft: { flexDirection: "row", alignItems: "center", gap: 14 },
  menuIconBox: {
    width: 36,
    height: 36,
    borderRadius: 10,
    backgroundColor: "#fff0eb",
    justifyContent: "center",
    alignItems: "center",
  },
  menuLabel: { fontFamily: "PoppinsMedium", fontSize: 14, color: "#1a1a2e" },
  menuRight: { flexDirection: "row", alignItems: "center" },
  menuValue: {
    fontFamily: "PoppinsRegular",
    fontSize: 13,
    color: "#aaa",
    marginRight: 6,
  },

  // LOGOUT
  logoutBtn: {
    flexDirection: "row",
    alignItems: "center",
    justifyContent: "center",
    marginHorizontal: 16,
    paddingVertical: 15,
    borderRadius: 18,
    borderWidth: 1.5,
    borderColor: "#ff5722",
    backgroundColor: "#fff0eb",
    gap: 8,
    marginBottom: 16,
  },
  logoutText: { fontFamily: "PoppinsSemiBold", fontSize: 15, color: "#ff5722" },

  version: {
    fontFamily: "PoppinsRegular",
    fontSize: 11,
    color: "#ccc",
    textAlign: "center",
    marginBottom: 10,
  },

  // FOOTER
  footer: {
    position: "absolute",
    bottom: 0,
    left: 0,
    right: 0,
    height: 65,
    backgroundColor: "#1a1a2e",
    flexDirection: "row",
    justifyContent: "space-around",
    alignItems: "center",
    paddingBottom: 4,
    elevation: 16,
  },
  footerBtn: {
    alignItems: "center",
    justifyContent: "center",
    paddingHorizontal: 14,
  },
  footerLabel: {
    color: "rgba(255,255,255,0.6)",
    fontFamily: "PoppinsRegular",
    fontSize: 10,
    marginTop: 3,
  },
});
</file>

<file path="RateAppScreen.js">
import React from "react";
import { View, Text, StyleSheet, TouchableOpacity,} from "react-native";
import { Ionicons } from "@expo/vector-icons";

export default function RateAppScreen({ navigation }) {
  return (
    <View style={styles.container}>
      
      {/* HEADER */}
      <View style={styles.header}>
        <TouchableOpacity
            style={styles.backBtn}
            onPress={() => navigation.goBack()}
          >
            <Ionicons name="arrow-back" size={20} color="#1a1a2e" />
          </TouchableOpacity>
        <Text style={styles.headerTitle}>Rate Our App</Text>
        <View style={{ width: 36 }} />
      </View>

      {/* CARD */}
      <View style={styles.card}>
        
        {/* ICON */}
        <View style={styles.iconWrapper}>
          <Ionicons name="star" size={28} color="#ff5722" />
        </View>

        {/* TITLE */}
        <Text style={styles.title}>Enjoying Annapurna?</Text>

        {/* DESCRIPTION */}
        <Text style={styles.text}>
          If you love using our app, please take a moment to rate us. Your
          feedback helps us improve!
        </Text>

        {/* BUTTON */}
        <TouchableOpacity style={styles.button} activeOpacity={0.8}>
          <Text style={styles.buttonText}>Rate Now</Text>
        </TouchableOpacity>
      </View>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#faf9f7",
  },

  /* HEADER */
  header: {
    flexDirection: "row",
    alignItems: "center",
    justifyContent: "space-between",
    marginTop: 54,
    paddingHorizontal: 18,
    marginBottom: 20,
  },

  backBtn: {
    width: 36,
    height: 36,
    borderRadius: 18,
    backgroundColor: "#f0ece8",
    justifyContent: "center",
    alignItems: "center",
  },

  headerTitle: {
    fontSize: 22,
    fontFamily: "PoppinsSemiBold",
    color: "#1a1a2e",
  },

  /* CARD */
  card: {
    backgroundColor: "#fff",
    marginHorizontal: 16,
    borderRadius: 22,
    padding: 22,
    alignItems: "center",
    elevation: 3,
  },

  iconWrapper: {
    width: 60,
    height: 60,
    borderRadius: 30,
    backgroundColor: "#fff0eb",
    justifyContent: "center",
    alignItems: "center",
    marginBottom: 14,
  },

  title: {
    fontFamily: "PoppinsSemiBold",
    fontSize: 18,
    color: "#1a1a2e",
    marginBottom: 6,
  },

  text: {
    fontFamily: "PoppinsRegular",
    fontSize: 13,
    color: "#888",
    textAlign: "center",
    marginBottom: 20,
    paddingHorizontal: 10,
  },

  button: {
    backgroundColor: "#ff5722",
    paddingVertical: 12,
    paddingHorizontal: 28,
    borderRadius: 20,
    elevation: 2,
  },

  buttonText: {
    fontFamily: "PoppinsSemiBold",
    fontSize: 14,
    color: "#fff",
  },
});
</file>

<file path="README.md">
# annapurnaresturant
</file>

<file path="SavedAddressesScreen.js">
import React, { useState, useEffect, useCallback } from "react";
import {
  View,
  Text,
  StyleSheet,
  TouchableOpacity,
  ScrollView,
  Alert,
  ActivityIndicator,
} from "react-native";
import { Ionicons } from "@expo/vector-icons";
import { useFocusEffect } from "@react-navigation/native";
import {
  getAddresses,
  addAddress,
  updateAddress,
  deleteAddress,
  setDefaultAddress,
} from "./api";

const TAG_COLORS = {
  Home: { bg: "#e8f5e9", icon: "#43a047" },
  Work: { bg: "#e3f2fd", icon: "#1e88e5" },
  Parents: { bg: "#fff3e0", icon: "#fb8c00" },
  Other: { bg: "#f3e5f5", icon: "#8e24aa" },
  House: { bg: "#e8f5e9", icon: "#43a047" },
  Office: { bg: "#e3f2fd", icon: "#1e88e5" },
};

const TAG_ICONS = {
  Home: "home",
  Work: "briefcase",
  Parents: "people",
  House: "home",
  Office: "briefcase",
  Other: "location",
};

export default function SavedAddressesScreen({ navigation, route }) {
  const [addresses, setAddresses] = useState([]);
  const [loading, setLoading] = useState(true);

  // Reload addresses every time this screen comes into focus
  // (covers the case when EditAddress screen calls onSave and pops back)
  useFocusEffect(
    useCallback(() => {
      fetchAddresses();
    }, [])
  );

  const fetchAddresses = async () => {
    try {
      setLoading(true);
      const data = await getAddresses();
      setAddresses(data);
    } catch (e) {
      console.log("Fetch addresses error:", e);
      Alert.alert("Error", "Could not load addresses. Please try again.");
    } finally {
      setLoading(false);
    }
  };

  // Called by EditAddress screen via route params callback
  useEffect(() => {
    if (route?.params?.savedAddress) {
      handleSaveFromEditor(route.params.savedAddress);
      // Clear params so it doesn't re-trigger
      navigation.setParams({ savedAddress: undefined });
    }
  }, [route?.params?.savedAddress]);

  const handleSaveFromEditor = async (formData) => {
    try {
      if (formData.id && formData.id !== null) {
        await updateAddress(formData.id, formData);
      } else {
        await addAddress(formData);
      }
      fetchAddresses();
    } catch (e) {
      Alert.alert("Error", e.message || "Could not save address.");
    }
  };

  const handleSetDefault = async (id) => {
    try {
      const updated = await setDefaultAddress(id);
      setAddresses(updated);
    } catch (e) {
      Alert.alert("Error", e.message || "Could not set default address.");
    }
  };

  const handleDelete = async (id) => {
    try {
      console.log("Deleting ID:", id);

      await deleteAddress(Number(id));   // 🔥 ensure number
      await fetchAddresses();            // 🔥 refresh list

      Alert.alert("Success", "Address deleted successfully");
    } catch (e) {
      console.log("Delete error:", e);
      Alert.alert("Error", e.message || "Could not delete address.");
    }
  };

  const openAddNew = () => {
    navigation.navigate("EditAddress", {
      address: null,
      onSave: handleSaveFromEditor,
    });
  };

  const openEdit = (addr) => {
    navigation.navigate("EditAddress", {
      address: {
        id: addr.id,
        tag: addr.tag || "Other",
        tagIcon: addr.tagIcon || "📍",
        name: addr.name || "",
        phone: addr.phone || "",
        line1: addr.line1 || "",
        line2: addr.line2 || "",
        city: addr.city || "",
        state: addr.state || "",
        pincode: addr.pincode || "",
        isDefault: addr.isDefault || false,
      },
      onSave: handleSaveFromEditor,
    });
  };

  const colors = (tag) => TAG_COLORS[tag] || TAG_COLORS["Other"];
  const iconName = (tag) => TAG_ICONS[tag] || "location";

  if (loading) {
    return (
      <View style={[styles.container, { justifyContent: "center", alignItems: "center" }]}>
        <ActivityIndicator size="large" color="#ff5722" />
        <Text style={styles.loadingText}>Loading addresses...</Text>
      </View>
    );
  }

  return (
    <View style={styles.container}>
      {/* HEADER */}
      <View style={styles.header}>
        <TouchableOpacity style={styles.backBtn} onPress={() => navigation.goBack()}>
          <Ionicons name="arrow-back" size={20} color="#1a1a2e" />
        </TouchableOpacity>
        <Text style={styles.headerTitle}>Saved Addresses</Text>
        <View style={{ width: 36 }} />
      </View>

      <ScrollView
        showsVerticalScrollIndicator={false}
        contentContainerStyle={{ paddingBottom: 110 }}
      >
        {addresses.length === 0 ? (
          <View style={styles.emptyBox}>
            <Ionicons name="location-outline" size={52} color="#ddd" />
            <Text style={styles.emptyTitle}>No addresses saved</Text>
            <Text style={styles.emptySubtitle}>Add a delivery address to get started</Text>
          </View>
        ) : (
          addresses.map((addr) => {
            const c = colors(addr.tag);
            return (
              <View
                key={addr.id}
                style={[styles.card, addr.isDefault && styles.cardDefault]}
              >
                {/* TAG ROW */}
                <View style={styles.cardTop}>
                  <View style={[styles.tagChip, { backgroundColor: c.bg }]}>
                    <Ionicons name={iconName(addr.tag)} size={13} color={c.icon} />
                    <Text style={[styles.tagText, { color: c.icon }]}>
                      {addr.tag || "Other"}
                    </Text>
                  </View>
                  {addr.isDefault && (
                    <View style={styles.defaultBadge}>
                      <Ionicons name="checkmark-circle" size={12} color="#ff5722" />
                      <Text style={styles.defaultText}>Default</Text>
                    </View>
                  )}
                </View>

                {/* ADDRESS BODY */}
                <Text style={styles.addrName}>{addr.name || ""}</Text>
                {addr.line1 ? (
                  <Text style={styles.addrLine}>{`${addr.line1},`}</Text>
                ) : null}
                {addr.line2 ? (
                  <Text style={styles.addrLine}>{`${addr.line2},`}</Text>
                ) : null}
                <Text style={styles.addrLine}>
                  {`${addr.city || ""}, ${addr.state || ""} – ${addr.pincode || ""}`}
                </Text>
                {addr.phone ? (
                  <View style={styles.phoneRow}>
                    <Ionicons name="call-outline" size={13} color="#888" />
                    <Text style={styles.phoneText}>{addr.phone || ""}</Text>
                  </View>
                ) : null}

                {/* ACTIONS */}
                <View style={styles.actionRow}>
                  {!addr.isDefault && (
                    <TouchableOpacity
                      style={styles.actionBtn}
                      onPress={() => handleSetDefault(addr.id)}
                    >
                      <Ionicons name="radio-button-on-outline" size={15} color="#ff5722" />
                      <Text style={styles.actionText}>Set Default</Text>
                    </TouchableOpacity>
                  )}
                  <TouchableOpacity
                    style={styles.actionBtn}
                    onPress={() => openEdit(addr)}
                  >
                    <Ionicons name="create-outline" size={15} color="#1a1a2e" />
                    <Text style={[styles.actionText, { color: "#1a1a2e" }]}>Edit</Text>
                  </TouchableOpacity>
                  <TouchableOpacity
                    style={styles.actionBtn}
                    onPress={() => {
                      console.log("Delete clicked:", addr.id);
                      handleDelete(addr.id);
                    }}
                  >
                    <Ionicons name="trash-outline" size={15} color="#e53935" />
                    <Text style={[styles.actionText, { color: "#e53935" }]}>Delete</Text>
                  </TouchableOpacity>
                </View>
              </View>
            );
          })
        )}
      </ScrollView>

      {/* ADD NEW ADDRESS BUTTON */}
      <View style={styles.addBtnWrapper}>
        <TouchableOpacity style={styles.addBtn} onPress={openAddNew}>
          <Ionicons name="add-circle-outline" size={20} color="#fff" />
          <Text style={styles.addBtnText}>Add New Address</Text>
        </TouchableOpacity>
      </View>
    </View>
  );
}

const styles = StyleSheet.create({
  container: { flex: 1, backgroundColor: "#faf9f7" },

  loadingText: {
    fontFamily: "PoppinsRegular",
    color: "#888",
    marginTop: 12,
    fontSize: 13,
  },

  header: {
    flexDirection: "row",
    justifyContent: "space-between",
    alignItems: "center",
    marginTop: 54,
    paddingHorizontal: 18,
    marginBottom: 20,
  },
  backBtn: {
    width: 36,
    height: 36,
    borderRadius: 18,
    backgroundColor: "#f0ece8",
    justifyContent: "center",
    alignItems: "center",
  },
  headerTitle: { fontSize: 20, fontFamily: "PoppinsSemiBold", color: "#1a1a2e" },

  emptyBox: {
    alignItems: "center",
    marginTop: 80,
    paddingHorizontal: 40,
  },
  emptyTitle: {
    fontFamily: "PoppinsSemiBold",
    fontSize: 16,
    color: "#aaa",
    marginTop: 16,
  },
  emptySubtitle: {
    fontFamily: "PoppinsRegular",
    fontSize: 13,
    color: "#bbb",
    marginTop: 8,
    textAlign: "center",
  },

  card: {
    backgroundColor: "#fff",
    marginHorizontal: 16,
    borderRadius: 18,
    padding: 16,
    marginBottom: 14,
    elevation: 2,
    borderWidth: 1.5,
    borderColor: "transparent",
  },
  cardDefault: {
    borderColor: "#ff5722",
    backgroundColor: "#fffaf8",
  },

  cardTop: {
    flexDirection: "row",
    justifyContent: "space-between",
    alignItems: "center",
    marginBottom: 10,
  },
  tagChip: {
    flexDirection: "row",
    alignItems: "center",
    gap: 5,
    paddingHorizontal: 10,
    paddingVertical: 4,
    borderRadius: 20,
  },
  tagText: { fontFamily: "PoppinsMedium", fontSize: 12 },
  defaultBadge: {
    flexDirection: "row",
    alignItems: "center",
    gap: 4,
    backgroundColor: "#fff0eb",
    paddingHorizontal: 8,
    paddingVertical: 3,
    borderRadius: 20,
  },
  defaultText: { fontFamily: "PoppinsMedium", fontSize: 11, color: "#ff5722" },

  addrName: {
    fontFamily: "PoppinsSemiBold",
    fontSize: 14,
    color: "#1a1a2e",
    marginBottom: 3,
  },
  addrLine: {
    fontFamily: "PoppinsRegular",
    fontSize: 13,
    color: "#555",
    lineHeight: 20,
  },
  phoneRow: { flexDirection: "row", alignItems: "center", gap: 5, marginTop: 6 },
  phoneText: { fontFamily: "PoppinsRegular", fontSize: 12, color: "#888" },

  actionRow: {
    flexDirection: "row",
    marginTop: 12,
    paddingTop: 12,
    borderTopWidth: 1,
    borderTopColor: "#f0ece8",
    gap: 16,
  },
  actionBtn: { flexDirection: "row", alignItems: "center", gap: 5 },
  actionText: { fontFamily: "PoppinsMedium", fontSize: 13, color: "#ff5722" },

  addBtnWrapper: {
    position: "absolute",
    bottom: 0,
    left: 0,
    right: 0,
    padding: 16,
    backgroundColor: "#faf9f7",
    borderTopWidth: 1,
    borderTopColor: "#f0ece8",
  },
  addBtn: {
    flexDirection: "row",
    backgroundColor: "#ff5722",
    borderRadius: 18,
    paddingVertical: 15,
    justifyContent: "center",
    alignItems: "center",
    gap: 8,
    elevation: 4,
  },
  addBtnText: { fontFamily: "PoppinsSemiBold", fontSize: 15, color: "#fff" },
});
</file>

<file path="Signup.js">
import React, { useState } from "react";
import {
  View, Text, Image, StyleSheet, TextInput,
  TouchableOpacity, ScrollView, KeyboardAvoidingView,
  Platform, ActivityIndicator, Alert,
} from "react-native";
import { Ionicons } from "@expo/vector-icons";
import logotp from "./assets/logotp.png";
import { register } from "./api";
import { sendOtp } from "./api";

export default function Register({ navigation }) {
  const [username, setUsername] = useState("");
  const [mobile, setMobile] = useState("");
  const [password, setPassword] = useState("");
  const [confirmPassword, setConfirmPassword] = useState("");
  const [showPassword, setShowPassword] = useState(false);
  const [showConfirmPassword, setShowConfirmPassword] = useState(false);
  const [loading, setLoading] = useState(false);
  const handleSignup = async () => {
    if (!username.trim() || !mobile || !password || !confirmPassword) {
      Alert.alert("Error", "Please enter all details");
      return;
    }
    if (!/^[A-Za-z ]{3,}$/.test(username.trim())) {
      Alert.alert("Error", "Enter valid full name (only letters)");
      return;
    }
    if (!/^[0-9]{10}$/.test(mobile)) {
      Alert.alert("Error", "Enter valid 10-digit mobile number");
      return;
    }
    if (password.length < 6) {
      Alert.alert("Error", "Password must be at least 6 characters");
      return;
    }
    if (password !== confirmPassword) {
      Alert.alert("Error", "Passwords do not match");
      return;
    }
    setLoading(true);
    try {
      const response = await sendOtp(mobile);
      if (response.success) {
        navigation.navigate("Otpscreen", {
          mobile,
          username,
          password
        });
      } else {
        Alert.alert("Registration Failed", response.message || "Something went wrong");
      }
    } catch (error) {
      Alert.alert("Error", "Could not connect to server. Check your network.");
    } finally {
      setLoading(false);
    }
  };
  return (
    <KeyboardAvoidingView style={{ flex: 1 }} behavior={Platform.OS === "ios" ? "padding" : "height"}>
      <ScrollView
        contentContainerStyle={{ flexGrow: 1, paddingBottom: 30 }}
        keyboardShouldPersistTaps="handled"
        showsVerticalScrollIndicator={false}
      >
        <View style={styles.container}>
          <View style={styles.content}>
            <View style={styles.imageContainer}>
              <Image source={logotp} style={styles.image} />
            </View>
            <View style={styles.tabs}>
              <TouchableOpacity onPress={() => navigation.navigate("Login")}>
                <Text style={styles.inactiveTab}>Login</Text>
              </TouchableOpacity>
              <View style={styles.activeTabWrap}>
                <Text style={styles.activeTab}>Sign up</Text>
              </View>
            </View>
            <View style={styles.inputWrapper}>
              <Ionicons name="person-outline" size={18} color="#aaa" style={styles.inputIcon} />
              <TextInput placeholder="Full Name" placeholderTextColor="#bbb" style={styles.input} value={username} onChangeText={setUsername} />
            </View>
            <View style={styles.inputWrapper}>
              <Ionicons name="call-outline" size={18} color="#aaa" style={styles.inputIcon} />
              <TextInput
                placeholder="Mobile Number" placeholderTextColor="#bbb" style={styles.input}
                value={mobile} keyboardType="numeric" maxLength={10}
                onChangeText={(text) => { const f = text.replace(/[^0-9]/g, ""); if (f.length <= 10) setMobile(f); }}
              />
            </View>
            <View style={styles.inputWrapper}>
              <Ionicons name="lock-closed-outline" size={18} color="#aaa" style={styles.inputIcon} />
              <TextInput placeholder="Password" placeholderTextColor="#bbb" style={[styles.input, { flex: 1 }]}
                secureTextEntry={!showPassword} value={password} onChangeText={setPassword} />
              <TouchableOpacity onPress={() => setShowPassword(!showPassword)} style={{ paddingRight: 4 }}>
                <Ionicons name={showPassword ? "eye" : "eye-off"} size={18} color="#aaa" />
              </TouchableOpacity>
            </View>
            <View style={styles.inputWrapper}>
              <Ionicons name="lock-closed-outline" size={18} color="#aaa" style={styles.inputIcon} />
              <TextInput placeholder="Confirm Password" placeholderTextColor="#bbb" style={[styles.input, { flex: 1 }]}
                secureTextEntry={!showConfirmPassword} value={confirmPassword} onChangeText={setConfirmPassword} />
              <TouchableOpacity onPress={() => setShowConfirmPassword(!showConfirmPassword)} style={{ paddingRight: 4 }}>
                <Ionicons name={showConfirmPassword ? "eye" : "eye-off"} size={18} color="#aaa" />
              </TouchableOpacity>
            </View>
            <TouchableOpacity style={styles.button} onPress={handleSignup} disabled={loading}>
              {loading ? <ActivityIndicator color="#fff" /> : (
                <>
                  <Text style={styles.buttonText}>Sign up</Text>
                  <Ionicons name="arrow-forward" size={16} color="#fff" style={{ marginLeft: 8 }} />
                </>
              )}
            </TouchableOpacity>
            <Text style={styles.loginLink}>
              Already a member?{" "}
              <Text style={styles.loginLinkAccent} onPress={() => navigation.navigate("Login")}>Login</Text>
            </Text>
          </View>
        </View>
      </ScrollView>
    </KeyboardAvoidingView>
  );
}
const styles = StyleSheet.create({
  container: { flex: 1, backgroundColor: "#faf9f7", padding: 24 },
  content: { flex: 1, paddingTop: 30 },
  imageContainer: { alignItems: "center", marginBottom: 8 },
  image: { width: 220, height: 160, resizeMode: "contain" },
  tabs: { flexDirection: "row", justifyContent: "center", alignItems: "center", gap: 32, marginBottom: 28 },
  activeTabWrap: { borderBottomWidth: 2, borderColor: "#ff5722", paddingBottom: 4 },
  activeTab: { fontFamily: "PoppinsSemiBold", fontSize: 16, color: "#ff5722" },
  inactiveTab: { fontFamily: "PoppinsMedium", fontSize: 16, color: "#bbb", paddingBottom: 4 },
  inputWrapper: {
    flexDirection: "row", alignItems: "center", backgroundColor: "#fff",
    borderRadius: 14, paddingHorizontal: 14, paddingVertical: 4,
    marginBottom: 14, borderWidth: 1, borderColor: "#ede8e4", elevation: 1,
  },
  inputIcon: { marginRight: 10 },
  input: { flex: 1, fontFamily: "PoppinsRegular", fontSize: 14, color: "#1a1a2e", paddingVertical: 13 },
  button: {
    backgroundColor: "#ff5722", paddingVertical: 16, borderRadius: 18, alignItems: "center",
    flexDirection: "row", justifyContent: "center", elevation: 4, marginTop: 6, marginBottom: 20, minHeight: 54,
  },
  buttonText: { color: "#fff", fontFamily: "PoppinsSemiBold", fontSize: 15, letterSpacing: 0.4 },
  loginLink: { fontFamily: "PoppinsRegular", fontSize: 13, color: "#888", textAlign: "center" },
  loginLinkAccent: { fontFamily: "PoppinsSemiBold", color: "#ff5722" },
});
</file>

<file path="Starters.js">
import React, { useState } from "react";
import {
  View,
  Text,
  StyleSheet,
  TextInput,
  TouchableOpacity,
  FlatList,
  Image,
  Share,
} from "react-native";
import { Ionicons } from "@expo/vector-icons";
import { allFoodItems } from "./Data";
import { useContext } from "react";
import { CartContext } from "./CartContext";
import { WishlistContext } from "./WishlistContext";

export default function StartersScreen({ navigation }) {
  const { cart, addToCart, removeFromCart } = useContext(CartContext);
  const { toggleWishlist, isWishlisted } = useContext(WishlistContext);
  const [searchText, setSearchText] = useState("");

  const totalItems = cart.reduce((sum, i) => sum + i.quantity, 0);

  const baseItems = allFoodItems.filter((item) => item.category === "Starters");
  const filteredItems = baseItems.filter((item) =>
    item.name.toLowerCase().includes(searchText.toLowerCase())
  );

  const shareItem = async (item) => {
    try {
      await Share.share({
        message: `${item.name} - Rs.${item.price}\nTry this delicious food!`,
      });
    } catch (error) {
      console.log(error);
    }
  };

  const renderItem = ({ item }) => {
    const cartItem = cart.find((i) => i.id === item.id);
    const wishlisted = isWishlisted(item.id);

    return (
      <View style={styles.foodCard}>
        {/* LEFT */}
        <View style={{ flex: 1, paddingRight: 10 }}>
          <Text style={styles.foodTitle}>{item.name}</Text>
          <Text style={styles.foodPrice}>Rs.{item.price}</Text>
          <Text style={styles.foodDesc}>{item.description}</Text>

          <View style={styles.iconRow}>
            {/* WISHLIST */}
            <TouchableOpacity style={styles.iconBtn} onPress={() => toggleWishlist(item)}>
              <Ionicons
                name={wishlisted ? "heart" : "heart-outline"}
                size={17}
                color="#ff5722"
              />
            </TouchableOpacity>

            {/* SHARE */}
            <TouchableOpacity style={styles.iconBtn} onPress={() => shareItem(item)}>
              <Ionicons name="share-social-outline" size={17} color="#444" />
            </TouchableOpacity>

            {/* ADD / QTY */}
            <View style={styles.actionBox}>
              {!cartItem ? (
                <TouchableOpacity style={styles.addInlineBtn} onPress={() => addToCart(item)}>
                  <Text style={styles.addInlineText}>ADD +</Text>
                </TouchableOpacity>
              ) : (
                <View style={styles.qtyInlineBox}>
                  <TouchableOpacity onPress={() => removeFromCart(item.id)}>
                    <Text style={styles.qtyBtnText}>-</Text>
                  </TouchableOpacity>
                  <Text style={styles.qtyText}>{cartItem.quantity}</Text>
                  <TouchableOpacity onPress={() => addToCart(item)}>
                    <Text style={styles.qtyBtnText}>+</Text>
                  </TouchableOpacity>
                </View>
              )}
            </View>
          </View>
        </View>

        {/* RIGHT */}
        <View style={styles.imageBox}>
          <Image source={item.image} style={styles.foodImage} />
        </View>
      </View>
    );
  };

  return (
    <View style={styles.container}>

      {/* HEADER */}
      <View style={styles.header}>
        <TouchableOpacity style={styles.iconCircle} onPress={() => navigation.goBack()}>
          <Ionicons name="arrow-back" size={22} color="#1a1a2e" />
        </TouchableOpacity>
        <Text style={styles.title}>Starters Menu</Text>
        <TouchableOpacity style={styles.iconCircle} onPress={() => navigation.navigate("Cart")}>
          <Ionicons name="cart-outline" size={20} color="#1a1a2e" />
          {cart.length > 0 && (
            <View style={styles.cartBadge}>
              <Text style={styles.cartText}>{totalItems}</Text>
            </View>
          )}
        </TouchableOpacity>
      </View>

      {/* SEARCH */}
      <View style={styles.searchContainer}>
        <Ionicons name="search" size={18} color="#aaa" />
        <TextInput
          placeholder="Search starters..."
          placeholderTextColor="#bbb"
          value={searchText}
          onChangeText={setSearchText}
          style={styles.input}
        />
        {searchText.length > 0 && (
          <TouchableOpacity onPress={() => setSearchText("")}>
            <Ionicons name="close-circle" size={18} color="#bbb" />
          </TouchableOpacity>
        )}
      </View>

      {/* COUNT */}
      <View style={styles.countRow}>
        <Text style={styles.countText}>{filteredItems.length} items</Text>
      </View>

      {/* LIST */}
      <FlatList
        data={filteredItems}
        keyExtractor={(item) => item.id.toString()}
        renderItem={renderItem}
        showsVerticalScrollIndicator={false}
        contentContainerStyle={{ paddingBottom: totalItems > 0 ? 130 : 30, paddingTop: 4 }}
        ListEmptyComponent={
          <View style={styles.emptyBox}>
            <Text style={styles.emptyEmoji}>🍽</Text>
            <Text style={styles.emptyTitle}>Nothing found</Text>
            <Text style={styles.emptySubtitle}>Try a different search</Text>
          </View>
        }
      />

      {/* CART BAR */}
      {totalItems > 0 && (
        <View style={styles.cartBar}>
          <View style={styles.cartBarLeft}>
            <View style={styles.cartCountBadge}>
              <Text style={styles.cartCountText}>{totalItems}</Text>
            </View>
            <Text style={styles.cartBarText}>
              {totalItems} {totalItems === 1 ? "item" : "items"} added
            </Text>
          </View>
          <TouchableOpacity style={styles.checkoutBtn} onPress={() => navigation.navigate("Cart")}>
            <Text style={styles.checkoutText}>Checkout</Text>
            <Ionicons name="arrow-forward" size={14} color="#ff5722" />
          </TouchableOpacity>
        </View>
      )}
    </View>
  );
}

const styles = StyleSheet.create({
  container: { flex: 1, backgroundColor: "#faf9f7" },

  // HEADER
  header: {
    flexDirection: "row",
    justifyContent: "space-between",
    alignItems: "center",
    marginTop: 50,
    paddingHorizontal: 16,
    paddingVertical: 14,
    backgroundColor: "#fff",
    elevation: 3,
    shadowColor: "#000",
    shadowOpacity: 0.07,
    shadowOffset: { width: 0, height: 2 },
    shadowRadius: 6,
  },
  title: {
    fontSize: 17,
    fontFamily: "PoppinsSemiBold",
    color: "#1a1a2e",
    letterSpacing: 0.2,
  },
  iconCircle: {
    padding: 8,
    borderRadius: 22,
    backgroundColor: "#f0ece8",
    shadowColor: "#000",
    shadowOpacity: 0.05,
    shadowOffset: { width: 0, height: 1 },
    shadowRadius: 3,
    elevation: 1,
  },
  cartBadge: {
    position: "absolute",
    top: -4,
    right: -4,
    backgroundColor: "#ff5722",
    borderRadius: 10,
    minWidth: 18,
    height: 18,
    justifyContent: "center",
    alignItems: "center",
    paddingHorizontal: 4,
  },
  cartText: { color: "#fff", fontSize: 9, fontFamily: "PoppinsBold" },

  // SEARCH
  searchContainer: {
    flexDirection: "row",
    alignItems: "center",
    backgroundColor: "#f0ece8",
    marginHorizontal: 16,
    marginTop: 14,
    marginBottom: 4,
    paddingHorizontal: 16,
    paddingVertical: 2,
    borderRadius: 30,
  },
  input: {
    marginLeft: 8,
    flex: 1,
    fontFamily: "PoppinsRegular",
    fontSize: 13.5,
    color: "#1a1a2e",
    paddingVertical: 10,
  },

  // COUNT
  countRow: {
    paddingHorizontal: 20,
    paddingTop: 10,
    paddingBottom: 2,
  },
  countText: {
    fontFamily: "PoppinsRegular",
    fontSize: 12,
    color: "#999",
  },

  // FOOD CARD
  foodCard: {
    flexDirection: "row",
    padding: 14,
    backgroundColor: "#fff",
    marginHorizontal: 14,
    marginVertical: 7,
    borderRadius: 18,
    alignItems: "stretch",
    elevation: 3,
    shadowColor: "#000",
    shadowOpacity: 0.07,
    shadowOffset: { width: 0, height: 3 },
    shadowRadius: 8,
  },
  foodTitle: {
    fontSize: 15,
    fontFamily: "PoppinsSemiBold",
    color: "#1a1a2e",
    letterSpacing: 0.1,
  },
  foodPrice: {
    fontSize: 14.5,
    fontFamily: "PoppinsBold",
    color: "#ff5722",
    marginTop: 3,
  },
  foodDesc: {
    fontSize: 12,
    fontFamily: "PoppinsRegular",
    color: "#888",
    marginTop: 5,
    lineHeight: 17,
    marginRight: 4,
    textAlign: "justify",
    flexShrink: 1,
  },
  iconRow: {
    flexDirection: "row",
    marginTop: 10,
    alignItems: "center",
    flexWrap: "nowrap",
  },
  iconBtn: {
    width: 34,
    height: 34,
    borderRadius: 17,
    borderWidth: 1,
    borderColor: "#ede8e4",
    justifyContent: "center",
    alignItems: "center",
    marginRight: 8,
    backgroundColor: "#faf9f7",
  },
  imageBox: {
    width: 120,
    justifyContent: "center",
    alignItems: "center",
  },
  foodImage: {
    width: 110,
    height: 115,
    borderRadius: 18,
  },
  actionBox: {
    marginLeft: 6,
    width: 90,
    height: 36,
    justifyContent: "center",
    alignItems: "center",
    flexShrink: 1,
  },
  addInlineBtn: {
    width: 90,
    height: 36,
    backgroundColor: "#fff",
    borderWidth: 1.5,
    borderColor: "#ff5722",
    borderRadius: 12,
    justifyContent: "center",
    alignItems: "center",
  },
  addInlineText: {
    color: "#ff5722",
    fontFamily: "PoppinsSemiBold",
    fontSize: 12.5,
    letterSpacing: 0.5,
  },
  qtyInlineBox: {
    width: 90,
    height: 36,
    flexDirection: "row",
    backgroundColor: "#ff5722",
    borderRadius: 12,
    justifyContent: "space-around",
    alignItems: "center",
  },
  qtyBtnText: {
    color: "#fff",
    fontSize: 17,
    fontFamily: "PoppinsMedium",
    marginHorizontal: 6,
  },
  qtyText: {
    color: "#fff",
    fontFamily: "PoppinsBold",
    fontSize: 14,
  },

  // EMPTY STATE
  emptyBox: { alignItems: "center", marginTop: 60 },
  emptyEmoji: { fontSize: 48 },
  emptyTitle: { fontFamily: "PoppinsSemiBold", fontSize: 16, color: "#1a1a2e", marginTop: 12 },
  emptySubtitle: { fontFamily: "PoppinsRegular", fontSize: 13, color: "#999", marginTop: 5 },

  // CART BAR
  cartBar: {
    position: "absolute",
    bottom: 16,
    left: 14,
    right: 14,
    height: 56,
    backgroundColor: "#1a1a2e",
    flexDirection: "row",
    justifyContent: "space-between",
    alignItems: "center",
    paddingHorizontal: 16,
    borderRadius: 32,
    elevation: 10,
    shadowColor: "#000",
    shadowOpacity: 0.22,
    shadowOffset: { width: 0, height: 5 },
    shadowRadius: 10,
  },
  cartBarLeft: { flexDirection: "row", alignItems: "center", gap: 10 },
  cartCountBadge: {
    width: 26,
    height: 26,
    borderRadius: 13,
    backgroundColor: "#ff5722",
    justifyContent: "center",
    alignItems: "center",
  },
  cartCountText: { color: "#fff", fontFamily: "PoppinsBold", fontSize: 11 },
  cartBarText: { color: "rgba(255,255,255,0.85)", fontFamily: "PoppinsMedium", fontSize: 13 },
  checkoutBtn: {
    flexDirection: "row",
    alignItems: "center",
    backgroundColor: "#fff",
    paddingHorizontal: 16,
    paddingVertical: 8,
    borderRadius: 20,
    gap: 5,
  },
  checkoutText: { color: "#ff5722", fontFamily: "PoppinsSemiBold", fontSize: 13 },
});
</file>

<file path="Success.js">
import React, { useEffect } from "react";
import { View, Text, StyleSheet } from "react-native";
import { Ionicons } from "@expo/vector-icons";

export default function Success({ navigation }) {
  useEffect(() => {
    const timer = setTimeout(() => {
      navigation.navigate("Home");
    }, 1500);
    return () => clearTimeout(timer);
  }, []);

  return (
    <View style={styles.container}>
      <View style={styles.iconCircle}>
        <Ionicons name="checkmark" size={44} color="#fff" />
      </View>
      <Text style={styles.title}>Login Successful</Text>
      <Text style={styles.subtitle}>
        Welcome back! You have successfully logged in.
      </Text>
      {/* <Text style={styles.redirectText}>Redirecting to home...</Text> */}
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#faf9f7",
    justifyContent: "center",
    alignItems: "center",
    padding: 30,
  },
  iconCircle: {
    width: 90,
    height: 90,
    borderRadius: 45,
    backgroundColor: "#ff5722",
    justifyContent: "center",
    alignItems: "center",
    marginBottom: 28,
    elevation: 6,
  },
  title: {
    fontSize: 22,
    fontFamily: "PoppinsBold",
    color: "#1a1a2e",
    marginBottom: 10,
  },
  subtitle: {
    fontSize: 14,
    fontFamily: "PoppinsRegular",
    color: "#888",
    textAlign: "center",
    marginBottom: 20,
    paddingHorizontal: 20,
    lineHeight: 22,
  },
  redirectText: {
    fontFamily: "PoppinsRegular",
    fontSize: 12,
    color: "#bbb",
    letterSpacing: 0.3,
  },
});
</file>

<file path="Successup.js">
import React, { useEffect } from "react";
import { View, Text, StyleSheet } from "react-native";
import { Ionicons } from "@expo/vector-icons";

export default function Successup({ navigation }) {
  useEffect(() => {
    const timer = setTimeout(() => {
      navigation.navigate("Home");
    }, 1500);
    return () => clearTimeout(timer);
  }, []);

  return (
    <View style={styles.container}>
      <View style={styles.iconCircle}>
        <Ionicons name="checkmark" size={44} color="#fff" />
      </View>
      <Text style={styles.title}>Signup Successful</Text>
      <Text style={styles.subtitle}>
        Welcome to Annapurna Restaurant. Let's get started!
      </Text>
      {/* <Text style={styles.redirectText}>Redirecting to home...</Text> */}
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#faf9f7",
    justifyContent: "center",
    alignItems: "center",
    padding: 30,
  },
  iconCircle: {
    width: 90,
    height: 90,
    borderRadius: 45,
    backgroundColor: "#ff5722",
    justifyContent: "center",
    alignItems: "center",
    marginBottom: 28,
    elevation: 6,
  },
  title: {
    fontSize: 22,
    fontFamily: "PoppinsBold",
    color: "#1a1a2e",
    marginBottom: 10,
  },
  subtitle: {
    fontSize: 14,
    fontFamily: "PoppinsRegular",
    color: "#888",
    textAlign: "center",
    marginBottom: 20,
    paddingHorizontal: 20,
    lineHeight: 22,
  },
  redirectText: {
    fontFamily: "PoppinsRegular",
    fontSize: 12,
    color: "#bbb",
    letterSpacing: 0.3,
  },
});
</file>

<file path="Tandoori.js">
import React, { useState } from "react";
import {
  View,
  Text,
  StyleSheet,
  TextInput,
  TouchableOpacity,
  FlatList,
  Image,
  Share,
} from "react-native";
import { Ionicons } from "@expo/vector-icons";
import { allFoodItems } from "./Data";
import { useContext } from "react";
import { CartContext } from "./CartContext";
import { WishlistContext } from "./WishlistContext";

export default function TandooriScreen({ navigation }) {
  const { cart, addToCart, removeFromCart } = useContext(CartContext);
  const { toggleWishlist, isWishlisted } = useContext(WishlistContext);
  const [searchText, setSearchText] = useState("");

  const totalItems = cart.reduce((sum, i) => sum + i.quantity, 0);

  const baseItems = allFoodItems.filter((item) => item.category === "Tandoori");
  const filteredItems = baseItems.filter((item) =>
    item.name.toLowerCase().includes(searchText.toLowerCase())
  );

  const shareItem = async (item) => {
    try {
      await Share.share({
        message: `${item.name} - Rs.${item.price}\nTry this delicious food!`,
      });
    } catch (error) {
      console.log(error);
    }
  };

  const renderItem = ({ item }) => {
    const cartItem = cart.find((i) => i.id === item.id);
    const wishlisted = isWishlisted(item.id);

    return (
      <View style={styles.foodCard}>
        {/* LEFT */}
        <View style={{ flex: 1, paddingRight: 10 }}>
          <Text style={styles.foodTitle}>{item.name}</Text>
          <Text style={styles.foodPrice}>Rs.{item.price}</Text>
          <Text style={styles.foodDesc}>{item.description}</Text>

          <View style={styles.iconRow}>
            {/* WISHLIST */}
            <TouchableOpacity style={styles.iconBtn} onPress={() => toggleWishlist(item)}>
              <Ionicons
                name={wishlisted ? "heart" : "heart-outline"}
                size={17}
                color="#ff5722"
              />
            </TouchableOpacity>

            {/* SHARE */}
            <TouchableOpacity style={styles.iconBtn} onPress={() => shareItem(item)}>
              <Ionicons name="share-social-outline" size={17} color="#444" />
            </TouchableOpacity>

            {/* ADD / QTY */}
            <View style={styles.actionBox}>
              {!cartItem ? (
                <TouchableOpacity style={styles.addInlineBtn} onPress={() => addToCart(item)}>
                  <Text style={styles.addInlineText}>ADD +</Text>
                </TouchableOpacity>
              ) : (
                <View style={styles.qtyInlineBox}>
                  <TouchableOpacity onPress={() => removeFromCart(item.id)}>
                    <Text style={styles.qtyBtnText}>-</Text>
                  </TouchableOpacity>
                  <Text style={styles.qtyText}>{cartItem.quantity}</Text>
                  <TouchableOpacity onPress={() => addToCart(item)}>
                    <Text style={styles.qtyBtnText}>+</Text>
                  </TouchableOpacity>
                </View>
              )}
            </View>
          </View>
        </View>

        {/* RIGHT */}
        <View style={styles.imageBox}>
          <Image source={item.image} style={styles.foodImage} />
        </View>
      </View>
    );
  };

  return (
    <View style={styles.container}>

      {/* HEADER */}
      <View style={styles.header}>
        <TouchableOpacity style={styles.iconCircle} onPress={() => navigation.goBack()}>
          <Ionicons name="arrow-back" size={22} color="#1a1a2e" />
        </TouchableOpacity>
        <Text style={styles.title}>Tandoori Menu</Text>
        <TouchableOpacity style={styles.iconCircle} onPress={() => navigation.navigate("Cart")}>
          <Ionicons name="cart-outline" size={20} color="#1a1a2e" />
          {cart.length > 0 && (
            <View style={styles.cartBadge}>
              <Text style={styles.cartText}>{totalItems}</Text>
            </View>
          )}
        </TouchableOpacity>
      </View>

      {/* SEARCH */}
      <View style={styles.searchContainer}>
        <Ionicons name="search" size={18} color="#aaa" />
        <TextInput
          placeholder="Search tandoori..."
          placeholderTextColor="#bbb"
          value={searchText}
          onChangeText={setSearchText}
          style={styles.input}
        />
        {searchText.length > 0 && (
          <TouchableOpacity onPress={() => setSearchText("")}>
            <Ionicons name="close-circle" size={18} color="#bbb" />
          </TouchableOpacity>
        )}
      </View>

      {/* COUNT */}
      <View style={styles.countRow}>
        <Text style={styles.countText}>{filteredItems.length} items</Text>
      </View>

      {/* LIST */}
      <FlatList
        data={filteredItems}
        keyExtractor={(item) => item.id.toString()}
        renderItem={renderItem}
        showsVerticalScrollIndicator={false}
        contentContainerStyle={{ paddingBottom: totalItems > 0 ? 130 : 30, paddingTop: 4 }}
        ListEmptyComponent={
          <View style={styles.emptyBox}>
            <Text style={styles.emptyEmoji}>🍽</Text>
            <Text style={styles.emptyTitle}>Nothing found</Text>
            <Text style={styles.emptySubtitle}>Try a different search</Text>
          </View>
        }
      />

      {/* CART BAR */}
      {totalItems > 0 && (
        <View style={styles.cartBar}>
          <View style={styles.cartBarLeft}>
            <View style={styles.cartCountBadge}>
              <Text style={styles.cartCountText}>{totalItems}</Text>
            </View>
            <Text style={styles.cartBarText}>
              {totalItems} {totalItems === 1 ? "item" : "items"} added
            </Text>
          </View>
          <TouchableOpacity style={styles.checkoutBtn} onPress={() => navigation.navigate("Cart")}>
            <Text style={styles.checkoutText}>Checkout</Text>
            <Ionicons name="arrow-forward" size={14} color="#ff5722" />
          </TouchableOpacity>
        </View>
      )}
    </View>
  );
}

const styles = StyleSheet.create({
  container: { flex: 1, backgroundColor: "#faf9f7" },

  // HEADER
  header: {
    flexDirection: "row",
    justifyContent: "space-between",
    alignItems: "center",
    marginTop: 50,
    paddingHorizontal: 16,
    paddingVertical: 14,
    backgroundColor: "#fff",
    elevation: 3,
    shadowColor: "#000",
    shadowOpacity: 0.07,
    shadowOffset: { width: 0, height: 2 },
    shadowRadius: 6,
  },
  title: {
    fontSize: 17,
    fontFamily: "PoppinsSemiBold",
    color: "#1a1a2e",
    letterSpacing: 0.2,
  },
  iconCircle: {
    padding: 8,
    borderRadius: 22,
    backgroundColor: "#f0ece8",
    shadowColor: "#000",
    shadowOpacity: 0.05,
    shadowOffset: { width: 0, height: 1 },
    shadowRadius: 3,
    elevation: 1,
  },
  cartBadge: {
    position: "absolute",
    top: -4,
    right: -4,
    backgroundColor: "#ff5722",
    borderRadius: 10,
    minWidth: 18,
    height: 18,
    justifyContent: "center",
    alignItems: "center",
    paddingHorizontal: 4,
  },
  cartText: { color: "#fff", fontSize: 9, fontFamily: "PoppinsBold" },

  // SEARCH
  searchContainer: {
    flexDirection: "row",
    alignItems: "center",
    backgroundColor: "#f0ece8",
    marginHorizontal: 16,
    marginTop: 14,
    marginBottom: 4,
    paddingHorizontal: 16,
    paddingVertical: 2,
    borderRadius: 30,
  },
  input: {
    marginLeft: 8,
    flex: 1,
    fontFamily: "PoppinsRegular",
    fontSize: 13.5,
    color: "#1a1a2e",
    paddingVertical: 10,
  },

  // COUNT
  countRow: {
    paddingHorizontal: 20,
    paddingTop: 10,
    paddingBottom: 2,
  },
  countText: {
    fontFamily: "PoppinsRegular",
    fontSize: 12,
    color: "#999",
  },

  // FOOD CARD
  foodCard: {
    flexDirection: "row",
    padding: 14,
    backgroundColor: "#fff",
    marginHorizontal: 14,
    marginVertical: 7,
    borderRadius: 18,
    alignItems: "stretch",
    elevation: 3,
    shadowColor: "#000",
    shadowOpacity: 0.07,
    shadowOffset: { width: 0, height: 3 },
    shadowRadius: 8,
  },
  foodTitle: {
    fontSize: 15,
    fontFamily: "PoppinsSemiBold",
    color: "#1a1a2e",
    letterSpacing: 0.1,
  },
  foodPrice: {
    fontSize: 14.5,
    fontFamily: "PoppinsBold",
    color: "#ff5722",
    marginTop: 3,
  },
  foodDesc: {
    fontSize: 12,
    fontFamily: "PoppinsRegular",
    color: "#888",
    marginTop: 5,
    lineHeight: 17,
    marginRight: 4,
    textAlign: "justify",
    flexShrink: 1,
  },
  iconRow: {
    flexDirection: "row",
    marginTop: 10,
    alignItems: "center",
    flexWrap: "nowrap",
  },
  iconBtn: {
    width: 34,
    height: 34,
    borderRadius: 17,
    borderWidth: 1,
    borderColor: "#ede8e4",
    justifyContent: "center",
    alignItems: "center",
    marginRight: 8,
    backgroundColor: "#faf9f7",
  },
  imageBox: {
    width: 120,
    justifyContent: "center",
    alignItems: "center",
  },
  foodImage: {
    width: 110,
    height: 115,
    borderRadius: 18,
  },
  actionBox: {
    marginLeft: 6,
    width: 90,
    height: 36,
    justifyContent: "center",
    alignItems: "center",
    flexShrink: 1,
  },
  addInlineBtn: {
    width: 90,
    height: 36,
    backgroundColor: "#fff",
    borderWidth: 1.5,
    borderColor: "#ff5722",
    borderRadius: 12,
    justifyContent: "center",
    alignItems: "center",
  },
  addInlineText: {
    color: "#ff5722",
    fontFamily: "PoppinsSemiBold",
    fontSize: 12.5,
    letterSpacing: 0.5,
  },
  qtyInlineBox: {
    width: 90,
    height: 36,
    flexDirection: "row",
    backgroundColor: "#ff5722",
    borderRadius: 12,
    justifyContent: "space-around",
    alignItems: "center",
  },
  qtyBtnText: {
    color: "#fff",
    fontSize: 17,
    fontFamily: "PoppinsMedium",
    marginHorizontal: 6,
  },
  qtyText: {
    color: "#fff",
    fontFamily: "PoppinsBold",
    fontSize: 14,
  },

  // EMPTY STATE
  emptyBox: { alignItems: "center", marginTop: 60 },
  emptyEmoji: { fontSize: 48 },
  emptyTitle: { fontFamily: "PoppinsSemiBold", fontSize: 16, color: "#1a1a2e", marginTop: 12 },
  emptySubtitle: { fontFamily: "PoppinsRegular", fontSize: 13, color: "#999", marginTop: 5 },

  // CART BAR
  cartBar: {
    position: "absolute",
    bottom: 16,
    left: 14,
    right: 14,
    height: 56,
    backgroundColor: "#1a1a2e",
    flexDirection: "row",
    justifyContent: "space-between",
    alignItems: "center",
    paddingHorizontal: 16,
    borderRadius: 32,
    elevation: 10,
    shadowColor: "#000",
    shadowOpacity: 0.22,
    shadowOffset: { width: 0, height: 5 },
    shadowRadius: 10,
  },
  cartBarLeft: { flexDirection: "row", alignItems: "center", gap: 10 },
  cartCountBadge: {
    width: 26,
    height: 26,
    borderRadius: 13,
    backgroundColor: "#ff5722",
    justifyContent: "center",
    alignItems: "center",
  },
  cartCountText: { color: "#fff", fontFamily: "PoppinsBold", fontSize: 11 },
  cartBarText: { color: "rgba(255,255,255,0.85)", fontFamily: "PoppinsMedium", fontSize: 13 },
  checkoutBtn: {
    flexDirection: "row",
    alignItems: "center",
    backgroundColor: "#fff",
    paddingHorizontal: 16,
    paddingVertical: 8,
    borderRadius: 20,
    gap: 5,
  },
  checkoutText: { color: "#ff5722", fontFamily: "PoppinsSemiBold", fontSize: 13 },
});
</file>

<file path="Transaction.js">
import React, { useState } from "react";
import {
  View, Text, StyleSheet, TouchableOpacity, ScrollView, TextInput, Alert,
} from "react-native";
import { Ionicons } from "@expo/vector-icons";
import { useContext } from "react";
import { CartContext } from "./CartContext";
import { placeOrder, processPayment } from "./api";
const PAYMENT_METHODS = [
  { id: "upi", label: "UPI", icon: "phone-portrait-outline" },
  { id: "card", label: "Credit / Debit Card", icon: "card-outline" },
  { id: "cod", label: "Cash on Delivery", icon: "cash-outline" },
];
export default function Transaction({ navigation, route }) {
  const total = route?.params?.total || 320;
  const { clearCart } = useContext(CartContext);
  const [selectedMethod, setSelectedMethod] = useState("upi");
  const [upiId, setUpiId] = useState("");
  const [cardNumber, setCardNumber] = useState("");
  const [expiry, setExpiry] = useState("");
  const [cvv, setCvv] = useState("");
  const [cardName, setCardName] = useState("");
  const [loading, setLoading] = useState(false);
  const handlePay = async () => {
    // Basic validation
    if (selectedMethod === "upi" && !upiId.trim()) {
      Alert.alert("Validation", "Please enter your UPI ID");
      return;
    }
    if (selectedMethod === "card" && (!cardNumber || !cardName || !expiry || !cvv)) {
      Alert.alert("Validation", "Please fill all card details");
      return;
    }
    setLoading(true);
    try {
      // Step 1: Place the order in the database
      const orderData = await placeOrder(
        selectedMethod.toUpperCase(),
        "Default Address",
        parseFloat(total)
      );
      // Step 2: Process payment (simulated)
      await processPayment({
        paymentMethod: selectedMethod,
        amount: parseFloat(total),
        orderId: orderData.id,
        upiId: selectedMethod === "upi" ? upiId : null,
        cardNumber: selectedMethod === "card" ? cardNumber : null,
      });
      // Step 3: Clear the cart
      await clearCart();
      // Step 4: Navigate to success
      navigation.navigate("OrderSuccess");
    } catch (error) {
      Alert.alert("Payment Failed", error.message || "Something went wrong. Please try again.");
    } finally {
      setLoading(false);
    }
  };
  return (
    <View style={styles.container}>
      <View style={styles.header}>
        <TouchableOpacity style={styles.iconCircle} onPress={() => navigation.goBack()}>
          <Ionicons name="arrow-back" size={20} color="#1a1a2e" />
        </TouchableOpacity>
        <Text style={styles.headerTitle}>Payment</Text>
        <View style={{ width: 38 }} />
      </View>
      <ScrollView showsVerticalScrollIndicator={false} contentContainerStyle={{ paddingBottom: 120 }}>
        <View style={styles.amountCard}>
          <Text style={styles.amountLabel}>Amount to Pay</Text>
          <Text style={styles.amountValue}>₹{total}</Text>
          <View style={styles.secureRow}>
            <Ionicons name="shield-checkmark" size={13} color="#4caf50" />
            <Text style={styles.secureText}>Secured Payment</Text>
          </View>
        </View>
        <Text style={styles.sectionLabel}>Select Payment Method</Text>
        <View style={styles.methodsCard}>
          {PAYMENT_METHODS.map((m, i) => (
            <TouchableOpacity
              key={m.id}
              style={[styles.methodRow, i < PAYMENT_METHODS.length - 1 && styles.methodBorder, selectedMethod === m.id && styles.methodRowActive]}
              onPress={() => setSelectedMethod(m.id)}
            >
              <View style={[styles.methodIconBox, selectedMethod === m.id && styles.methodIconBoxActive]}>
                <Ionicons name={m.icon} size={18} color={selectedMethod === m.id ? "#fff" : "#ff5722"} />
              </View>
              <Text style={[styles.methodLabel, selectedMethod === m.id && styles.methodLabelActive]}>{m.label}</Text>
              <View style={[styles.radio, selectedMethod === m.id && styles.radioActive]}>
                {selectedMethod === m.id && <View style={styles.radioDot} />}
              </View>
            </TouchableOpacity>
          ))}
        </View>
        {selectedMethod === "upi" && (
          <View style={styles.fieldsCard}>
            <Text style={styles.fieldTitle}>Enter UPI ID</Text>
            <View style={styles.inputWrapper}>
              <Ionicons name="at" size={18} color="#aaa" style={styles.inputIcon} />
              <TextInput placeholder="yourname@upi" placeholderTextColor="#bbb" style={styles.input}
                value={upiId} onChangeText={setUpiId} autoCapitalize="none" />
            </View>
          </View>
        )}
        {selectedMethod === "card" && (
          <View style={styles.fieldsCard}>
            <Text style={styles.fieldTitle}>Card Details</Text>
            <View style={styles.inputWrapper}>
              <Ionicons name="card-outline" size={18} color="#aaa" style={styles.inputIcon} />
              <TextInput placeholder="Card Number" placeholderTextColor="#bbb" style={styles.input}
                value={cardNumber} onChangeText={setCardNumber} keyboardType="numeric" maxLength={19} />
            </View>
            <View style={styles.inputWrapper}>
              <Ionicons name="person-outline" size={18} color="#aaa" style={styles.inputIcon} />
              <TextInput placeholder="Name on Card" placeholderTextColor="#bbb" style={styles.input}
                value={cardName} onChangeText={setCardName} />
            </View>
            <View style={styles.rowInputs}>
              <View style={[styles.inputWrapper, { flex: 1, marginRight: 10 }]}>
                <Ionicons name="calendar-outline" size={18} color="#aaa" style={styles.inputIcon} />
                <TextInput placeholder="MM/YY" placeholderTextColor="#bbb" style={styles.input}
                  value={expiry} onChangeText={setExpiry} keyboardType="numeric" maxLength={5} />
              </View>
              <View style={[styles.inputWrapper, { flex: 1 }]}>
                <Ionicons name="lock-closed-outline" size={18} color="#aaa" style={styles.inputIcon} />
                <TextInput placeholder="CVV" placeholderTextColor="#bbb" style={styles.input}
                  value={cvv} onChangeText={setCvv} keyboardType="numeric" maxLength={3} secureTextEntry />
              </View>
            </View>
          </View>
        )}
        {selectedMethod === "cod" && (
          <View style={styles.codCard}>
            <Ionicons name="information-circle-outline" size={20} color="#ff5722" />
            <Text style={styles.codText}>
              Please keep exact change ready at the time of delivery.
            </Text>
          </View>
        )}
        <Text style={styles.sectionLabel}>Order Summary</Text>
        <View style={styles.summaryCard}>
          <View style={styles.summaryRow}>
            <Text style={styles.summaryLabel}>Subtotal</Text>
            <Text style={styles.summaryValue}>₹{Math.round(total / 1.1)}</Text>
          </View>
          <View style={styles.summaryRow}>
            <Text style={styles.summaryLabel}>Tax (10%)</Text>
            <Text style={styles.summaryValue}>₹{Math.round(total - total / 1.1)}</Text>
          </View>
          <View style={styles.summaryRow}>
            <Text style={styles.summaryLabel}>Delivery</Text>
            <Text style={[styles.summaryValue, { color: "#4caf50" }]}>FREE</Text>
          </View>
          <View style={styles.divider} />
          <View style={styles.summaryRow}>
            <Text style={styles.totalLabel}>Total</Text>
            <Text style={styles.totalValue}>₹{total}</Text>
          </View>
        </View>
      </ScrollView>
      <View style={styles.footer}>
        <TouchableOpacity style={styles.payBtn} onPress={handlePay} disabled={loading}>
          {loading ? (
            <Text style={styles.payBtnText}>Processing...</Text>
          ) : (
            <>
              <Ionicons name="lock-closed" size={16} color="#fff" style={{ marginRight: 8 }} />
              <Text style={styles.payBtnText}>Pay ₹{total}</Text>
            </>
          )}
        </TouchableOpacity>
      </View>
    </View>
  );
}
const styles = StyleSheet.create({
  container: { flex: 1, backgroundColor: "#faf9f7" },
  header: { flexDirection: "row", justifyContent: "space-between", alignItems: "center", marginTop: 54, paddingHorizontal: 18, marginBottom: 20 },
  headerTitle: { fontSize: 17, fontFamily: "PoppinsSemiBold", color: "#1a1a2e" },
  iconCircle: { padding: 8, borderRadius: 22, backgroundColor: "#f0ece8", elevation: 2 },
  amountCard: { backgroundColor: "#1a1a2e", marginHorizontal: 16, borderRadius: 22, padding: 22, alignItems: "center", marginBottom: 24, elevation: 4 },
  amountLabel: { fontFamily: "PoppinsRegular", fontSize: 13, color: "rgba(255,255,255,0.55)", marginBottom: 6 },
  amountValue: { fontFamily: "PoppinsBold", fontSize: 38, color: "#fff", letterSpacing: 1 },
  secureRow: { flexDirection: "row", alignItems: "center", marginTop: 10, gap: 5 },
  secureText: { fontFamily: "PoppinsRegular", fontSize: 11, color: "#4caf50" },
  sectionLabel: { fontFamily: "PoppinsSemiBold", fontSize: 12, color: "#aaa", letterSpacing: 1, textTransform: "uppercase", marginHorizontal: 20, marginBottom: 10 },
  methodsCard: { backgroundColor: "#fff", marginHorizontal: 16, borderRadius: 18, elevation: 2, marginBottom: 16, overflow: "hidden" },
  methodRow: { flexDirection: "row", alignItems: "center", paddingVertical: 14, paddingHorizontal: 16, gap: 14 },
  methodBorder: { borderBottomWidth: 1, borderBottomColor: "#f0ece8" },
  methodRowActive: { backgroundColor: "#fff8f6" },
  methodIconBox: { width: 38, height: 38, borderRadius: 11, backgroundColor: "#fff0eb", justifyContent: "center", alignItems: "center" },
  methodIconBoxActive: { backgroundColor: "#ff5722" },
  methodLabel: { flex: 1, fontFamily: "PoppinsMedium", fontSize: 14, color: "#555" },
  methodLabelActive: { color: "#1a1a2e", fontFamily: "PoppinsSemiBold" },
  radio: { width: 20, height: 20, borderRadius: 10, borderWidth: 2, borderColor: "#ddd", justifyContent: "center", alignItems: "center" },
  radioActive: { borderColor: "#ff5722" },
  radioDot: { width: 10, height: 10, borderRadius: 5, backgroundColor: "#ff5722" },
  fieldsCard: { backgroundColor: "#fff", marginHorizontal: 16, borderRadius: 18, padding: 16, elevation: 2, marginBottom: 16 },
  fieldTitle: { fontFamily: "PoppinsSemiBold", fontSize: 14, color: "#1a1a2e", marginBottom: 12 },
  inputWrapper: { flexDirection: "row", alignItems: "center", backgroundColor: "#faf9f7", borderRadius: 12, paddingHorizontal: 12, paddingVertical: 2, marginBottom: 10, borderWidth: 1, borderColor: "#ede8e4" },
  inputIcon: { marginRight: 8 },
  input: { flex: 1, fontFamily: "PoppinsRegular", fontSize: 14, color: "#1a1a2e", paddingVertical: 11 },
  rowInputs: { flexDirection: "row" },
  codCard: { flexDirection: "row", backgroundColor: "#fff0eb", marginHorizontal: 16, borderRadius: 14, padding: 14, gap: 10, marginBottom: 16, borderWidth: 1, borderColor: "#ffd5c8", alignItems: "flex-start" },
  codText: { flex: 1, fontFamily: "PoppinsRegular", fontSize: 13, color: "#555", lineHeight: 20 },
  summaryCard: { backgroundColor: "#fff", marginHorizontal: 16, borderRadius: 18, padding: 18, elevation: 2, marginBottom: 16 },
  summaryRow: { flexDirection: "row", justifyContent: "space-between", marginBottom: 8 },
  summaryLabel: { fontFamily: "PoppinsRegular", fontSize: 13.5, color: "#888" },
  summaryValue: { fontFamily: "PoppinsMedium", fontSize: 13.5, color: "#1a1a2e" },
  divider: { height: 1, backgroundColor: "#f0ece8", marginVertical: 10 },
  totalLabel: { fontFamily: "PoppinsSemiBold", fontSize: 15, color: "#1a1a2e" },
  totalValue: { fontFamily: "PoppinsBold", fontSize: 15, color: "#ff5722" },
  footer: { position: "absolute", bottom: 20, left: 16, right: 16 },
  payBtn: { backgroundColor: "#ff5722", paddingVertical: 17, borderRadius: 18, alignItems: "center", flexDirection: "row", justifyContent: "center", elevation: 5 },
  payBtnText: { color: "#fff", fontFamily: "PoppinsSemiBold", fontSize: 15, letterSpacing: 0.4 },
});
</file>

<file path="WishlistContext.js">
import React, { createContext, useState, useEffect } from "react";
import AsyncStorage from "@react-native-async-storage/async-storage";
import * as api from "./api";

export const WishlistContext = createContext();

export function WishlistProvider({ children }) {
  const [wishlist, setWishlist] = useState([]);

  useEffect(() => {
    AsyncStorage.getItem("token").then((token) => {
      if (token) fetchWishlist();
    });
  }, []);


  const toggleWishlist = async (itemId) => {

    // ✅ update UI first
    setWishlist((prev) => {
      if (prev.includes(itemId)) {
        return prev.filter((id) => id !== itemId);
      } else {
        return [...prev, itemId];
      }
    });

    // ✅ sync with backend
    const token = await AsyncStorage.getItem("token");
    if (token) {
      try {
        await api.toggleWishlist(itemId); // 🔥 IMPORTANT
      } catch (e) {
        console.log("Wishlist sync error:", e);
      }
    }
  };

  const isWishlisted = (id) =>
    !!wishlist.find((i) => (i.menuItemId ?? i.id) === id);

  return (
    <WishlistContext.Provider value={{ wishlist, toggleWishlist, isWishlisted, fetchWishlistFromServer }}>
      {children}
    </WishlistContext.Provider>
  );
}
</file>

<file path="WishlistScreen.js">
import React, { useContext } from "react";
import {
  View,
  Text,
  StyleSheet,
  FlatList,
  Image,
  TouchableOpacity,
} from "react-native";
import { Ionicons } from "@expo/vector-icons";
import { WishlistContext } from "./WishlistContext";
import { CartContext } from "./CartContext";
import { allFoodItems } from "./Data";


export default function WishlistScreen({ navigation }) {
  const { wishlist, toggleWishlist } = useContext(WishlistContext);
  const wishlistItems = allFoodItems.filter(item =>
    wishlist.includes(item.id)
  );
  const { cart, addToCart, removeFromCart } = useContext(CartContext);

  const totalItems = cart.reduce((sum, i) => sum + i.quantity, 0);

  const renderItem = ({ item }) => {
    // Server wishlist items have menuItemId, local items have id
    const itemId = item.menuItemId ?? item.id;
    const cartItem = cart.find((i) => i.id === itemId);

    // Build a cart-compatible item (needs id field for addToCart/removeFromCart)
    const cartCompatibleItem = { ...item, id: itemId };

    return (
      <View style={styles.foodCard}>
        <View style={{ flex: 1, paddingRight: 10 }}>
          <Text style={styles.foodTitle}>{item.name}</Text>
          <Text style={styles.price}>₹{item.price}</Text>
          <Text style={styles.desc}>{item.description}</Text>

          <View style={styles.iconRow}>
            <TouchableOpacity
              style={styles.iconBtn}
              onPress={() => toggleWishlist(item.id)}
            >
              <Ionicons name="heart" size={18} color="#ff5722" />
            </TouchableOpacity>

            <View style={styles.actionBox}>
              {!cartItem ? (
                <TouchableOpacity
                  style={styles.addInlineBtn}
                  onPress={() => addToCart(cartCompatibleItem)}
                >
                  <Text style={styles.addInlineText}>ADD +</Text>
                </TouchableOpacity>
              ) : (
                <View style={styles.qtyInlineBox}>
                  <TouchableOpacity onPress={() => removeFromCart(itemId)}>
                    <Text style={styles.qtyBtn}>-</Text>
                  </TouchableOpacity>
                  <Text style={styles.qtyText}>{cartItem.quantity}</Text>
                  <TouchableOpacity onPress={() => addToCart(cartCompatibleItem)}>
                    <Text style={styles.qtyBtn}>+</Text>
                  </TouchableOpacity>
                </View>
              )}
            </View>
          </View>
        </View>

        <View style={styles.imageBox}>
          <Image source={item.image} style={styles.image} />
        </View>
      </View>
    );
  };

  return (
    <View style={styles.container}>
      {/* HEADER */}
      <View style={styles.header}>
        <TouchableOpacity
          onPress={() => navigation.goBack()}
          style={styles.iconCircle}
        >
          <Ionicons name="arrow-back" size={20} color="#1a1a2e" />
        </TouchableOpacity>
        <Text style={styles.title}>My Wishlist</Text>
        <TouchableOpacity
          style={styles.iconCircle}
          onPress={() => navigation.navigate("Cart")}
        >
          <Ionicons name="cart-outline" size={20} color="#1a1a2e" />
          {cart.length > 0 && (
            <View style={styles.cartBadge}>
              <Text style={styles.cartText}>
                {cart.reduce((sum, i) => sum + i.quantity, 0)}
              </Text>
            </View>
          )}
        </TouchableOpacity>
      </View>

      {/* EMPTY STATE */}
      {wishlist.length === 0 ? (
        <View style={styles.emptyContainer}>
          <View style={styles.emptyIconCircle}>
            <Ionicons name="heart-outline" size={40} color="#ff5722" />
          </View>
          <Text style={styles.emptyText}>No favourites yet</Text>
          <Text style={styles.emptySubText}>
            Tap the heart icon on any item to save it here
          </Text>
        </View>
      ) : (
        <FlatList
          data={wishlistItems}
          // WITH:
          keyExtractor={(item, index) => (item.menuItemId ?? item.id ?? index).toString()}
          renderItem={renderItem}
          showsVerticalScrollIndicator={false}
          contentContainerStyle={{
            paddingHorizontal: 14,
            paddingTop: 10,
            paddingBottom: totalItems > 0 ? 140 : 30,
          }}
        />
      )}

      {/* CART BAR */}
      {totalItems > 0 && (
        <View style={styles.cartBar}>
          <View style={styles.cartBarLeft}>
            <View style={styles.cartCountBadge}>
              <Text style={styles.cartCountText}>{totalItems}</Text>
            </View>
            <Text style={styles.cartBarText}>
              {totalItems} {totalItems === 1 ? "item" : "items"} added
            </Text>
          </View>
          <TouchableOpacity
            style={styles.checkoutBtn}
            onPress={() => navigation.navigate("Cart")}
          >
            <Text style={styles.checkoutText}>Checkout</Text>
            <Ionicons name="arrow-forward" size={14} color="#ff5722" />
          </TouchableOpacity>
        </View>
      )}
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#faf9f7",
  },

  // HEADER
  header: {
    flexDirection: "row",
    justifyContent: "space-between",
    alignItems: "center",
    marginTop: 54,
    paddingHorizontal: 18,
    paddingVertical: 12,
    backgroundColor: "#faf9f7",
  },
  title: {
    fontSize: 17,
    fontFamily: "PoppinsSemiBold",
    color: "#1a1a2e",
  },
  iconCircle: {
    padding: 8,
    borderRadius: 22,
    backgroundColor: "#f0ece8",
    shadowColor: "#000",
    shadowOpacity: 0.06,
    shadowOffset: { width: 0, height: 2 },
    shadowRadius: 4,
    elevation: 2,
  },
  cartBadge: {
    position: "absolute",
    top: -4,
    right: -4,
    backgroundColor: "#ff5722",
    borderRadius: 10,
    minWidth: 18,
    height: 18,
    justifyContent: "center",
    alignItems: "center",
    paddingHorizontal: 4,
  },
  cartText: {
    color: "#fff",
    fontSize: 9,
    fontFamily: "PoppinsBold",
  },

  // EMPTY
  emptyContainer: {
    flex: 1,
    alignItems: "center",
    justifyContent: "center",
  },
  emptyIconCircle: {
    width: 90,
    height: 90,
    borderRadius: 45,
    backgroundColor: "#fff0eb",
    justifyContent: "center",
    alignItems: "center",
    marginBottom: 18,
  },
  emptyText: {
    fontSize: 17,
    fontFamily: "PoppinsSemiBold",
    color: "#1a1a2e",
    marginTop: 4,
  },
  emptySubText: {
    fontSize: 13,
    fontFamily: "PoppinsRegular",
    color: "#999",
    marginTop: 8,
    textAlign: "center",
    paddingHorizontal: 40,
    lineHeight: 20,
  },

  // FOOD CARD
  foodCard: {
    flexDirection: "row",
    padding: 14,
    backgroundColor: "#fff",
    marginVertical: 7,
    borderRadius: 18,
    alignItems: "stretch",
    elevation: 3,
  },
  foodTitle: {
    fontSize: 15,
    fontFamily: "PoppinsSemiBold",
    color: "#1a1a2e",
  },
  price: {
    fontSize: 14.5,
    fontFamily: "PoppinsBold",
    color: "#ff5722",
    marginTop: 3,
  },
  desc: {
    fontSize: 12,
    fontFamily: "PoppinsRegular",
    color: "#888",
    marginTop: 5,
    lineHeight: 17,
    marginRight: 4,
    textAlign: "justify",
    flexShrink: 1,
  },
  iconRow: {
    flexDirection: "row",
    marginTop: 10,
    alignItems: "center",
  },
  iconBtn: {
    width: 34,
    height: 34,
    borderRadius: 17,
    borderWidth: 1,
    borderColor: "#ede8e4",
    justifyContent: "center",
    alignItems: "center",
    marginRight: 10,
    backgroundColor: "#faf9f7",
  },
  imageBox: {
    width: 120,
    justifyContent: "center",
    alignItems: "center",
  },
  image: {
    width: 110,
    height: 115,
    borderRadius: 18,
  },
  actionBox: {
    width: 90,
    height: 36,
    justifyContent: "center",
    alignItems: "center",
    flexShrink: 1,
  },
  addInlineBtn: {
    width: 90,
    height: 36,
    backgroundColor: "#fff",
    borderWidth: 1.5,
    borderColor: "#ff5722",
    borderRadius: 12,
    justifyContent: "center",
    alignItems: "center",
  },
  addInlineText: {
    color: "#ff5722",
    fontFamily: "PoppinsSemiBold",
    fontSize: 12.5,
    letterSpacing: 0.5,
  },
  qtyInlineBox: {
    width: 90,
    height: 36,
    flexDirection: "row",
    backgroundColor: "#ff5722",
    borderRadius: 12,
    justifyContent: "space-around",
    alignItems: "center",
  },
  qtyBtn: {
    color: "#fff",
    fontSize: 17,
    fontFamily: "PoppinsMedium",
    marginHorizontal: 6,
  },
  qtyText: {
    color: "#fff",
    fontFamily: "PoppinsBold",
    fontSize: 14,
  },

  // CART BAR
  cartBar: {
    position: "absolute",
    bottom: 14,
    left: 14,
    right: 14,
    height: 56,
    backgroundColor: "#1a1a2e",
    flexDirection: "row",
    justifyContent: "space-between",
    alignItems: "center",
    paddingHorizontal: 16,
    borderRadius: 32,
    elevation: 10,
  },
  cartBarLeft: {
    flexDirection: "row",
    alignItems: "center",
    gap: 10,
  },
  cartCountBadge: {
    width: 26,
    height: 26,
    borderRadius: 13,
    backgroundColor: "#ff5722",
    justifyContent: "center",
    alignItems: "center",
  },
  cartCountText: {
    color: "#fff",
    fontFamily: "PoppinsBold",
    fontSize: 11,
  },
  cartBarText: {
    color: "rgba(255,255,255,0.85)",
    fontFamily: "PoppinsMedium",
    fontSize: 13,
  },
  checkoutBtn: {
    flexDirection: "row",
    alignItems: "center",
    backgroundColor: "#fff",
    paddingHorizontal: 16,
    paddingVertical: 8,
    borderRadius: 20,
    gap: 5,
  },
  checkoutText: {
    color: "#ff5722",
    fontFamily: "PoppinsSemiBold",
    fontSize: 13,
  },
});
</file>

</files>
